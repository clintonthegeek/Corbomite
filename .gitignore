# Build directories
build/
build-*/
cmake-build-*/

# IDE
.idea/
.vscode/
*.swp
*~
compile_commands.json

# Generated
*.moc
moc_*.cpp
ui_*.h
qrc_*.cpp
*.autosave
*.o

# Claude Code per-project state
.claude/

# Corbomite runtime
.corbomite/

# OS
.DS_Store
Thumbs.db
.worktrees/
**/__MACOSX/

# Upstream Obsidian reference vaults cloned as nested git repos.
# Only testvaults/starter-vault/ and testvaults/DevVault/ are fixtures we own
# (relied on by tst_e2e_gui / tst_mainwindow_action_wiring respectively).
# Everything else — scratch vaults, sample downloads — lives in samplevaults/.
testvaults/jsoncanvas/
testvaults/obsidian-api/
testvaults/obsidian-canvas-candy/
testvaults/obsidian-developer-docs/
testvaults/obsidian-help/
testvaults/obsidian-hub-main/
testvaults/obsidian-skills/

# Local scratch/sample vaults not relied upon by any test
samplevaults/

# Packaging build products (scripts stay tracked)
packaging/appimage/tools/
packaging/appimage/AppDir/
packaging/appimage/out/
packaging/ubuntu/out/
packaging/arch/pkg/
packaging/arch/src/
packaging/arch/*.pkg.tar.*
packaging/arch/*.log
packaging/arch/corbomite-*/

# Local scratch
Testing/
docs/archive/HandbookGraphDrawing.txt

# Obsidian runtime state (regenerated every session — caches, workspace, trash)
testvaults/**/.obsidian/
testvaults/**/.trash/

# Local scratch vaults / scratch files inside fixture vaults
testvaults/DevVault/
testvaults/starter-vault/Parser Tests/
testvaults/starter-vault/dfsfg.canvas
testvaults/starter-vault/My new Note.md
testvaults/starter-vault/TestCreate.canvas
testvaults/starter-vault/Untitled.canvas
testvaults/starter-vault/heres a new notw.md
testvaults/starter-vault/katabasis anabasis.md

# Reference-only Obsidian clones (local study, not part of project)
ObsClones/
qmarkdowntextedit
