# PROJECT-STATE

> **Living document.** Single source of truth for "where we are right now" on the Obsidian-compatibility roadmap. Keep under 200 lines by offloading closeout prose to `decisions-archive.md`. Follow Ritual 2/3 in `CONTRIBUTING-OPS.md` after every meaningful work session.

**Last updated:** 2026-04-25 — Cluster V.2 closed (5 phases, 7 commits `6f737933..bb12fbbd`, retro at `cluster-retros/cluster-v2.md`). `VaultConfig::mergeJson` helper + 3-page settings persistence + e2e cache verification + autosave applier + `WorkspaceWindow` standalone-facade deletion. Next: Cluster Z brainstorm (linked views + active-leaf tracking).

---

## Current focus

**Cluster V.2 closed.** Editor/Workspace debt cleanup landed across 5 phases (`6f737933..bb12fbbd`): `VaultConfig::mergeJson` unknown-key-preserving helper, `MainWindow::applyVaultPortableSettings` persisting Appearance/Daily Notes/Templates kcfg keys to `.obsidian/*.json`, end-to-end CachedMetadataStore round-trip test (the wiring itself was already in place — surprise discovered during audit), `applyAutosaveDelay` applier, and `WorkspaceWindow` standalone QWidget facade deletion (class shrinks to identity token). Next: Cluster Z brainstorm (active-leaf tracking + linked views) per Y-first sequencing decision.

---

## Roadmap (20 clusters — 15 parity + 3 UI-chrome + 1 post-parity + 1 internal refactor)

Status legend: `Not started` · `Plan-needed` (no cluster plan yet) · `Stub plan` (sketch exists, expand before dispatch) · `In progress (phase N)` · `Blocked — waiting on X` · `Done` · `Deferred`.

| Cluster | Title | Plan | Status | Blocks / Notes |
|---|---|---|---|---|
| A | Link / frontmatter correctness | [full](superpowers/plans/archive/2026-04-14-cluster-a-link-frontmatter-correctness.md) | Done | Unblocks D, F, I, J, K, L |
| B | Vault I/O | [full](superpowers/plans/archive/2026-04-14-cluster-b-vault-io.md) | Done | Unblocks C, E, G, H, N |
| C | Lifecycle / plugin primitives | [full](superpowers/plans/archive/2026-04-14-cluster-c-lifecycle-plugin-primitives.md) | Done (primitives + hookup) | Phases 1–3 + 4a landed 2026-04-15. Unblocks G, H, N. Phase 4b-d (SessionDestroyer, hotkeys.json I/O, Modal Scope push/pop) deferred — build when consumers demand |
| D | Search / suggester parity | [full](superpowers/plans/archive/2026-04-14-cluster-d-search-suggester-parity.md) | Done | Landed 2026-04-15 across 5 commits (70f7d64, 39ac6e1, f939ba9, fd612e5, f3367f7). Unblocks H, I |
| E | Three-mode pivot (Source/LivePreview/Reading) | [full](superpowers/plans/archive/2026-04-14-cluster-e-markoff-three-mode-pivot.md) | Done | Landed 2026-04-15 across 18 commits (7 phases + fork Phases 1+2 prereq + docs). MVP contract satisfied: three-mode encoding round-trips to `workspace.json` via `PaneLeaf.unknown["eState"]`; `Corbomite::SourceEditor` (qutepart-corbomite-backed) + `Markoff::Editor` (existing) + `Corbomite::ReadingView::ReadingView` (greenfield) live as `QStackedWidget` children of `NoteEditorWidget`; visual-line float scroll + fold persist per-leaf; ReadingView has section recycling + 10240-byte async parse + 5ms/10-section frame budget + virtualization + heading fold. See `cluster-retros/cluster-e.md`. Unblocks J. |
| F | Templates / Daily Notes / Moment | [full](superpowers/plans/archive/2026-04-15-cluster-f-templates-daily-notes-moment.md) | Done | Landed 2026-04-15 (5 phases + doc closeout). Enables vault-portable template + daily-note config |
| G | Views hierarchy + TextFileView contract | [Part 1](superpowers/plans/archive/2026-04-15-cluster-g-views-hierarchy.md), [Part 2](superpowers/plans/archive/2026-04-15-cluster-g-part2-workspace.md) | Done | Full cluster closed 2026-04-16. Part 1 (15 commits) + Part 2 infra (11 commits) + Tasks 9-10 (3 commits on top of pre-existing `e3143f1` delete). See `cluster-retros/cluster-g.md`. Unblocks M, N. |
| H | Menus / hover / suggester UI | [full](superpowers/plans/archive/2026-04-14-cluster-h-menus-hover-suggester-ui.md) | Done | Landed 2026-04-15 across 5 commits. Unblocks N |
| I | MetadataCache parity | [full](superpowers/plans/archive/2026-04-15-cluster-i-metadatacache-parity.md) | Done | Landed 2026-04-15 (10 commits across 8 phases). Unblocks F, J, K (partial), L |
| J | Embed / rendering primitives | [full](superpowers/plans/archive/2026-04-15-cluster-j-embed-rendering.md) | Done | Landed 2026-04-15 across 18 commits (6 phases). HoverPopover renders math/mermaid/wiki-links/images/nested embeds via `EmbedRenderer` + ReadingView; internal registries (`EmbedRegistry`, `PostProcessorRegistry`, `CodeBlockProcessorRegistry`) + lifecycle types (`MarkdownRenderChild`, `EmbedDepthGuard`) live in `libs/core/`; `Markoff::LinkRenderer` + `ReadingView::LinkRenderer`/`EmbedRenderer` consolidate inline link emission. See `cluster-retros/cluster-j.md`. Unblocks K, L-extensions, N. |
| K | Bases | [full](superpowers/plans/archive/2026-04-17-cluster-k-bases.md) + [DSL addendum](obsidian-audit/addenda/2026-04-17-bases-formula-dsl.md) | Done | Closed 2026-04-17 across 9 phases (~37 commits). Hand-rolled Pratt-parser formula engine + `std::shared_ptr<Value>` 18-type hierarchy + `.base` YAML round-trip + QTableView-backed BasesView registered as a built-in view for extension `.base`. MVP parity: 5/5 "must work" audit §9 features (open/render/filter/sort/inline-edit). 12 deferred follow-ups (cards/list layouts, KPluginFactory wrapping, rich widgets, view-rename wikilink rewrite, embed-in-markdown, clipboard export, formula editor, NewItemMenu, per-view undo, column-reorder persist, multi-key sort UI, group-header render). Retro at [`cluster-retros/cluster-k.md`](cluster-retros/cluster-k.md). |
| L | Properties panel | — | Done | Landed 2026-04-15 as single-phase normal task (commit 89b1df4); 6 editor widget types + 500ms debounced writeback via FrontMatterWriter |
| M | Internal-plugin feature audits (Graph, Canvas) | — | Deferred | Treat as two normal tasks; no cluster plan needed |
| N | Plugin-ready surfaces | [full](superpowers/plans/archive/2026-04-17-cluster-n-plugin-ready-surfaces.md) | Done | Closed 2026-04-17 across 19 commits (`2e4e3a4` → `ec32fc0`) in 5 phases. VaultProxy QObject + SearchProxy + PluginContext::search + stop-gap deletion + QtKeychain SecretStorage + plugin data.json + corbomite_add_plugin CMake helper + CorbomiteConfig.cmake + MinVersion/ApiLevel enforcement + note-stats reference plugin + docs/plugin-development/ (1737 lines). Retro at `cluster-retros/cluster-n.md`. |
| O | Advanced query layer (graph + enriched FTS) | [scouting](superpowers/plans/2026-04-14-cluster-o-query-layer-SCOUTING.md) | Scouting doc | Post-parity. Expand only after A/B/I/K land and demand signals materialise |
| P | Graffodil adoption (libs/forcegraph + libs/canvas) | [scouting](superpowers/plans/2026-04-14-cluster-p-graffodil-adoption-SCOUTING.md) | Scouting doc | Internal refactor. Parallelisable with parity roadmap. Expand after Graffodil API stabilises (2–3 wk observation) + Cluster A lands |
| Q | Internal-plugin wrapping + permissions | [full](superpowers/plans/archive/2026-04-16-cluster-q-internal-plugin-wrapping.md) | Done | Closed 2026-04-17. 8 InternalPlugins shipped: Backlinks, Outlinks, Outline, Properties, Search, FileExplorer, LocalGraph (full), GraphView (shell — main-area view-type registration deferred). PluginsPage in SettingsDialog. Permission-gated proxies + lifecycle. Retro at `cluster-retros/cluster-q.md`. |
| R | View-header menus (hamburger "…" on every leaf) | [full](superpowers/plans/archive/2026-04-19-cluster-r-view-header-menus.md) | Done | Closed 2026-04-19 across 4 phases (~20 commits `09296582` → `c5448b38`). P1 substrate (MenuSectionHelper canonical order + submenu + `View::onMoreOptionsMenu` hook) + P2 EditableFileView universal items + P3 per-view specialisations (MarkdownView / CanvasFileView / GraphView) + P4 inline backlinks-in-document PostProcessor. Disabled-placeholder slots for G#6 / S / T / Qutepart-fork P3. Absorbs Cluster H follow-up #2 (menu-construction-site migrations) partially; residue at EditorViewSpace tab bar + TextControl + CorbomiteMDI Sidebar tracked in Cluster U scope. Retro at [`cluster-retros/cluster-r.md`](cluster-retros/cluster-r.md); 5 follow-ups captured (top two: Cluster U File Explorer, plugin mid-construction menu injection). Spec at `superpowers/specs/2026-04-19-cluster-r-view-header-menus-design.md`. 10 audit addenda at `obsidian-audit/addenda/2026-04-19-*.md` + one new addendum `2026-04-19-file-explorer-context-menu.md`. |
| U | File Explorer enhancements (right-click + keyboard) | [scouting](superpowers/plans/2026-04-19-cluster-u-file-explorer-enhancements-SCOUTING.md) | Scouting doc | Added 2026-04-19 as Cluster R closeout follow-up #1. Reuses Cluster R primitives (FileManager prompt modals + Platform + PathUtils + MenuSectionHelper). 6 phases sketched: right-click substrate, file-row items, folder-row items, empty-area menu, keyboard (F2/Delete/Enter/Ctrl-click), Cluster H follow-up #2 residue. 3–5 days once expanded. Blocked on: Cluster R follow-up #2 (plugin menu-injection ordering) landing first or same-phase, and `TFolder*` / `TAbstractFile*` modal signature decision. Audit addendum at `obsidian-audit/addenda/2026-04-19-file-explorer-context-menu.md`. |
| S | Bookmarks core plugin | [full](superpowers/plans/archive/2026-04-20-cluster-s-bookmarks.md) | Done | Closed 2026-04-20 across ~8 commits. `src/plugins/bookmarks/` InternalPlugin with `.obsidian/bookmarks.json` Obsidian-compat round-trip (unknown-key preservation), right-dock `BookmarksView` with drag-reorder + rename/move-to-group/delete context menu, 7 `bookmarks:*` commands (2 functional, 5 gated on missing WorkspaceController accessors — tracked as Cluster V follow-up), full `BookmarkModal` with group picker, VaultProxy rename/delete sync, Cluster R "Bookmark…" hamburger slot goes live. `CommandRegistrar::addCommandRaw` added for canonical Obsidian-id registration. Retro at [`cluster-retros/cluster-s.md`](cluster-retros/cluster-s.md). |
| T | File Recovery plugin (Version History) | — | Deferred (post-parity) | Ship only if a user asks. Covers periodic snapshots + modal UI + restore. Cluster R ships a disabled "Open version history" menu slot that activates when T lands. Addendum at `obsidian-audit/addenda/2026-04-19-file-recovery-plugin.md`. |
| V | Editor & Workspace UI surfacing | [full](superpowers/plans/2026-04-20-cluster-v-editor-workspace-ui-surfacing.md) + [spec](superpowers/specs/2026-04-20-cluster-v-editor-workspace-ui-surfacing-design.md) | Done | Closed 2026-04-20. Phases 1 + 2+3 shipped Find/Zoom/About wiring, `editor_toggle_mode` Ctrl+E cycle, `View::zoomIn/Out/Reset` + `MarkdownView` delegation, `KColorSchemeManager` theme applier, ~40-entry KActionCollection (Edit/View/Format/Heading/Insert/Table/Fold), Editor Mode radio submenu, `corbomiteui.rc.in` v10 menu tree, `refreshEditorActions`, `tst_mainwindow_action_wiring`. **Phase 4 absorbed by Markoff C5** — ReadingView hover + Corbomite HoverPopover integration shipped in `eef21e8e` alongside Markoff `v0.4.0`. Retro at [`cluster-retros/cluster-v.md`](cluster-retros/cluster-v.md). Debt cleanup tracked in Cluster V.2. |
| V.2 | Editor/Workspace debt cleanup | [full](superpowers/plans/archive/2026-04-25-cluster-v2-debt-cleanup.md) + [scouting](superpowers/plans/archive/2026-04-20-cluster-v2-debt-cleanup-SCOUTING.md) | Done | Closed 2026-04-25 across 5 phases (7 commits `6f737933..bb12fbbd`). `VaultConfig::mergeJson` unknown-key-preserving helper + Appearance/Daily Notes/Templates persistence wired through `MainWindow::applyVaultPortableSettings` + e2e CachedMetadataStore verification (wiring already in place — caught during audit) + `applyAutosaveDelay` applier on the `onSettingsApplied` dispatcher + `WorkspaceWindow` standalone QWidget facade deletion (class shrinks to identity token). Phase 5b/5c kcfg + SHARED-SYMBOLS sweeps found no orphans. 6 carry-forwards (3 unwired VaultConfig writers blocked on UI; vault-level cache fingerprint; 4 no-op settings keys; `WorkspaceWindow` identity-token review for Cluster Z; fold-gutter + LRU per user direction). Retro at [`cluster-retros/cluster-v2.md`](cluster-retros/cluster-v2.md). |
| W | Canvas & Graph affordances | [scouting](superpowers/plans/2026-04-20-cluster-w-canvas-graph-affordances-SCOUTING.md) | Scouting doc | Added 2026-04-20 from same audit. Split from V because it needs UX design: canvas Link/File node creation, tool palette, resize handles + snap, group ops, force-graph physics + pin UI, GraphViewTab signal wiring. 8 phases, ~8-12 days. Coordinates with Cluster M (graph/canvas audit) and Cluster P (Graffodil). |
| X | Block-substitution widget promotion | [scouting](superpowers/plans/2026-04-23-cluster-x-block-substitution-widgets-SCOUTING.md) | Scouting doc | Added 2026-04-23 after dogfood found inline-ORC canonical-coherence bug (Markoff Phase C work-unit C8 is the stop-gap). Lifts block display math (`$$…$$`) and mermaid fences out of Markoff's `QTextDocument`/ORC model into peer `QGraphicsItem`s modelled on `ImageBlockItem`. Inline math + inline checkbox stay ORC permanently (C8 translator handles them). Blocked on C8 landing + regression tests green. |
| Y | Workspace migration onto KDDockWidgets | [full](superpowers/plans/archive/2026-04-23-cluster-y-workspace-kddockwidgets.md) + [scouting](superpowers/plans/archive/2026-04-23-cluster-y-workspace-kddockwidgets-SCOUTING.md) | Done | Closed 2026-04-25 across 8 phases (43 commits `fd336369..bd1b50aa`, ~3 days dispatch-to-closeout). Workspace substrate is now `KDDockWidgets::QtWidgets::MainWindow` + `DockWidget`; `WorkspaceTabs`/`WorkspaceSplit`/`WorkspaceItem`/`WorkspaceParent` deleted. `.obsidian/workspace.json` byte-compat round-trip against 9 Obsidian fixtures via Corbomite-owned `WorkspaceSerializer` (LayoutSaver unused). Tab drag, drag-to-split, drag-to-floating-window all KDDW-driven. `Workspace::popoutLeaf` over `FloatingWindow` with geometry + maximize round-trip — closes G follow-up #6. `Workspace::openLinkText` dispatcher with injectable `LinkResolverFn` seam — closes G follow-up #3. `WorkspaceActiveLeafRouter` composes focus → activeLeafChanged with vault-switch suppression. `getLeaf(LeafMode, LeafDirection)` factory + 5 WorkspaceController additions (`getLeavesOfType` / `iterateAllLeaves` / `getActiveViewOfType` / `openLinkText` / `getLeaf`). `WorkspaceContainer`/`WorkspaceRoot`/`WorkspaceFloating`/`WorkspaceSidedock` ship as bookkeeping shells (left/right sidedock accessors stub `nullptr` until a future cluster migrates `CorbomiteMDI` sidebars). Approach B, opacity (ii), scope β. γ-scope events deferred to owner clusters (R / H#6 / Z + 3 backlog items). 5 carry-forward follow-ups: real `LinkResolverFn` wiring, `tst_e2e_gui::testCloseTab` close-flow assertions (KDDW signal not externally drivable), `WorkspaceWindow` standalone facade cleanup, plus 2 manual-QA-pending hypotheses (Wayland tab drag + split-right hamburger quirk). Retro at [`cluster-retros/cluster-y.md`](cluster-retros/cluster-y.md). |
| Z | Active-leaf tracking + linked views | — (brainstorm pending) | Plan-needed | Added 2026-04-23 from same dogfood. Three gaps: (1) `Workspace::activeLeafChanged` under-wired — GraphView/Canvas don't subscribe; (2) LocalGraph/Backlinks/Outlinks live in sidebars but belong in main-area linked leaves per Obsidian (architectural divergence from Cluster Q); (3) linked-leaf lifecycle (peer-close → linked-close) needs new `Workspace::openLinkedLeaf` API + `ViewRegistry` linkable-flag + `workspace.json` binding serialization. Constrained by Y sequencing. |

---

## In-flight work items

*(No clusters in flight. Cluster Y closed 2026-04-25 — see retro at `cluster-retros/cluster-y.md`. Next: Cluster Z brainstorm.)*

When work begins, each in-flight cluster gets a row here:

```
### Cluster X — <title>
- **Phase:** N of M
- **Last completed step:** <one line, with date>
- **Next expected step:** <one line>
- **Owner:** <human / agent name>
- **Date last touched:** YYYY-MM-DD
- **Open sub-questions:** <list, or "none">
```

Move the row to "Recent decisions" or a cluster retro on completion.

## Parallel long-term internal refactors

*Not on the Obsidian-parity roadmap. Shaping work that runs alongside cluster work. Two origin-classes: **internal** (Corbomite-authored, cadence set here) and **external-origin integration** (upstream repo drives phases; Corbomite absorbs on its own schedule).*

| Project | Origin | Plan | Status | Notes |
|---|---|---|---|---|
| Markoff integration | **external-origin**, **under Corbomite-agent stewardship** | Authoritative status: **[`phase-c-status.md`](../libs/markoff-family/docs/phase-c-status.md)** in the Markoff submodule | **Phase C closed** 2026-04-23 | All 7 C-phase work-units done (C1 → C5 → C6 → C3 → C7 → C2 → C4). C4 shipped 2026-04-23 at Markoff `v0.9.0` + Corbomite `8c6aa0ef..0d9171f7`. Submodule pinned at Markoff `master` HEAD (one commit past `v0.9.0` — `mermaidRenderer()` accessor for pointer-equality tests). See §Markoff Phase C + Recent decisions for the full C4 closeout. |
| Qutepart-Corbomite fork | internal | [spec](superpowers/specs/2026-04-15-qutepart-corbomite-fork-design.md) + [plan](superpowers/plans/2026-04-15-qutepart-corbomite-fork.md) | Phases 1 + 2 done; Phase 3 absorbed into Markoff Phase C (C7) | Phases 1 (vendor + CMake + smoke test) and 2 (`Corbomite::SourceEditor` shim + visual-line float scroll + cursor/fold API + NoteEditorWidget mount) both landed 2026-04-15. Phase 2 extended `Qutepart` public API with `scrollPositionVisualLine` accessors (logged in library's `PROVENANCE.md`). **Phase 3 (public find/replace API) now absorbed into Markoff Phase C work-unit C7** — the `Markoff::Source` widget owns that surface now. Phases 4–8 on Markoff side under `Markoff::Source` maintainership; no longer tracked here. |
| Graffodil adoption | internal | [scouting](superpowers/plans/2026-04-14-cluster-p-graffodil-adoption-SCOUTING.md) | Scouting | Listed as Cluster P in the roadmap above — internal refactor porting libs/forcegraph + libs/canvas onto Graffodil. |

## Markoff Phase C (external-origin integration under Corbomite-agent stewardship)

Phase C of the Markoff tri-view absorption. **Single source of truth:** `libs/markoff-family/docs/phase-c-status.md` in the Markoff submodule. The table below is a mirror summary for at-a-glance PROJECT-STATE visibility — update it when the authoritative status changes, but don't treat this as the master copy.

Ownership model: this agent holds commit authority on both repos. See Ritual 5 in `CONTRIBUTING-OPS.md` for the cross-repo session flow and the Markoff-side invariants to preserve (standalone build green, no `Corbomite`-named types in Markoff public API, tags append-only, `master` append-only).

| ID | Work-unit | Absorbs Corbomite-side | Markoff tag | Status |
|---|---|---|---|---|
| C1 | DI seam (retires `MARKOFF_READING_USE_REAL_COREDEPS`) | — | `v0.3.0` | **Done 2026-04-20** — Markoff v0.3.0 + Corbomite `59ecd5cb`/`751fe268`; adapter layer at `libs/core/include/corbomite/markoff_adapters/` |
| C5 | Reading-mode interaction parity | Cluster V Phase 4 | `v0.4.0` | **Done 2026-04-20** — Markoff `v0.4.0` + Corbomite `eef21e8e` |
| C6 | Editor state + context-menu contribution surface | Backlog §3 (editor-state + editor-menu) | `v0.5.0` | **Done 2026-04-20** — Markoff `v0.5.0` + Corbomite `a893c88d` |
| C3 | `MarkoffDocument` content-authoritative | Phase-A-deferred async/scroll/cursor plumbing | `v0.6.0` + `v0.6.1` | **Done 2026-04-21** — Markoff `v0.6.1` (soak closed) + Corbomite `81e3c82d`. Editor key-dispatch KateView refactor + D1 focus-restore + live-formatting span-refresh + default-theme init + `goToLineAndColumn` all landed across alpha.9-alpha.11 + v0.6.1. |
| C7 | Source feature completion (find/replace + fold-gutter) | Qutepart-fork Phase 3 + Cluster V.2 fold-gutter | `v0.7.0` | **Done 2026-04-21** — Markoff `v0.7.0` + Corbomite `efd8940e..9e4ae995`. SearchAdapter currentIndex param, MarkdownView 3 virtuals, SearchBar whole-word + regex toggles, SourceEditor mounts SearchBar + ReplaceController + 4 shortcut QActions, SourceFoldBridge over Qutepart. Cluster R hamburger items go live; MainWindow Find/Replace dispatch. |
| C2 | Theme / ResourceProvider / LinkResolver consolidation | — | `v0.8.0` | **Done 2026-04-22** — Markoff `v0.8.0` + Corbomite `2ba85428..a7535f00`. Markoff::Theme + ResourceProvider + LinkResolver promoted to markoff-core (Vault:: prefix dropped); 6 built-in JSON themes shipped at `:/markoff/themes/*.json`; importFromQOwnNotesIni; SourceEditor.setViewTheme bridges Qutepart palette + base font + current-line; Reading::CodeBlockHighlighter consumes Markoff::Theme. Corbomite-side: Corbomite::Core::ThemeService + SystemThemeBuilder (KColorScheme palette-derived + hand-tuned base layer); NoteEditorWidget subscribes to themeChanged; SettingsDialog "Editor theme:" combobox + "Import QOwnNotes scheme…" button. |
| C4 | Renderer unification (Code/Math/Mermaid) | — | `v0.9.0` | **Done 2026-04-23** — Markoff `v0.9.0` (`734d351..607e434`; accessor addendum `429e4b6`) + Corbomite `8c6aa0ef..0d9171f7`. Registry consolidation (Corbomite wrappers + adapter sections deleted); canonical `Markoff::MathRenderer` in markoff-core with shared process-wide LRU (Live + Reading math flow through one cache); mermaid-in-Live via `MermaidTextObject` substitution (cursor-in = source, cursor-out = SVG pixmap; empty-SVG fallback = source). Qt6::Svg added to markoff-live link deps. |

Ordering: `C1 → C5 → C6 → C3 → C7 → C2 → C4`. Rationale in the Markoff-side status doc.

---

## Recent decisions

Append-only. Most recent on top. Archive entries older than ~3 months to `docs/decisions-archive.md` (quarterly).

- **2026-04-25 — Cluster Y closed (Phase 8 verification + closeout).** Full ctest 284/289 (5 pre-existing flakes only — `tst_markoff_undo_grouping`, `tst_markoff_table_operations`, `tst_e2e_gui`, `tst_completion_popup`, `tst_benchmark_layout` — all in backlog §10 or Markoff submodule's known-flaky list). All 18 workspace-related tests pass including the 8-fixture serializer round-trip (`tst_workspace_serializer`). Plugin regression 15/15. Retro at `cluster-retros/cluster-y.md`. Plan + scouting moved to `superpowers/plans/archive/`. 5 carry-forward follow-ups logged in backlog: real `LinkResolverFn` wiring (P7.3 carry-forward — `MainWindow` should install a Vault+MetadataCache+create-if-missing lambda via `Workspace::setLinkResolver(...)` at startup, or absorb into Cluster Z); `tst_e2e_gui::testCloseTab` close-flow assertions skipped (KDDW signal not externally drivable from synthetic events — already tracked in §10); `WorkspaceWindow` standalone QWidget facade dead-code cleanup (`widget()`/`setWindowGeometry`/`showWindow`/`closeWindow`/`setMaximized`/`serialize()` survive but production reads from KDDW `FloatingWindow*` directly via `DockRegistry` — already tracked in §3); plus 2 manual-QA-pending hypotheses (P4b may close Wayland tab-drag-reorder regression incidentally since substrate is no longer `QTabBar`; P7 `getLeaf` factory may close split-right hamburger quirk). Manual QA itself **deferred** — user will walk Task 8.4 GUI checklist (drag interactions, vault-switch reparent, sidebar non-regression) when not over SSH. How to apply: when starting Cluster Z brainstorm, fold the real `LinkResolverFn` wiring into Z scope — Z is the natural caller because linked-leaves drive most `openLinkText` traffic.

- **2026-04-25 — Cluster Y Phase 7 closed; `Workspace::openLinkText` ships with an injectable `LinkResolverFn` seam rather than a `m_vault->resolveLink` direct call.** The cluster plan's pseudocode (`auto resolved = m_vault->resolveLink(path, source)`) would have inverted the dep direction (Cluster Q.0 made `libs/vault → libs/core` canonical; libs/core can reach `LinkResolver` via storage but not Vault/MetadataCache/FileManager which orchestrate resolution). Decision: keep core dep-clean, expose `Workspace::setLinkResolver(std::function<QString(path, source)>)` with identity default; Obsidian-shape API surface ships now, real Vault+MetadataCache+create-if-missing wiring is a follow-up task on MainWindow (or absorbed into the pending Cluster Z linked-leaf brainstorm). Same shape ripples to the `WorkspaceController` proxy — `openLinkText` + `getLeaf` take string `mode` ("split" / "tab" / "window" / "same") and parse to enum internally; unrecognised modes fall back to "tab" matching Obsidian's `getLeaf(true)` shorthand. WorkspaceContainer/Root/Floating/Sidedock all ship as bookkeeping shells over the existing KDDW substrate; left/right sidedock accessors stub `nullptr` until a future cluster migrates the CorbomiteMDI-resident sidebars into the Workspace tree. 19 new test cases across 3 files (`tst_workspace_factory` 9 cases, `tst_workspace_containers` 5 cases, `tst_proxy_workspace` 5 new cases). Full ctest 284/289 — 5 pre-existing flakes only (`tst_markoff_undo_grouping`, `tst_markoff_table_operations`, `tst_completion_popup`, `tst_e2e_gui`, `tst_benchmark_layout`). Existing `StubView` in `tst_proxy_workspace.cpp` returns a hardcoded "stub" viewType regardless of registry-key + ignores ephemeralState — added a typed `MarkdownStubView` for the new proxy tests so leaves whose live `view->getViewType()` actually reports "markdown" exist in the suite. Five commits (`076a8346..bd1b50aa`); next is Phase 8 (verification + retro + closeout). How to apply: when a future cluster needs to wire link resolution end-to-end, install a Vault+MetadataCache lambda via `Workspace::setLinkResolver(...)` from MainWindow startup, and audit `WorkspaceController::openLinkText` callers to confirm the resolver runs; don't push the resolution back into libs/core.

- **2026-04-25 — Cluster Y Phase 3 done end-to-end.** WorkspaceSerializer round-trip against synthetic KDDW trees, executed via `superpowers:executing-plans` directly on master across 10 commits (`19469965..05da6a04`). Shipped: `libs/core/src/WorkspaceSerializer.{h,cpp}` (private; consumed by tests via `target_include_directories(... PRIVATE libs/core/src)` — kept out of `include/` because Phase 4 will replace `walkKddwTreeSimple` with a real Workspace-driven walker). Round-trip works for 9 fixture shapes (single leaf, 2-child horizontal split, nested splits, stacked tabs, floating window, pinned+group, empty-default, unknown-keys, orphan-leaf recovery) plus a malformed-JSON test, 12 test cases total in `tests/core/tst_workspace_serializer.cpp`. Plan adaptations from the as-written spec: `corbomite_add_test(... DATA_DIR ...)` helper doesn't exist (used direct `add_executable` + `target_compile_definitions(... CORBOMITE_TEST_FIXTURE_DIR=...)` instead, matching `tests/core/CMakeLists.txt` convention); KDDW headers live under `kddockwidgets/qtwidgets/` not `kddockwidgets/` (KDDW 2.4 frontend split); `KDDockWidgets::initFrontend(FrontendType::QtWidgets)` required in `initTestCase()`; tests live in `tests/core/` not `libs/core/tests/`. Two architectural workarounds carry forward to Phase 4 (both signposted in code comments): (1) **Sidecar maps** for round-trip metadata that the KDDW layout itself doesn't carry — `stackedSidecar` (keyed by first-leaf-id of the tabs group) for the Obsidian "stacked tabs" flag; `leafSidecar` (keyed by leaf id) for pinned/group/viewType/icon/title/state/unknownKeys. Phase 4 retires both by reading from the live `WorkspaceLeaf` model. (2) **Floating-window construction quirk** — `setFloating(true)` is a no-op on a freshly-constructed never-attached DockWidget; we dock the first leaf into the host MainWindow as a transient hold, then `setFloating(true)` detaches it into a fresh FloatingWindow. Also requires the MainWindow already be `show()`-n; production callers (Workspace) satisfy this trivially, tests must call `mainWindow->show()` before `fromJson`. **Orphan-leaf recovery:** when a previous sibling in a split fails to materialize (empty tabs node), `anchorForNext` stays null and KDDW gracefully falls back to docking at the MainWindow root; we log via `corbomite.workspace.serializer` category. **Malformed-JSON path:** wrong-typed or empty `main` (and a parsed root-split with no children) installs a single default-empty-leaf dock widget rather than leaving the workspace blank. Full Corbomite ctest 283/288 — 5 pre-existing flakes only (`tst_markoff_undo_grouping`, `tst_markoff_table_operations`, `tst_completion_popup`, `tst_quadtree`, `tst_benchmark_layout`); confirmed pre-existing by stashing the WorkspaceSerializer changes and re-running. New backlog entry: split-right hamburger sometimes duplicates the open tab (Obsidian-correct) and sometimes opens a blank tab — hard to pin down, possibly vault-layout-corruption or a CommandRegistry dispatch race; expected to iron out under Phase 4+ when the real Workspace routes through KDDW. Next: Phase 4 (flip Workspace internals to KDDW substrate, ~3-4 days). How to apply: when extending the serializer, prefer adding to the parse/render pair + sidecar map over wiring KDDW-specific accessors directly; Phase 4 will collapse the sidecars into WorkspaceLeaf-resident state and the parse/render pair stays.

- **2026-04-23 — Markoff Phase C closed. C4 done end-to-end.** 17-task plan at `libs/markoff-family/docs/plans/2026-04-22-phase-c4-renderer-unification.md` executed via subagent-driven development, ~1 subagent per task, serial (no parallel dispatch — plan tasks have sequential dependencies). Markoff side (8 content commits `734d351..6defe6d` + 1 status commit `607e434`, tag `v0.9.0`, + 1 post-tag accessor `429e4b6`): `Markoff::MathRenderer` promoted to markoff-core with a single process-wide LRU keyed `(latex, displayMode, fontSize, dpr)`; jkqtmathtext moved to markoff-core PUBLIC dep so Live + Reading pick it up transitively; Live's in-library `MathRenderer.{h,cpp}` deleted and `MathTextObject` re-pointed; Reading's four JKQTMathText call sites (inline via `ReadingMathObject`, block via `ReadingView` registry lambda, DisplayMath via `SectionLayout`, fenced math/latex via registry) all routed through the shared renderer; `MarkoffParser::SourceSpan::codeBlockInfo` populated from tree-sitter's `language` leaf node inside `fenced_code_block` (extracted via same byte offsets already handled by `walkNode`'s leaf-node path — no new tree-sitter API); `Markoff::MermaidTextObject` (`QTextObjectInterface`, TypeId=UserObject+2, SourceProperty+RawProperty, per-instance LRU) renders mermaid source via `QSvgRenderer` → `QPixmap`, empty-SVG fallback paints nothing; Qt6::Svg newly linked; `Editor::setMermaidRenderer(MermaidRenderer*)` DI seam mirrors `ReadingView::setMermaidRenderer`, delegates to `SceneCoordinator::setMermaidRenderer` which fans out to every `MarkdownTextItem` via the same `m_items` iteration that C2's `setTheme` uses; `MarkdownTextItem::setMermaidRenderer` lazy-constructs the text object on first injection and registers it as the document-layout handler; `MarkdownTextItem::applyInlineSubstitutions` extended to detect fenced mermaid spans (walks back from `codeBlockInfo` span to capture leading backticks, finds matching fence-close, substitutes entire range including trailing `\n` with single `U+FFFC` carrying both `MermaidTextObject::RawProperty` and `MathTextObject::RawProperty` as shared round-trip key so existing `stripInlineSubstitutions`/`allMarkdown`/reveal-glyph lookup works unchanged); `updateReveal` extended for cursor-toggle of multi-line mermaid source via existing `QTextCursor::insertText(\n-preserving)` path. 16 new test slots across the three moves: `tst_math_renderer` (6 slots), `tst_sourcespan` (4 slots — mermaid/rust/empty-fence/non-fence), `tst_mermaid_substitution` (6 slots — TypeId distinctness / empty-SVG fallback / nullptr safe / DI propagation / multi-line collapse+round-trip / cursor-inside reveal). Corbomite side (5 commits `8c6aa0ef..0d9171f7`): submodule pin bump to `v0.9.0`; `HoverPopover` + `MainWindow` + `MarkoffAdapters` callers re-pointed at `Markoff::{CodeBlockProcessor,Embed}Registry` (mechanical rename — Task 1 audit confirmed no widening needed because `Handle`/`unregister` were test-only and `resources`/`depth` fields never consumed by any real caller); `Markoff::EmbedRegistry` directly constructed in `MainWindow` (no longer needs adapter); `NoteEditorWidget::setMermaidRenderer` + lazy-init path injects into both Live + Reading leaves from `MainWindow` setup block; `Corbomite::Core::{CodeBlockProcessorRegistry,EmbedRegistry}` + their adapter sections in `markoff_adapters/Adapters.h` + their test files deleted; `tst_mermaid_injection` (4 slots — Live immediate / Reading lazy / post-Reading injection / pointer equality) verifies leaves share the same renderer instance. Spec §12's 10 load-bearing decisions all held through execution (scope (b) not (c); registry consolidation C2-pattern; shared math in markoff-core; cache sizing punted; substitution-not-block-item; source-only error fallback; theme-gap untouched; no plugin registrars; per-instance mermaid cache; Markoff invariants held). Both Markoff invariants verified post-work (no Corbomite-named types in public Markoff headers; no `<corbomite/...>` includes inside Markoff). Standalone Markoff ctest 273/278, full Corbomite ctest 271/277 — only the documented pre-existing flakes in both. Side-catches during execution: `SectionLayout.cpp` had a fourth JKQTMathText call the plan didn't enumerate (surfaced by grep, migrated); `StubMermaidRenderer` in Task 8 initially missed `const` (base class's `renderSvg` is `const = 0` — controller fix); `Qt6::Svg` was genuinely absent from markoff-live deps (new use by `QSvgRenderer`); `tst_hover_popover_render.cpp` was missed by Tasks 13-14's call-site sweep (surfaced by Task 15 pre-deletion grep, rewritten). Carry-forward follow-ups logged in phase-c-status activity log: (a) `updateReveal` Case-2 collapse-on-leave currently re-substitutes only math — mermaid relies on next reparse's `refreshInlineSubstitutions()` (functional but not instantaneous; dedicated branch would mirror math path); (b) option (c) whole-block dispatch in Live (generic-language replacement via registry) = future post-Phase-C polish cluster; (c) theme-coverage gap in Live + Reading (separate backlog §3); (d) plugin-facing registrar methods (defer until consumer exists). Full closeout in `decisions-archive.md`. **Phase C is now closed** — no remaining C-phase work-units. Next: pick next cluster from backlog.

- **2026-04-22 — Markoff Phase C2 done end-to-end.** 23-task plan executed inline (no subagent dispatch — strictly serial work-unit). Markoff side (Tasks 1-14, `208ad23..89790f25`) tagged `v0.8.0`: `Markoff::Theme` promoted to `markoff-core` with new `ElementStyle` data model (optional fg/bg + bold/italic/underline/strike + optional fontFamilyOverride + percentage `fontSizeAdaptionPercent`); Element enum extended with Source-mode entries (Selection/Cursor/LineNumber*/Fold/BracketMatch/IndentGuide) + Reading entries (Embed/HtmlBlock/HtmlInline/FootnoteDef); `Markoff::ResourceProvider` + `Markoff::LinkResolver` promoted to markoff-core (Vault:: prefix dropped, broader C1 API shape); 6 built-in JSON themes shipped at `:/markoff/themes/*.json` (Light/Dark/Solarized*/Dracula/Monokai); `Markoff::Theme::importFromQOwnNotesIni` reads QOwnNotes scheme files via QSettings::IniFormat (importer mapping table covers `HighlighterState` integers -1, 0-30, 96-100, 1000-1006); `SourceEditor::setViewTheme` bridges Qutepart palette + base codeFont + current-line color (line-number gutter / fold marker / indent guide / bracket match deferred to qutepart-corbomite-fork Phase 6); `Reading::CodeBlockHighlighter` consumes `Markoff::Theme` (Reading::Theme enum retired); `Live::Editor` + ~20 painter sites read `m_theme.materializeFormat()` instead of `m_theme.formats[]`. Three new tests in markoff-core (roundtrip, default-load, qownnotes-import); two existing tests rewritten (tst_theme migrated to ElementStyle API, tst_resourceprovider rewritten for new resolveWikiLink/wikiLinkExists API). Corbomite side (Tasks 15-23, `2ba85428..a7535f00`): submodule pin bump to v0.8.0 + adapter parent class swap (`VaultResourceProvider` + `LinkResolverAdapter` retarget to non-Vault Markoff types, `src/editor/VaultResourceProvider` API method rename + `loadImageBytes` added); `Corbomite::Core::SystemThemeBuilder::buildFromKColorScheme` reads palette roles from `KColorScheme(View+Selection)` + hand-tuned base layer for code-syntax tokens + 27-entry callout accent table differentiated by background luminance; `Corbomite::Core::ThemeService` owns active theme + 6 built-in themes from Qt resources + user themes from `${XDG_CONFIG_HOME}/corbomite-dev/themes/`; MainWindow constructs ThemeService alongside KColorSchemeManager + restores persisted `markoffTheme` kcfg key on startup; per-active-editor wiring calls `editor->setThemeService(m_themeService)`; NoteEditorWidget subscribes to `themeChanged` + applies to every constructed leaf (lazy-constructed Source/Reading get setViewTheme on construction); SettingsDialog "Editor theme:" combobox + "Import QOwnNotes scheme…" button; `corbomite.kcfg` MarkoffTheme entry (default "Follow system"). Two new tests in `tests/app/` (`tst_system_theme_builder`, `tst_theme_service`). Spec §12's 8 load-bearing decisions all held through execution (one canonical surface per concept; full Source absorb; Markoff-native + manual importer; snapshot-only architecture; ElementStyle struct; comprehensive element coverage; KColorScheme-derived + hand-tuned base layer; six JSON built-ins). Both Markoff invariants verified (no Corbomite-named types in public Markoff headers; no `<corbomite/...>` includes). Standalone Markoff ctest 269/273 PASS; full Corbomite ctest 271/275 PASS — only the 4 documented pre-existing flakes (`tst_markoff_undo_grouping`, `tst_markoff_table_operations`, `tst_completion_popup`, `tst_benchmark_layout`). Zero C2 regressions. Manual UI smoke deferred (no display in execution environment); recommended pre-user-facing-closeout. Implementation surfaced two minor pitfalls worth recording: (1) `Q_INIT_RESOURCE` macro expands to extern decl + call; wrapping in an anonymous namespace causes the call to recurse on the wrapper itself (stack overflow on first call) — fix by forward-declaring `qInitResources_themes` at file scope (outside any namespace) and calling it directly from the namespaced factory functions; (2) `QSettings::IniFormat` treats comma-separated values as a `QStringList` — `value(...).toString()` returns empty on multi-entry lists; use `value(...).toStringList()` instead. Carry-forward follow-ups (logged in spec §11): KSyntaxHighlighting per-token CodeKeyword/String/Comment override (needs qutepart-corbomite-fork Phase 4); line-number gutter / fold marker / indent guide / bracket match Qutepart palette wiring (needs new public API in fork Phase 6); vendored `Qutepart::Theme` JSON loader deletion (touches multiple files); in-app FontColorWidget-equivalent theme editor; QOwnNotes importer round-trip test against all 35 default schemes (currently smoke against 2). Full closeout in `decisions-archive.md`. **Next Phase C work-unit: C4** (Renderer unification — collapse two-path code-block + math + mermaid rendering into single registry pattern; tag `v0.9.0`). Smallest C-phase work-unit; final polish before Phase C closeout.

- **2026-04-21 — Markoff Phase C7 done end-to-end.** 16-task plan executed cross-repo via subagent-driven development. Markoff `v0.7.0` tagged after Tasks 0-10 across 12 commits (`fd31cde` → `109bc79`): `markoff-core` extends `SearchAdapter::highlightMatches` with additive `int currentIndex = -1` + adds 3 `MarkdownView` virtuals (`showFindBar`/`showReplaceBar`/`hideFindBar`) + `SearchBar` whole-word + regex toggles + `flagsChanged` signal + `Q_DECLARE_METATYPE` for Flags. `markoff-source` mounts `SearchBar` in `SourceEditor`'s QVBoxLayout, constructs a single `ReplaceController` exposed as both `searchController()` and `replaceController()` (Task 6's TDD test surfaced a Task 4 architecture bug — two independent controllers had separate match state, so replace was a silent no-op despite matchCount reading correctly), wires 7 SearchBar signals through the engine, adds 4 shortcut QActions (Ctrl+F / Ctrl+H / F3 / Shift+F3), implements `SourceFoldBridge` filling the Phase-A `foldedHeadings`/`setFoldedHeadings` stubs via `MarkoffParser::HeadingInfo::sourceOffset` → block-number translation. F.2 needed the spec §6.2 fallback path (Qutepart's `setFoldedLines` only sets a flag on `TextBlockUserData`, which is created by the syntax highlighter not currently wired in Source — bridge lazy-creates user-data + calls `QTextBlock::setVisible(false)` on body ranges). `markoff-live`'s existing `Editor::showReplaceBar` becomes the explicit override; `showFindBar`/`hideFindBar` are trivial delegations. F.1 fold-gutter cleanup turned out to be 5 lines (the `setCoordinator` wire-up at `Editor.cpp:179` already shipped during C3; only the stale TODO comment remained). Standalone Markoff ctest 97/99 (only documented pre-existing flakes); a `libs/jkqtmathtext` symlink was used for standalone verification — needs a lasting fix (vendor / submodule / document the sibling-path requirement). Corbomite-side (Tasks 11-14, `efd8940e` → `9e4ae995`): submodule pin bump, Cluster R disabled placeholders at `MarkdownView.cpp:333-348` flipped to enabled + connected to `m_editorWidget->activeLeaf()->showFindBar()` (`activeLeaf()` promoted private→public), `MainWindow::triggerEditorAction` special-cases `Markoff::ActionId::FindNext`/`FindPrevious`/`Replace` and routes through `activeLeaf()` + Source's `findNextAction()`/`findPrevAction()` accessors, end-to-end `tst_markdownview_find_replace_dispatch.cpp` with 7 slots + probe-reversion verification. Corbomite ctest 266/270 (only the 4 documented pre-existing flakes). Spec §12's 8 recorded design decisions all held through execution. Carry-forward follow-ups (queued in `phase-c-status.md`): standalone-build sibling-jkqtmathtext gap; Reading-mode find-highlight visualization (Phase-A stub remains); `applyFoldedLines` interim path replaced by qutepart-corbomite-fork Phase 7 native heading-fold engine. Full closeout in `decisions-archive.md`. **Next Phase C work-unit: C2** (Theme/ResourceProvider/LinkResolver consolidation, tag `v0.8.0`).

- **2026-04-21 — Markoff Phase C3 soak closed at `v0.6.1`; Editor key-dispatch architectural fix + live-formatting restoration shipped; C7 unblocked.** Three-alpha cadence across one session: alpha.9 (Option D KateView-pattern key-dispatch refactor — `Editor::keyPressEvent` + `Editor::event` Tab interception deleted entirely, claimed keys routed through the existing `Editor::eventFilter` on `m_view`, `ensureFocusedCursorVisible` + `detectCompletionTriggers` relocated to `SceneCoordinator::textChanged` slot fixing a latent dead-code bug where they only fired on unaccepted-bubble-back keys), alpha.10 (D1 scene-focus-restore surgical fix via canonical-offset snapshot + defense-in-depth fallback with load-bearing `qCWarning`, alpha.8 re-entrance-guard debris removed, alpha.6 `rectForPosition` clamp retained with `TODO(D2)` gate), alpha.11 (three regressions unmasked by bandage removal: per-item dirty-tracked `SceneCoordinator::refreshHighlightingSpans` for typed-formatting refresh, `Theme::defaultLight` default-init for heading font-point-size, `Editor::goToLineAndColumn` + NoteEditorWidget wiring for cursor-column preservation on Source→Live). Four gated decisions recorded in the fix spec: scope (b: surgical + D1), mechanism (D: KateView alignment over minimal-A, disruptive-B/C, or hybrids E/F), staging (i: two-alpha + patch tag), D1 approach (β: diagnose + surgical + defensive fallback), bandage policy (X: keep alpha.6 clamp pending D2 follow-up). 12-slot regression suite at `libs/markoff-live/tests/tst_key_dispatch.cpp` — all green. Markoff ctest 57/59, Corbomite ctest 261/265 (only pre-existing flakes remain). Manual dogfood verified live heading sizing, inline bold/italic/strike styling, table-editing stability, cursor-column preservation. Five v0.6.1 carry-forward follow-ups queued in `phase-c-status.md` activity log (D2 CJK-autocorrect; D3 font warning on canonical setDocument; live-table promotion to QTextTable; inline-substitution splice-path span refresh; optional heading-bg removal). Full closeout in `decisions-archive.md`. C7 status: `requirements — see inputs`; spec next.

- **2026-04-21 — Markoff C3 landing review received; five asks actioned.** The Markoff dev team posted a pre-C7 review at `/home/clinton/dev/Markoff/2026-04-21-c3-landing-review.md` (~150 lines) raising five concerns about the C3 landing. Summary: "the design direction is right; the build is not yet earned." Concerns: (1) `m_sceneNeedsFullRebuildOnNextParse` is an escape hatch at the critical Live-leaf seam (fires on multi-paragraph paste / cross-heading delete / edit-touching-image — common operations, each costing focus loss + scroll jump + ephemeral-state reset); (2) two test files quarantined during C3 (MarkoffDocument + search/replace controllers) — though on inspection Task 8 had already rewritten them against the new API at `3682de0`, current state is three tests registered + passing, not debt; (3) anchor-math bug-fix landed 7 minutes after the interface (one bug caught, family of ~6 edge cases likely uncovered by the 11-slot `tst_canonical_buffer`); (4) velocity vs. soak: 2h37m spec-to-tag with zero dogfooding time, one alpha tag for 34 minutes; (5) 45 commits + 4 tags (`v0.4.0` / `v0.5.0` / `v0.6.0-alpha.1` / `v0.6.0`) ahead of canonical `~/dev/Markoff/` at `v0.3.0-alpha.2` — library state depends on which clone you read. User approved all asks + committed to personal dogfooding. Actioned: (a) tags + commits pushed to canonical via `git pull --ff-only` (canonical's `master` was checked out; direct push rejected, fast-forwarded from canonical's side instead); (b) 11 new anchor-semantics edge-case test slots added covering right/left bias at SOT and EOT, bias under replace-with-mismatched-removed/inserted-lengths, anchor at deleteEnd (non-straddle follow), anchor at deleteStart (unaffected), macro-grouped multi-delta edits, bias stability under undo→redo; (c) `QLoggingCategory "markoff.live.scene.rebuild"` instrumented on both the splice happy-path and every `m_sceneNeedsFullRebuildOnNextParse = true` site (enable at runtime via `QT_LOGGING_RULES="markoff.live.scene.rebuild=true"`) so dogfooding can measure fallback-fire rate; (d) `v0.6.0-alpha.2` re-tag added alongside `v0.6.0` (append-only; same commit; honesty-level annotation); (e) phase-c-status.md §C3 row annotated "premature — dogfooding pending per 2026-04-21 C3 landing review." Soak week starts now: user dogfoods `v0.6.0`; any fix commits tag `v0.6.0-alpha.3/.4/...`; genuine stability re-tags as `v0.6.1`. Decision: C7 does NOT start until the soak week closes — the 2h37m velocity was the problem, not the design. How to apply: for future multi-task plans with multi-repo reach, build an explicit dogfooding checkpoint between alpha.N and release tag; don't treat passing tests as the sole release gate.

- **2026-04-21 — Markoff Phase C3 done.** 25-task plan executed across both repos via subagent-driven development. Markoff `v0.6.0` tagged after Tasks 1-19 (core primitives, MarkoffDocument rewrite, three-leaf adaptation, tri-view interop, cross-mode undo tests). Corbomite-side adaptation (Tasks 20-23) shipped NoteDocument wrapper over MarkoffDocument, Vault ParsePool + raw-byte saveDocument + byte-equality echo suppression, external-reload Origin dispatch (clean → ExternalReloadClean; dirty → externalReloadConflict signal → resolveExternalReload with Resolved), and NoteEditorWidget flush/restore retirement (four pre-C3 call sites deleted; mode swap now pure signal-driven). Full Corbomite ctest 260/264 (4 pre-existing/environmental failures). Zero C3-introduced regressions. Spec §10's eight recorded design decisions all held through execution. Full closeout in `decisions-archive.md`. Next Phase C work-unit: C7 (Source find/replace + fold-gutter).

- **2026-04-20 — Markoff Phase C3 spec drafted.** 574-line spec at Markoff `2e7e7d1` (`docs/specs/2026-04-20-phase-c3-markoff-document-content-authoritative.md`). Design converged on **symmetric-B**: canonical = markdown bytes (`QString` behind `Markoff::CanonicalBuffer` interface); `MarkoffDocument` owns one `QUndoStack`; three leaves subscribe to `contentsChanged(offset, removed, inserted)` + `parseUpdated(Document*)` + `documentReloaded()`; every edit routes through `MarkdownDelta` commands; native Qt per-leaf undo disabled everywhere. Brainstorm considered and **rejected Option A** (single `QTextDocument` under all three leaves; Live rebuilt as one `QTextEdit`) after the Markoff agent correctly caught two framing errors in the initial pitch: (1) `QTextCursor` + `QTextFrame` cannot carve a `QTextDocument` into N independently-rendered widgets — A collapses to A(i) "retire Live's scene-graph" (full Live rewrite, no comparable Qt markdown editor ships folding + `QTextTable` + math + images + rich decorations in one `QTextDocument`) or A(ii) "custom `QAbstractTextDocumentLayout`" (Calligra-scale, multi-person-year); (2) B-as-dual-stacks was strawmanned — B-done-right disables `QTextDocument` native undo on every leaf and uses one `QUndoStack` on `MarkoffDocument`, plus the offset-translation work is paid by A *and* B, not "on top of A." Symmetric-B is also the shape that matches Source (Qutepart buffer) and Reading (AST) already subscribing at the markdown-bytes level, not at `QTextDocument`-object level. Eight additional design calls recorded (see spec §10): wrapper-not-pool (Vault's NoteDocument cache is the de-facto pool); shared single-worker `ParsePool` (Cluster I `MetadataWorker` precedent); no internal `QTextDocument` at all on `MarkoffDocument` (footgun by type-leak — deprecate-and-carry is the trap); `Origin` enum on `resetContent` (FirstOpen / ExternalReloadClean / ExternalReloadResolved / UserRevertToSaved / TestFixture); `documentReloaded` signal distinct from `contentsChanged` (wholesale-replace semantics are different from delta-apply); byte-equality defense-in-depth echo suppression (enabled by raw-byte save path); `HoverPopover` live-binding deferred to post-C3 Corbomite follow-up. **Phase-E hedge** (spec §11, scouting doc at `docs/superpowers/plans/2026-04-20-phase-e-crdt-canonical-SCOUTING.md`): `CanonicalBuffer` interface + `CursorPosition` opaque handle ship in C3 at small real cost (interface + one concrete + anchor bookkeeping; CursorPosition consumer is SearchController's current-match). Door stays open for a future `CrdtCanonicalBuffer` backed by `~/dev/collabtext/` without re-architecting canonical. Scope **excludes** Live scene-graph rewrite (separately-scoped future phase if we ever want it), renderer unification (C4), Theme/ResourceProvider/LinkResolver consolidation (C2), find/replace API + fold-gutter coordinator (C7), plugin-visible `MarkoffDocument` (plugin-ready-surfaces discipline from Cluster N applies). 14 acceptance criteria (7 Markoff-side, 7 Corbomite-side) + 7-item breaking-changes list for the removed `textDocument`/`setPlainText`/`plainText`/`replace`/`insert`/`remove`/`beginTransaction`/`endTransaction`/`parsed()` API surface + 12-file signatures-at-a-glance manifest. User pre-approved spec + plan; next step is plan file via `superpowers:writing-plans`, then staged implementation toward Markoff `v0.6.0`. How to apply: when a C3 implementation decision surfaces, cross-check against the 8 recorded decisions in spec §10 before relitigating; the `Origin` enum → stack-behavior mapping in §4.5 is load-bearing and must not drift.

- **2026-04-20 — Markoff Phase C6 shipped.** C6 landed at Markoff `v0.5.0` (6 content commits `19f41c7` → `9f500e1`, test-extension `cdce1a2`, phase-c-status `a9cbc9a`) + Corbomite adapter `a893c88d`. **Option C scope** (stub + defer unused fields) adopted over full classification, matching C5's "defer speculative coverage" pattern. Full `Markoff::EditorContext` struct shape stable; initial classifier populates: `blockKind` (Paragraph/Heading/Table/ListItem/Empty), `headingLevel`, `table` (row/col/rows/cols/isHeaderRow), `inBold`, `inItalic`, `inStrikethrough`, `inInlineCode`, `hasSelection`, `atBlockStart`, `atBlockEnd`, `readOnly`. ~15 fields (callout/blockquote/code-block sub-context, link/tag/footnote context, math/highlight inline, task-state/list-marker detail, column-alignment) stay default-zero until a consumer materializes — fills incrementally via future C-phases, struct shape doesn't churn. `Editor::contextChanged` signal debounced at 16ms via `QTimer::singleShot`, triggered by per-item `TextControl::cursorPositionChanged` / `selectionChanged` / `QTextDocument::contentsChanged` / `SceneCoordinator::reparsed` / `setReadOnly` — all coalesce into one emission per 16ms activity burst. `Editor::aboutToShowContextMenu` fires in both branches of `contextMenuEvent` (table + general) after built-ins before `menu.exec()`; after-built-ins shape keeps Markoff out of the section-ordering business. Inline-code detection stores a new `kInlineCodeProperty = QTextFormat::UserProperty + 100` on the highlighter's inline-code runs (bold/italic/strike use standard Qt flags: `fontWeight() >= Bold` / `fontItalic()` / `fontStrikeOut()`). Corbomite-side: `MainWindow::onEditorContextChanged` drives Format toolbar check-state + Heading radio + Table delete gating + toggle_fold; the 4 format actions were **not previously checkable** and are now — latent UX fix surfaced during adapter wiring. `MainWindow::onAboutToShowContextMenu` uses `MenuSectionHelper::addToSection` (plan assumed `addItem` + `flush` — actual API is `addToSection` + `finalize`; no `"action-primary"` bucket, grouped into `"action"`). `refreshEditorActions` retained as initial-state primer for non-context-driven actions. Two slots promoted from `private` to `public Q_SLOTS` for slot-level testability (`tst_mainwindow_action_wiring` extended with a synthetic-`EditorContext` test that asserts Format toolbar flips). Full Markoff ctest 56/56; Corbomite ctest green modulo documented pre-existing flakes (a stale-vault artifact briefly masqueraded as a C6 regression during execution — verified on parent commit, unrelated). How to apply: future `EditorContext` field fills should preserve struct shape stability; MenuSectionHelper API is `addToSection(QAction *, section)` + `finalize()`, not the shape sketched in the consumer-spec §9.5.



- **2026-04-20 — Markoff Phase C5 shipped + Cluster V closed.** C5 landed at Markoff `v0.4.0` (5 content commits `b8d24ec` → `b2514ac` + 2 doc commits `4b95f3d`/`04ff127`) and Corbomite adapter `eef21e8e`. Scope narrowed twice during execution via explicit user decisions: (1) `codeBlockProcessorRegistry` routing deferred to C3/C4 — post-C1 inspection revealed the registry's `bool`-returning `CodeBlockProcessor` contract can't produce mount payloads, and Markoff Live has zero `dispatch()` call sites (contradicting the original "same as Live" rationale); (2) regular-URL hover forwarding in Reading mode deferred — T3 implementer discovered `Markoff::Reading::LinkRenderer` is entirely orphaned (constructed nowhere in the reading pipeline), so wiring ReadingView to its signal would have no observable effect. The real fix is either a URL-span hit-test equivalent to `wikiLinkTargetAt` or LinkRenderer integration into the section pipeline — both belong under C3/C4, not a signal-unification work-unit. What shipped: unified `ReadingView::linkHovered(href, globalPos)` replacing `wikiLinkHovered` (breaking); `Markoff::Editor::linkHovered` widened to match; `TextControl::linkHovered` deliberately unchanged (internal leaf signal, not part of the tri-view contract); click-to-fold regression guard via real `QTest::mouseClick` dispatch (leveraging `kFoldArrowSectionIdxProperty` already exposed in `ReadingViewConstants.h`); and — new user-visible behavior — Corbomite's `NoteEditorWidget` now wires `ReadingView::linkHovered` → `HoverPopover`, so wiki-link hover in Reading mode shows the same preview Live mode has shown all along (Reading mode previously had no hover popover wired at all). Markoff standalone 64/64 green; Corbomite 243/245 modulo documented flakes + one pre-existing `tst_editorsuggest` bounds bug confirmed on the pre-C5 parent commit. Cluster V Phase 4 closed out in the adapter beat; retro at `cluster-retros/cluster-v.md`; Cluster V.2 remains open. How to apply: future cross-repo hover signal work should follow the two-arg `(href, globalPos)` shape set here; a URL-span hit-test mirror of `wikiLinkTargetAt` is the natural C3/C4 addition to unlock regular-URL Reading-mode hover.



- **2026-04-20 — Markoff Phase C1 (DI seam) closed.** Full closeout prose in `decisions-archive.md`. Retires the Phase B bridge: Markoff `v0.3.0` ships `Markoff::*` / `Markoff::Vault::*` abstracts + `Default*` no-op fallbacks + ReadingView setters for injection; Corbomite ships composition-based adapters (`libs/core/include/corbomite/markoff_adapters/` + `Corbomite::Core::MermaidRenderer` concrete). Standalone Markoff builds 76/76 ctest green with zero external deps; Corbomite builds green modulo the 2 documented pre-existing flakes. Next: C5 spec.

- **2026-04-20 — Markoff Phase C baton taken; scope expanded C1–C4 → C1–C7; Cluster V Phase 4 formally deferred.** Read the handoff at `libs/markoff-family/docs/handoff/2026-04-20-phase-c-ownership-handoff.md`; Phase C ownership now sits with this agent, working both sides of the submodule boundary. Scope expanded by folding in three Corbomite-prescribed work bundles the handoff didn't cover: **Cluster V Phase 4** (ReadingView `linkHovered`/click-to-fold/codeBlockProcessorRegistry/zoom virtuals) → **C5**; the **consumer editor-state surface spec** (443 + 203 lines prescribing `EditorContext` + `contextChanged` + `aboutToShowContextMenu`, recovered from Corbomite's stranded submodule-master in Markoff commits `8d92247`/`245bd56`, tagged `v0.2.8`) → **C6**; **Qutepart-fork Phase 3 (public find/replace API)** + **Cluster V.2 fold-gutter coordinator** (both now Markoff-side) → **C7**. New ordering: `C1 → C5 → C6 → C3 → C7 → C2 → C4`. Cluster V Phase 4 is paused as "blocked on Markoff Phase C C5"; its original scope ships as C5's Corbomite-side adaptation beat. Submodule pinned at Markoff `v0.2.9`. `CONTRIBUTING-OPS.md` gained **Ritual 5** (cross-repo session flow for working Corbomite + Markoff as one system). Recent-decisions bullet for the Phase B closeout (below) is now two bullets deep; Phase C session-start flow differs from normal clusters — `CONTRIBUTING-OPS.md` Ritual 5 is the authoritative how-to. Per backlog: struck the three items now absorbed by Markoff Phase C (consumer editor-state surface requirements → C6; editor right-click menu enrichment → C6; Obsidian-id CommandRegistry mirror now mostly Markoff-internal). Qutepart-fork row in §Parallel long-term internal refactors updated to mark Phase 3 as absorbed by C7.

- **2026-04-20 — Markoff Phase B absorbed (external-origin integration).** CorbomiteApp flipped onto the Markoff submodule's tri-view API: `Markoff::Live` / `Markoff::Reading` / `Markoff::Source` replace Corbomite's in-tree `Corbomite::QutepartSource` + `Corbomite::ReadingView` copies, which are deleted. Corbomite commit `da9a0a2c`; Markoff pin `v0.2.7`. Mid-migration the agent discovered three Cluster V Phase 2+3 Markoff-editor commits (`SetHeading1..6`, `cursorInTable`, widen `insertCallout`/`insertTable`, `currentHeadingLevel()`) that had been authored on Corbomite's local submodule master but never reached Markoff origin; cherry-picked onto Markoff master at `v0.2.1`. Spec's "Corbomite::Storage is dead" assumption disproved — `EmbedRenderer.cpp` includes `<corbomite/storage/CachedMetadata.h>` directly; Storage stays linked in real-deps mode (Markoff `v0.2.2`). Four Phase-A-skipped markoff-reading tests now re-enabled under `MARKOFF_READING_USE_REAL_COREDEPS` (`v0.2.3`). Trailing fixups: cherry-picked tests' stale `markoff` target name (`v0.2.5`), `Corbomite::Core` becomes PUBLIC on `markoff_reading` because the forwarding header `markoff/reading/VaultResourceProvider.h` needs it (`v0.2.6`), and a test-specific Storage link for `tst_readingview_embedrenderer` (`v0.2.7`). 241/243 tests pass (2 pre-existing flakes: parallel-run e2e segfault, benchmark timeout). Cluster V Phase 4 (ReadingView interactions) resumes next; any Markoff-side impact now lands through the submodule, not via direct Corbomite-side widget edits. **Phase C** (DI seam retiring the CMake-option bridge + `Theme`/`ResourceProvider`/`LinkResolver` consolidation) is Markoff-led, not yet plan-ready. New origin-class label introduced in §Parallel long-term internal refactors: **external-origin integration** (upstream repo owns the phase cadence; Corbomite absorbs on its own schedule).

- **2026-04-20 — Cluster V scoped via brainstorm; spec written; debt split to V.2.** Three-agent recon across editor/reading/workspace subsystems validated the scouting doc but caught several errors: fork Phase 6 does **not** gate the fold gutter (fork Phase 6 is about themes, unrelated), `NoteEditorWidget::cycleViewMode()` does not exist (API is `setViewMode(ViewMode)`), `Editor::cursorInTable()` does not exist (must be added), ReadingView emits `wikiLinkHovered` not `linkHovered` (adapter/rename needed), `DisplayProperty` on math **is already honored** (drop from scope), stacked Notices + tab context-menu residue already shipped in 2026-04-19 odd-jobs (shrinks Phase 5+6). KColorSchemeManager available system-wide (one-line `find_package(KF6ColorScheme)` add). User chose *surface-first* framing (option "a") with the constraint that nothing slips — so V covers the 6 user-visible phases (~5-7 days) and new Cluster V.2 scouting doc captures everything deferred (fold-gutter coordinator, 6× VaultConfig writer routing with merge-unknown helper, persistent metadata cache hookup, autosave delay spinbox, LRU-reopen upgrade, post-V dead-code audit). Architecture decisions baked into spec: new `View::zoomIn/Out/Reset` virtuals with default no-op (graph-view behavior); KColorSchemeManager single-dispatcher via `MainWindow::onSettingsApplied` connected to `CorbomiteSettings::configChanged`; `editor_toggle_mode` defined with `Ctrl+E` + View > Editor Mode submenu radio; new `Markoff::ActionId::SetHeading1..6` + `Ctrl+1..6` for direct-select H1-H6 menu; menu layout adds Edit/View/Format/Heading/Insert/Table with `activeView isa MarkdownView` gates and `cursorInTable()` gate on Table submenu; Phase 6 nominates 7 toast call sites, plan commits to 5 of 7. Next: `superpowers:writing-plans` for V, then execution.

- **2026-04-20 — Dead-code audit + two new scouting clusters (V, W).** Five parallel Explore agents audited editor stack, app shell, vault/storage, plugin system, and canvas/graph for built-but-unreachable features. Pattern confirmed (recent `Corbomite::EmptyView` was representative): roughly 50 features have no UI surface. Findings split: Tier 1+2 (dead app-shell actions + unsurfaced editor actions + Workspace power-features + persistent cache loader + Notice toasts + search UI completion) → **Cluster V** scouting doc. Tier 4 (canvas node creation + tool palette + resize/snap + group ops + force-graph physics + signal wiring + mermaid double-dispatch) → **Cluster W** scouting doc. Tier 3 / Tier 5 (Bases discoverability, plugin-API dead methods like `VaultProxy::modifyBinary` / `SearchProxy` link/tag queries / `secrets()/process()/network()`) deferred to whenever real third-party plugin development materialises. Both new scouting docs at `superpowers/plans/2026-04-20-cluster-{v,w}-*-SCOUTING.md`. INDEX updated. Backlog updated.

- **2026-04-19 — Odd-jobs sweep: 5 small follow-ups shipped.** `Corbomite::EmptyView` in `libs/core/` registered as viewType `"empty"` (Create/Go-to/Close buttons) + `WorkspaceLeaf::setViewState` falls back to it on missing factory — closes Cluster G follow-ups #1 (empty-state) and #2 (unknown-viewType fallback). `View::onTabMenu` default impl + `WorkspaceTabs::requestClose{Tab,Others,ToRight,All}` helpers + `QTabBar::customContextMenu` wiring — closes Cluster G #7. `SearchDSL::CompiledPlan` gained `regexPatterns` + `caseSensitiveTerms`; `SQLiteIndex::searchCompiled` overload post-filters via `QRegularExpression` / `Qt::CaseSensitive` over `notes_fts.content` — closes Cluster D #3 (regex) and #4 (match-case). `ui.views` permission scope clarified in `API-REFERENCE.md` §Permissions (narrow: `ViewRegistrar` only; sidebar `createView()` doesn't need it) — closes the Cluster N open spec question.

- **2026-04-19 — Backlog sweep: six small items struck as already closed by recent commits.** `56d0db85` (stacked Notices), `1d4b3a9a` (CorbomiteConfigVersion.cmake + lastOpenFiles sibling key), `5f858059` (ViewRegistry error-path hardening), `96c59821` (search-result rich snippet rendering), `261fb3cd` (Bases column-reorder persist). `backlog.md` updated inline. No code changes.

- **2026-04-19 — Cluster R + S specs written; 10 audit addenda fill view-header-menu gaps.** Brainstorming session scoped the per-view hamburger menu (the "…" overflow in the ItemView header) after the user noted the markdown hamburger only shows a single unfunctional "Rename…" entry. Audit survey of the 20 Obsidian menu items found 15 had some form of prior documentation (in `views.md`/`workspace.md`/`ui-bundle.md` or in Cluster G/H/Qutepart plans) but were never collected into a menu-wiring cluster, and 10 had zero coverage. Four audit addenda newly document previously-missing core-plugin / feature surfaces (Bookmarks, file-recovery UI, Canvas Export-as-image, Graph Copy-screenshot); six addenda document the modals and platform primitives (`promptForFileRename` / `promptForMove` / `promptForDeletion` / `openWithDefaultApp` / `showInFolder` / merge-file-modal) and the add-file-property menu entry. Design decision: split the work into **Cluster R** (view-header menus, 4-phase ~6-7 days: P1 menu substrate alignment including `View::onMoreOptionsMenu` hook + `MenuSectionHelper` canonical-order rename `title→close`, `action-primary→pane`, add `find`+`view.linked`, new `addSubmenu`; P2 `EditableFileView` universal file-menu items including real `promptForFileRename` modal replacing the stub + 9 other entries; P3 per-view specialisations on `MarkdownView`/`CanvasFileView`/`GraphView`; P4 inline backlinks-in-document via Cluster J's PostProcessorRegistry) and **Cluster S** (Bookmarks core plugin, single-phase ~5 days: `.obsidian/bookmarks.json` round-trip with unknown-key preservation, right-dock `BookmarksView` on `BookmarksModel`, 7 `bookmarks:*` commands, "Bookmark…" modal with group picker). Four items ship in R as **disabled placeholders** whose tooltips cite the blocking cluster: "Open in new window" (G#6), "Find…"/"Replace…" (Qutepart fork P3), "Open version history" (deferred Cluster T — file-recovery plugin), "Bookmark…" (Cluster S — goes live when S ships). "Open linked view" submenu in R P3 uses **interim dispatch** to each plugin's `:open` command (focuses dock panel) until Cluster G follow-up #3 (`Workspace::openLinkText`) lands; then submenu upgrades to real leaf-opening without menu changes. "Merge entire file with…" explicitly dropped as deferred (low-value). `MenuSectionHelper` has zero production callers today (Cluster H shipped it but FileExplorerPanel's reference got absorbed into the plugin migration), so the section-key renames are cost-free. 11 doc edits: 10 new addenda + 2 new specs + `00-taxonomy.md` addenda index + `PROJECT-STATE.md` roadmap table (added R/S/T rows; heading updated 16→19 clusters) + Cluster G follow-ups #3/#6 annotated R-blocking-partial + Cluster H follow-up #2 annotated "partially absorbed by R" + this Recent-decisions entry + `docs/superpowers/plans/INDEX.md` (to be updated). **Next:** `superpowers:writing-plans` for R, then execution via `superpowers:executing-plans` or parallel dispatch. S follows after R P1 lands. How to apply: any future view-header-menu additions go through `onMoreOptionsMenu(MenuSectionHelper &helper)`; the full section-assignment rulebook is in §4.2 of the R spec. Audit reference discipline: if implementation surfaces a new Obsidian behaviour, add an addendum at `obsidian-audit/addenda/YYYY-MM-DD-<topic>.md` per `addenda/README.md` — never edit the original domain docs.

- *(Entries for 2026-04-15 through 2026-04-17 rolled off to `docs/decisions-archive.md` on 2026-04-19. See that file for Q.0, Q, K, N, G-Part1, J, and E phase closeouts.)*

---

## Open questions blocking progress

Questions that need human input before the linked work can proceed.

- **Should Cluster Q be expanded to a full plan via brainstorm now?** User directed next focus (2026-04-16) to internal-plugin wrapping: an `InternalPlugin` `Component` wrapper around built-in features (FileExplorer / Search / Backlinks / Outlinks / Outline / Properties / LocalGraph), `core-plugins.json` persistence, Settings "Core plugins" toggle page, and an explicit plugin-permissions system ("this plugin wants a, b, c — allow?") that gates a broader-than-community-Plugin surface. Design input captured at `memory/project_cluster_q_permissions.md`. Next session step: dispatch `superpowers:brainstorming` against that input + Cluster G done state. New cluster slot allocated as Q (existing M/N/O/P retained as-is to avoid semantic collision with the pre-existing "feature audits" / "plugin-ready surfaces" / "query layer" / "Graffodil adoption" scopes).

*(No open questions. The Vault/VaultModel tension was resolved by Cluster Q.0 — canonical `Corbomite::Vault` + `FileManager` in `libs/vault/` replace VaultModel entirely; `VaultProxy` + `FileManagerProxy` facade them with permission gating. See `docs/cluster-retros/cluster-q0.md`.)*

When questions arise, format:
```
### <one-line question>
- **Blocks:** <cluster ID + phase, or "general planning">
- **Context:** <one paragraph — why we need this answer>
- **Asked:** YYYY-MM-DD by <session/agent>
- **Resolved:** YYYY-MM-DD with answer "<one-line answer>" (then move to Recent decisions)
```

---

## Pointers

- **Audit reference (canonical):** `docs/obsidian-audit/` — taxonomy + 15 domain docs + 5 synthesis docs + 2 running lists. Treat as read-only except for *additions* via `addenda/`.
- **Cluster plans:** `docs/superpowers/plans/` — one file per cluster, named `2026-MM-DD-cluster-<letter>-<title>.md`. Index at `docs/superpowers/plans/INDEX.md`.
- **Rituals (how to update this file and others):** `docs/CONTRIBUTING-OPS.md`.
- **Cluster retrospectives:** `docs/cluster-retros/cluster-<letter>.md` — written when a cluster lands fully.
- **Unified backlog (map):** `docs/backlog.md` — every deferred follow-up, not-started cluster, and known-flaky test, grouped by theme. Read before picking up new work.
- **Decisions archive (journal):** `docs/decisions-archive.md` — append-only closeout summaries + rolled-off decisions. Consult for *why* a prior call was made.
