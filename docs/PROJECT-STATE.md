# Project state

> Slim session-start orientation. **Reset 2026-04-26** after a full audit (`docs/audit-2026-04-26/`) regrouped everything into a fresh A-onwards cluster scheme + a flat punch list. Pre-reset state archived at `docs/archive-2026-04-26/PROJECT-STATE-pre-reset.md`.

> **2026-05-25 — Foundation port landed on `master`.** Markoff's QML/D2 rebuild merged to Markoff `master` (tag `v0.7.0-freeze`); Corbomite re-pinned there and merged `port/foundation-exploration` → `master`. The old four-leaf QGraphicsView editor is retired; the QML/D2 live editor is canonical. The rewrite **obsoleted clusters G, H, J** and **re-scopes E**. **Reading mode resolved 2026-05-29: read-only `Markoff::Styled::Editor` leaf** (`775fa54e`; the earlier read-only-Live steer is retired). Port doc (historical): [`docs/port-foundation-exploration.md`](port-foundation-exploration.md). Handoffs: [`docs/handoff/`](handoff/).
>
> **2026-06-10 — Docs reset + full audit.** Code/docs drift audit (9 verification agents): punch list re-triaged (+21 verified items, ④ bucket unblocked), 40 executed/obsolete plans+specs archived, obsidian-audit corrections in `obsidian-audit/addenda/` (4 new), live parity tracking moved to [`docs/PARITY-MATRIX.md`](PARITY-MATRIX.md). Roadmap to a dogfoodable release: [`docs/superpowers/plans/2026-06-10-road-to-dogfood.md`](superpowers/plans/2026-06-10-road-to-dogfood.md).

## Two tracks

Work flows through **two parallel tracks**. Both must be checked at session start.

1. **Punch list** — flat severity-ranked list of small fixes. File: [`docs/punch-list.md`](punch-list.md). Pick from top (P0 first). 58 items at reset.
2. **Strategic clusters** — multi-phase coordinated initiatives. Index: [`docs/superpowers/plans/INDEX.md`](superpowers/plans/INDEX.md). 10 active at reset (A–J).

Most P0/P1 punch-list items are **silent vault-format corruption risks**. Drain them before strategic-cluster work unless explicitly redirected.

The P0/P1 bands were re-verified 2026-06-10 and drained in Phase 0 (branch `feature/phase0-data-safety`): non-atomic `saveDocument`, `modify()` reconciliation, link-clicks-dead, LinkResolver staleness, BOM strip, and DB relocation — all fixed. Remaining open items start at P1 (`onExternalRenamed` descendants) and P2; see punch-list.

## Active strategic clusters (snapshot)

| Cluster | Title | Status | Source |
|---|---|---|---|
| P | STATIC → SHARED internal library refactor | **CLOSED 2026-08-21 — all phases P0-P6 done.** Phase P6 (cross-repo: markoff-family + graffodil, dispatched on explicit user go-ahead) flipped 13 more libraries SHARED (`build-dev/` 3.6 GB → 1.8 GB, **-84% end-to-end** from the 11 GB baseline) and caught a real regression: re-pinning Markoff straight to `master` HEAD silently dropped Cluster N's clipboard API (it lived on an unmerged `feature/rich-clipboard` branch) — Corbomite's own incremental rebuilds missed it, the Arch PKGBUILD's fresh clone caught it. Fixed by merging the branch upstream and re-verifying with genuinely clean rebuilds throughout (the lesson: submodule re-pins need `rm -rf` clean builds, not incremental ones). [Plan](superpowers/plans/2026-08-20-cluster-p-shared-libraries.md) (§1 corrections are normative **over** the audit; §2 doctrine D1-D7); audit: [`audit-2026-08-20-shared-libraries-refactor.md`](../audit-2026-08-20-shared-libraries-refactor.md). Full detail: `decisions-archive.md`, 2026-08-21. | New 2026-08-20, user-directed, CLOSED 2026-08-21 |
| D | Bases UI completion | **Only D.5 (plugin API) remains** — needs fresh spec. D.1–D.4c + formula editor + filter builder shipped 05-25..05-28; D.6 (base creation UX) and the filter builder's leaf-level property/operator/value picker both discovered *and* shipped 2026-08-20, user live-verified | Audit |
| E | Markoff Editor API parity | **Re-scope vs D2 block model** — stub body targets the retired editor (see banner in plan file) | Audit |
| F | Internal-plugin gap fill | Plan-needed (stub) — substrate-independent, dispatchable after brainstorm | Audit |
| I | Editor & Workspace UI surfacing | **CLOSED 2026-08-20 by absorption.** Phases 1–4 shipped 2026-04-20 as legacy Cluster V; Phase 5 mostly shipped in Cluster L4, its tail folded into Cluster O (O5.T5); Phase 6 punch-listed as two `[ex-cluster-i]` P3 items. [Retro](cluster-retros/cluster-i-ui-surfacing.md) | Carried (was Cluster V) |
| L | Workspace/KDDW stabilization & nativization | **L0-L4 landed + closed. L5 PAUSED (not closed) as of 2026-08-18** — soak dogfood found + fixed a P0 nested-split-scrambling bug in the Obsidian round-trip path (direction-mapping inversion + anchor-threading order); see punch-list `[cluster-l]`. Soak was interrupted same-day by the v0.1.0 release push (below) and has not resumed. **Next: resume L5 soak** (drag/split/popout/close-undo/vault-switch), then closeout | New 2026-08-17 |
| M | Canvas authoring parity (Graffodil rebase first) | **M0-M3 CLOSED. M4 (move/snap/selection polish, M4.1-M4.4) code-complete + offscreen-green (2026-08-20), live-tested — found+fixed a real snap-drag-never-releases bug (`917bc1b3`); M4.4 chrome overlay's visual feel still unverified.** | New 2026-08-19 |
| O | Context-sensitive menu bar, toolbar & sidebar | **O0-O3 CLOSED 2026-08-20. O4 (`CanvasViewActions`) code-complete + offscreen-green 2026-08-21 (331/331), NOT yet live-eyeballed.** Canvas menu + `canvasToolBar`: snap-to-grid/snap-to-objects/show-grid (kcfg-backed, fans out to every open canvas), zoom in/out/reset/fit/to-selection. Next: live-verify O4, then Phase O5 (`BasesViewActions`/`GraphViewActions`). | Report: [`audit-2026-08-20-context-sensitive-ui.md`](audit-2026-08-20-context-sensitive-ui.md). New 2026-08-20, user-directed. |
| N | Rich clipboard (copy-as + smart paste) | **CLOSED and MERGED to `master` 2026-08-20.** N5 eyeball passed on the worktree; pre-merge cleanup pass then found + fixed the stalled paste-fix (canvas Smart paste was still calling `applyFlatEdit`, never the finished `Cmd::pasteMarkdown`) plus a BlockQuote marker asymmetry bug in both canvas and Styled. Clipboard actions ported from the pre-O3 flat `MainWindow` mechanism onto `MarkdownViewActions` (O3.T6 moved that mechanism while N was on its own branch). Image paste/drop out of scope. | New 2026-08-20 |
| K | Markoff canvas leaf adoption | **CLOSED 2026-08-18 (Phase 5)** — Canvas is the sole LivePreview engine; QML `markoff_live` unlinked. Callouts remain frozen on Markoff E3 | New 2026-08-15 |

Closed (post-reset): A (2026-04-27), B (2026-04-28), C (2026-04-27), **K (2026-08-18)**, **N (2026-08-20, merged to `master`)**, **P (2026-08-21)**.

**Obsoleted by the foundation rewrite (2026-05-25):** G (inline-ORC coherence — no more QTextDocument/ORC substrate), H (block-substitution widgets — peer-delegate model is the new baseline; math/mermaid → Markoff E5), J (qutepart Source fork — likely superseded by `Markoff::Source::Editor`; confirm before formal closeout). See INDEX + decisions-archive.

Full table + plan-file links: [`docs/superpowers/plans/INDEX.md`](superpowers/plans/INDEX.md).

## Recent decisions

- **2026-08-21 — Both worktrees from the punch-list sweep below finished and landed on `master`.** Search regex/match-case toggles (`6461bf05`) and 5 swallowed-error → `Notice::post` sites (`c6267e2b`); both worktrees + branches removed after landing; punch-list marked `[x]`. Full detail in `decisions-archive.md`.
- **2026-08-21 — Post-Cluster-P punch-list sweep, dispatched via parallel subagents; two left mid-flight in worktrees when the session hit its usage limit.** After Cluster P closed (see below), worked top-down through `punch-list.md` P1-P3. Landed directly on `master`: (1) **P1 fix** — `Vault::onExternalRenamed` now walks folder descendants on external rename (mirrors the programmatic `Vault::rename` path), and along the way found + fixed a real bug in `Watcher::drainPending`'s rename-pairing: it matched delete/create purely by mtime, so renaming a folder with 2+ files sharing a millisecond mtime (common — folder rename doesn't touch file mtimes) could cross-wire which old path paired with which new path, silently corrupting the `renamed(old,new)` signal's identity guarantee. Fixed by preferring a same-mtime+same-basename match first. Commit `6deb429b`. (2) **Re-triaged `tst_e2e_gui::testSaveShortcut`** (filed 2026-08-17 as a save/dirty-flag race) — does not reproduce under normal execution (17/17 clean x3); the original repro only ran the one QTest slot in isolation via CLI filter, skipping the earlier slots it's implicitly order-dependent on. Real, separate finding: that test points directly at the tracked `testvaults/starter-vault/PKM LM/` fixture and saves into it for real, which is likely a contributor to the vault's already-known-and-tracked P0 blank-line-collapse dirty-file drift (see punch-list) — fix identified (copy to `QTemporaryDir` in `initTestCase`) but not yet implemented. Commit `d7701532` (docs only). **Three parallel `Agent(isolation: worktree)` subagents were dispatched for independent P2/P3 items** (app.json settings wiring incl. a real trashOption dialog/actual-delete mismatch; search panel regex/case-sensitive toggles; swallowed-error sites → `Notice::post` toasts) — all three hit the session's usage limit mid-work (resets 9am America/Toronto) before finishing. State: the app.json-settings agent made no file changes (still researching; its worktree was auto-cleaned, nothing to resume — redispatch fresh). The other two left **uncommitted, unfinished, unbuilt** work in on-disk worktrees: `.claude/worktrees/agent-aa703e8335a3da873` (branch `worktree-agent-aa703e8335a3da873`, based on `6f0954ec`) has a modified `src/plugins/search/SearchView.h` + new `src/plugins/search/tests/tst_search_options_regex.cpp` but no `.cpp` implementation yet and no `CMakeLists.txt` wiring for the new test — will not build as-is. `.claude/worktrees/agent-af6107c8616243b1d` (branch `worktree-agent-af6107c8616243b1d`, based on `6f0954ec`) has `libs/vault/{include/corbomite/vault/Vault.h,src/Vault.cpp}` (a new save-failure signal, in progress), `src/dialogs/Notice.{h,cpp}` (a `post()` factory), and `src/app/MainWindow.cpp` (wiring, was mid-edit when cut off — its own last message says it was about to add "the `documentSaveFailed` connection near the other `m_vaultObj` connects"). **To resume either**: `cd` into the worktree path, read the diff (`git diff`), finish per the original brief (re-read this session's transcript or just: build+test to find what's missing, since both were following this project's TDD/verify-first discipline), then merge/cherry-pick onto `master` and delete the worktree branch. Neither has been reviewed for correctness — treat as an unverified draft, not a trusted patch.
- **2026-08-21 — Cluster P CLOSED: Phase P6 (cross-repo) flipped markoff-family + graffodil SHARED, caught a real cross-repo regression the same way P4/P5 caught real bugs.** All 7 remaining markoff-family libraries (`markoff_core/canvas/source/styled`, `markoff-parser`, `ts-markdown-parser`, `collabtext`) and all 6 Graffodil libraries flipped STATIC→SHARED on explicit user go-ahead; `build-dev/` dropped 3.6 GB → 1.8 GB (**-84% end-to-end** from the 11 GB baseline). The real find: re-pinning Markoff straight to its `master` HEAD (rather than the `feature/rich-clipboard` commit Corbomite's old pin actually sat on) silently dropped Cluster N's clipboard API — `feature/rich-clipboard` was never merged to Markoff's `master` even after Corbomite's own Cluster N closed. Corbomite's incremental dev-preset rebuilds missed it twice (327/327, both false positives — a stale `.o` for the one `.cpp` referencing the dropped API never got recompiled); the Arch PKGBUILD's fresh `git+file://` clone caught it on the first try, exactly the packaging-verification value this cluster exists to prove. Fixed by merging the branch upstream (clean, no conflicts) and re-verifying everything with genuinely clean rebuilds (`rm -rf`, not `--fresh`) — Markoff 213/213, Corbomite 327/328 (one confirmed `-j10` flake, `tst_gc`, green standalone), AppImage + Arch package both build/stage/launch/open-a-real-vault clean. Installed-release (`sudo cmake --install`) deferred — no interactive sudo available, user opted to skip rather than block. One cosmetic packaging finding punch-listed (`$srcdir` string leakage in two SHARED libs, non-blocking). Detail: [`decisions-archive.md`](decisions-archive.md). Cluster P is now fully closed, P0 through P6.
- **2026-08-21 — Cluster P Phase P5 closed: packaging verification found and fixed a real plugin-RPATH bug.** AppImage/installed-release/Arch `PKGBUILD` all verified against the full post-P3 SHARED-library set. `corbomite_add_plugin()` had never set `INSTALL_RPATH`, so every plugin silently relied on ldconfig's default `/usr/lib` search rather than a correct RPATH — the same accidental-correctness shape Cluster P exists to eliminate; fixed via `file(RELATIVE_PATH)` in `cmake/CorbomitePlugin.cmake`, verified with `ldd` against a real `/usr/local` install. Also fixed two pre-existing `packaging/arch/PKGBUILD` bugs (wrong `kf6-*` dependency names, invalid `#HEAD` git-source fragment) found getting the Arch package to build at all. Commit `013b14d8`. Detail: [`decisions-archive.md`](decisions-archive.md). **Next: Phase P6** (cross-repo — markoff-family + graffodil) needs explicit user go-ahead per the plan's §3 before starting.
- **2026-08-20 — Cluster D: D.6 shipped + live-verified (base creation UX), and the filter builder's leaf editor upgraded to a point-and-click picker, also shipped + live-verified.** Both discovered and closed same day. **D.6:** `file_new_base` KAction (icon `x-office-spreadsheet`, File menu), `MainWindow::createNewBase()`, `FileExplorerView::onNewBaseIn()` + "New Base"/"New Base Here" context-menu entries — silent create-then-rename, mirroring `createNewCanvas()` exactly; no seed content needed since an empty `.base` is already a valid default Table view. Full offscreen suite green throughout (327→328), user live-tested against a real vault and confirmed creation works. **Filter picker:** while helping the user understand why their first Base showed a wall of empty rows (a Base queries every note in the vault by default — no filter means no narrowing), it emerged Corbomite's 2026-05-28 filter builder only ever shipped raw-formula-text leaves (`!note.Practical.isEmpty()` typed by hand) — Obsidian's real Filter menu is Property/Operator/Value dropdowns with an advanced-editor escape hatch (confirmed against the mounted `testvaults/obsidian-help/Bases/Views.md`). New `Corbomite::Bases::FilterRuleRow` (`libs/bases/{include/corbomite/bases/,src/}FilterRuleRow.{h,cpp}`) provides that: type-aware operator lists (type inferred by sampling actual row data — no central type registry exists), a raw-text advanced-mode fallback, and a reverse-parser that recovers simple-mode fields from an on-disk expression when possible and falls back losslessly to advanced mode otherwise. 11 new tests (`tst_filter_rule_row`); 328/328 offscreen (excl. by-design benchmark timeout); user live-verified against their own vault ("works great"). Full detail + resolved open questions (icon, create-flow, and the filter-picker design) in [the Cluster D plan file](superpowers/plans/2026-04-26-cluster-d-bases-ui-completion.md). Only D.5 (plugin API) remains in Cluster D.
- **2026-08-20 — Cluster D: D.6 discovered — no UX exists to create a `.base` file.** User-reported: a vault with existing `.base` files opens and edits them fine (D.1–D.4c shipped that), but nothing in Corbomite can *make* a new one — verified there is no `file_new_base` KAction, no `MainWindow::createNewBase()`, and no File Explorer "New Base"/"New Base Here" context-menu entry, versus the shipped `file_new_canvas`/`createNewCanvas()`/"New Canvas" equivalents. Confirmed wiring-only, not a missing-feature gap: `FileManager::createNewFile` is already extension-generic (canvas already calls it with `extension="canvas"`; `extension="base"` needs no library change), `BasesQuery::fromString` already treats an empty string as a valid default one-view Table query (audit's `[CRIT]` invariant, `BasesQuery.cpp:185-196` — so unlike canvas, no seed content is even needed), and `.base`→`BasesView` is already registered for opening (`MainWindow.cpp:1582`). Missing pieces: one KAction, one `MainWindow` method, one `FileExplorerView` method, two context-menu entries, one `.rc` edit — full list with an Obsidian parity reference (`New note → New canvas → New base → New folder` ordering, per `obsidian-audit/addenda/2026-04-19-file-explorer-context-menu.md`) written up as a new stub phase **D.6** in [the Cluster D plan file](superpowers/plans/2026-04-26-cluster-d-bases-ui-completion.md#d6--base-creation-ux-discovered-2026-08-20). Distinct from Cluster O's Phase O5 (which promotes an *already-open* `BasesView`'s 7 in-view toolbar buttons to KActions) and from `BasesQuery.newItemFolder`'s `+ New` button (which creates *notes* matching a base's filter, not new `.base` files) — neither of those cover file creation. Not yet dispatched; needs the two open questions in D.6 (icon choice, prompt-vs-silent-create) settled first.
- **2026-08-20 — Cluster P Phase P4 closed: plugin-boundary correctness gate — proven, not just asserted.** `tst_plugin_type_identity` drives the real production `PluginManager`/`KPluginFactory` dlopen path (no test bypass) to load the actual `corbomite-file-explorer.so`, constructs a `NotesTreeModel` inside the plugin's own compiled code, and `qobject_cast`s it back from the host — demonstrated **red** in a scratch `git worktree` at the pre-P3 commit (nullptr, no error, exactly the predicted symptom) before being trusted green today. `tst_no_duplicate_metaobjects` (new structural guard, `cmake/CheckNoDuplicateMetaObjects.cmake`) caught a real, live divergence outside P3's scope entirely on its first run: `src/sidebar/PropertyEditorWidget.cpp`/`PropertyRow.cpp` were compiled once into `CorbomiteApp` and a second time directly into `corbomite-properties.so`'s sources — fixed by linking the plugin against `CorbomiteApp` instead of recompiling; verified the guard catches this class of bug by reverting the fix and confirming red before restoring it. Allowlist for future markoff-family/graffodil duplication (P6) is genuinely empty today — no such duplication exists yet. 325/325 offscreen.
- **2026-08-20 — Cluster P Phase P3 closed: all 8 remaining Corbomite libraries flipped STATIC→SHARED.** `search`, `forcegraph`, `core` (now a leaf post-P2, largest blast radius but clean), `storage`, `models`, `bases` (largest `.a`, 51 MB), `canvas`, `jkqtmathtext` — leaf-first order, one commit each, suite green after every single flip (no batching). `search`/`forcegraph`/`canvas`/`jkqtmathtext` needed a fresh `install(TARGETS ...)` rule added (none had one while `STATIC`). One flake (`tst_quadtree`, unrelated to the `bases` flip it landed on) was the only suite noise across all 8. `build-dev/` dropped **6.5 GB → 3.6 GB** (11 GB → 3.6 GB, **-67%**, end to end across P1+P3). `libcorbomite-core.so` (62 MB) and `libCorbomiteApp.so` (47 MB) are now the two largest binaries in the tree — ahead of every test executable, the inverse of the pre-refactor shape.
- **2026-08-20 — Cluster P Phase P2 closed: `core`↔`storage` dependency cycle broken.** Moved `MarkoffAdapters.{h,cpp}` (core's one textual storage dependency) into `libs/storage`; discovered along the way that the whole file is `#if 0`-disabled end to end (parked pending `Markoff::Vault::*` restoration), so it compiled to zero symbols even before the move — lower-risk than the plan assumed, but the CMake-level link-graph cycle was real regardless and still needed cutting. Moved `DataAdapter.h` (pure-abstract, header-only) the other way, into `libs/core`, fixing the inverted dependency `TextFileView.cpp` had on it; ~11 includers across core/storage/vault/tests repointed. New structural guard `tst_no_library_cycles` (`cmake/CheckNoLibraryCycles.cmake`, greps `cmake --graphviz` output for the edge) demonstrated red against the pre-fix tree, green after. 323/323 offscreen, no linkage-type changes in this phase (pure layering fix, per doctrine D1).
- **2026-08-20 — Cluster P Phase P1 closed: `CorbomiteApp` STATIC→SHARED, live-verified, -41% `build-dev/`.** One-line flip (`src/CMakeLists.txt`) plus a new named test `tst_xmlgui_resource_present` guarding the compiled-in KXMLGUI `.rc` resource against the shared-vs-static self-registration risk P1.T2 flagged. `build-dev/` dropped 11 GB → 6.5 GB in one commit — every former 131-147 MB test binary (43 link sites) is now 55-56 MB, sharing one `libCorbomiteApp.so` instead of each embedding a private 47 MB copy. Live-verified twice: dev build (user confirmed menus/toolbars render and update correctly across tabs/document types) and the installed release build (`sudo cmake --install build-release`, launched from `/usr/local/bin`, user confirmed working) — RPATH needed zero new configuration, already inherited `RUNPATH: [/usr/local/lib]` from the same machinery that made `libvault.so`'s install work. 322/322 offscreen.
- **2026-08-20 — Cluster P Phase P0 closed: baseline measured, ccache installed.** Clean `dev`-preset build: 1475s wall time (no ccache), 11 GB total (`bin/` 8.9 GB, `lib/` 613 MB), largest test binary 147 MB, incremental relink after touching `View.h` 161s — matches the audit's figures. Symbol-duplication check (P0.T3) confirmed C2 empirically: `Corbomite::NotesTreeModel`/`SearchResultsModel` (Models/Search) already hold two different `staticMetaObject` definitions between the exe and `corbomite-search.so`/`corbomite-file-explorer.so` today; `Bases::BasesView` has no plugin consumer at all currently, so P4's correctness gate should target Models or Search, not Bases. `/home` free space improved to 34 GB/85% since the audit's 13 GB/95% — refactor no longer under acute disk pressure but still the right call for the correctness fix. `ccache` installed (needed interactive sudo, done by the user) and wired into all three `CMakePresets.json` presets via `CMAKE_C/CXX_COMPILER_LAUNCHER`, added after the baseline was captured so it wouldn't skew the numbers; verified actually invoked via a verbose incremental build. Audit corrections addendum appended per P0.T1. 321/321 offscreen, no regressions.
- **2026-08-20 — Cluster P opened: STATIC → SHARED internal library refactor, dispatch priority next.** Investigating why `build-dev/` runs 10-11GB per worktree (33 app-level test binaries at 131-147MB each, since every internal library — Corbomite's own `libs/*`, all of `markoff-family`, all of `graffodil` — is `STATIC`, so every test binary embeds its own private copy of everything it links) surfaced a more important finding: `libs/vault` was already flipped `STATIC`→`SHARED` in Cluster Q specifically because plugins (`dlopen`-loaded `.so`s) got a private copy of its RTTI/`QMetaObject`s when static, silently breaking `qobject_cast` across the host/plugin boundary. Every other library plugins link — confirmed via `corbomite_add_plugin`'s `LINK_LIBRARIES` pass-through, e.g. `src/plugins/backlinks/CMakeLists.txt` linking `Corbomite::Core` — has the identical latent bug today, just not yet triggered by what current in-tree plugins happen to cast. User: no other developers to inconvenience, wants to support future plugin authors, open to plugin-architecture rework if needed to enable this — directed it to the top of the queue, ahead of Cluster O Phase O4 and Cluster L5 soak resumption. Full evidence, feasibility findings (no export-macro work needed — project never sets `-fvisibility=hidden`; RPATH and packaging already generic per the vault precedent), and cross-repo scope analysis (Corbomite-only vs. also touching `markoff-family`/`graffodil` for the full win) in the audit report. **Superseded in part same-day by the plan-expansion pass — see Last touched: the audit's "latent everywhere" framing and its "no structural blockers" premise were both refuted empirically, and the plan's §1 is normative over the audit where they disagree.**
- **2026-08-20 — Cluster N merged to `master`: rich clipboard (copy-as + smart paste), ported onto Cluster O's `MarkdownViewActions`.** N5 eyeball had already passed on the branch (multi-flavor copy, Copy as, HTML/RTF paste, Reading-faithful copy, Quote → LibreOffice Block Quote via `bafa3095`). A pre-merge cleanup pass found the stalled paste-fix agent had left canvas Smart paste still calling `applyFlatEdit` (never the finished `Cmd::pasteMarkdown` — so pasted tables/headings/lists still flattened to paragraphs) and a BlockQuote typed-vs-loaded marker asymmetry bug present in **both** canvas and Styled (Styled was missing ListItem markers too — worse than canvas's pre-fix state) — all fixed and tested (markoff-family 210/210). Merging found Cluster O's O3.T6 had since removed `MainWindow::addEditorActionBase` (the mechanism N's host wiring depended on) in favor of the `MarkdownViewActions` provider; ported the 8 clipboard actions + their `hasCursor()`/`canEdit()` enable-state onto that provider (merging a `<Menu name="edit">` into the universal Edit menu, same pattern O3.T6 already uses for the View menu's Editor Mode group) rather than reintroducing the flat pre-O3 mechanism. Image paste/drop still out of scope. Plan: [`superpowers/plans/2026-08-20-cluster-n-rich-clipboard.md`](superpowers/plans/2026-08-20-cluster-n-rich-clipboard.md).
- **2026-08-20 — Cluster O audited + planned; Cluster I closed by absorption.** Full action/menu/toolbar/sidebar audit ([report](audit-2026-08-20-context-sensitive-ui.md)) found **zero of ~79 KActions belong to canvas, bases, or graph** (39 markdown-only, 17 permanently-disabled stubs, 7 silent no-ops off-markdown) and that the polymorphic seams for this already exist and rotted (`View::zoomIn/Out/Reset` overridden with empty TODOs and bypassed by `MainWindow`). User answered all 12 open questions; doctrine of record: three tiers (presence keyed on view type / enablement keyed on view-answered capability / check-state), **hide other types' menus + disable this type's unavailable actions**, per-view-type `KXMLGUIClient` providers swapped on the factory, toolbars tri-state and user-overridable, sidebars **not** contextual, graph controls relocated into the graph pane, ribbon stays plugin-only, canvas settings app-wide in kcfg. Verified during planning that `applyFlatEdit` creates only `BlockKind::Paragraph` with no kind re-parse — which cut the "cheap stub" set from 7 to 3 and generated a Markoff steer (O7.T3). Reason: canvas is functionally capable after Cluster M but its settings are unreachable, and patching one-off actions would deepen the static-chrome problem.
- **2026-08-18 — v0.1.0 tagged and released; canonical host moved Codeberg → GitHub.** Discovered via git-log audit — five commits landed after the last docs refresh and were never written up. `origin` retargeted to GitHub (`db51acd6`), `libs/markoff-family` submodule repointed to GitHub Markoff (`16b27bdb`), `v0.1.0` tagged with Ubuntu `.deb` CI + AppImage + Arch PKGBUILD packaging (`50a4f424`), two post-tag CI/compat fixes (`f288cadd`/`884e63c3` build deps, `313586de` markoff-family Qt 6.9 bump, `95167dde` Qt 6.10 gate), and a README rewrite with screenshots (`80a1e605`). **Cluster L5 soak was mid-flight and is paused, not closed**, as a result. Known loose end: a stale "Codeberg" mention in a `MainWindow.cpp` comment (cosmetic only). Also flagged: `~/dev/CLAUDE.md`'s cross-project registry still lists Corbomite as Codeberg-owned — needs a manual fix outside this repo. Full detail: `decisions-archive.md`.
- **2026-08-18 — Cluster K Phase 5 closed: QML LivePreview retired; Canvas is the sole engine.** User directed skipping the soft Phase 4 default-flip and going straight to Phase 5. `NoteEditorWidget` always constructs `Markoff::Canvas::EditorWidget`; `markoff_live` / Qt QuickWidgets dropped from Corbomite's link line (`MARKOFF_BUILD_LIVE=OFF` in the submodule); `CanvasLivePreview` kcfg/settings toggle removed. Canvas retire-on-destroy fixed to also clear the composed `View`'s document pointer (UAF on resize after vault teardown). **308/308** offscreen (excl. benchmark). Callouts still frozen on Markoff E3. Detail: `decisions-archive.md`.
- **2026-08-18 — Cluster L Phase L5: found + fixed a P0 nested-split-scrambling bug in the Obsidian round-trip path.** A real Obsidian-authored 3-pane vault, opened in Corbomite, had every pane except the anchor land in the wrong quadrant. Root causes: Obsidian's `"vertical"`/`"horizontal"` split-direction convention was mapped backwards in `WorkspaceSerializer` (direction names the divider line's orientation, not the stacking axis — code comments had it inverted), plus `materializeSplit`'s nested-split anchor-threading placed a nested child's internal structure before its outer siblings existed, carving up the wrong KDDW frame. Fixed via a two-phase seed-then-fill materialization algorithm; took two live-test iterations (first fix only addressed the direction inversion) before the user confirmed the layout matched Obsidian exactly. New geometry-asserting regression test added (`fixture16`) since the prior nested-split test only checked JSON shape, not actual on-screen position, and passed throughout the bug's lifetime. Detail: punch-list `[cluster-l]`, decisions-archive.
- **2026-08-18 — Cluster L Phase L4 closed: live dogfood pass confirms D1/D4.** User live-tested the D1 (KDDW title-bar removal, middle-click tab close) and D4 (sidebar-width restore) changes against the running app and confirmed both work as intended. Phase L4 is done; next is Phase L5 (soak & closeout).
- **2026-08-17 — Cluster K: all 13 actionable canvas dogfood findings fixed and merged to master.** `feature/find-replace` (which had accumulated Find/Replace, hover-preview re-light, and the full Cluster K canvas-adoption arc) merged into `master`; see the Cluster K Phase 3 entries below for the dogfood detail. `libs/markoff-family` re-pinned to `1fd3ef11`. Only the callouts finding remains open (frozen, blocked on Markoff E3).
- **2026-06-14 — Find/Replace shipped (branch `feature/find-replace`).** Phase 2 placebo removal: Replace built on the consumer-owned `FindBar` via new upstream `MarkoffDocument::replaceMatches` (one-undo) + mutation-free `FindController::selectMatchAtOrAfter` (Markoff `master` `c3b5070f`, re-pinned `49f8ac7a`); hamburger Find…/Replace… wired. Two dogfood bugs fixed: black highlights (`ThemeService` stub returned an empty `Markoff::Theme{}` → now palette-based `defaultLight/Dark`) and find-highlights stuck after a replace (markoff-live `applyOps` wiped adapter-owned `findSpans`). 271/271 offscreen. Part B open (Insert Table/Callout dialogs, multi-snippet search `SQLiteIndex.cpp:461`). Detail: [`decisions-archive.md`](decisions-archive.md).
- **2026-06-10 — Full code/docs audit + docs reset.** 9-agent verification pass over all live docs, the obsidian-audit corpus (vs decompiled source), feature parity (vs code), and code quality. Outcomes: punch list re-triaged (3 closed, ④ bucket unblocked → RE-TARGETABLE, `.obsidian` artifacts P4→P1, +21 verified items), 40 executed/obsolete plans+specs archived, 4 correction addenda in `obsidian-audit/addenda/`, new living [`PARITY-MATRIX.md`](PARITY-MATRIX.md), CLAUDE.md/OPS/port-doc drift fixed, `qmarkdowntextedit` symlink + stale `build/` removed. Roadmap: [`plans/2026-06-10-road-to-dogfood.md`](superpowers/plans/2026-06-10-road-to-dogfood.md). Test baseline verified: 250/251 offscreen.
- **2026-06-04 — Retired-renderer tests gated; embed bug steered to Markoff.** `tst_markdownrenderer`/`tst_renderengine` QSKIP-gated + EditorSuggest test mock clamped (`a6a664d5`); `![[…]]` embed image-node target bug handed to Markoff (`b6ae2c0f`). Markoff has since completed its MarkdownView contract-v2 arc — adoption brief awaits (see banner).
- **2026-05-30 — Canvas cards render via StyledRenderEngine.** `StyledRenderEngine` wraps `Markoff::Styled::DocumentRenderer` (`e7a40ae2`); canvas wiring + re-render + teardown (`dee26c2f`, `aaca39b7`, `658bbee8`). First working render engine since the port.
Older closeouts (2026-04-26 reset → 2026-04-28) and all full closeout paragraphs: [`decisions-archive.md`](decisions-archive.md).

## Open questions

- **Confirm J's obsolescence** before formal closeout: does `Markoff::Source::Editor` fully cover the qutepart-fork intent (visual-line scroll, fold serialization, find/replace, markdown awareness)? (Plan file archived 2026-06-10 with this caveat noted.)
- **Eyeball-verification backlog.** Eight UI work-packages shipped offscreen-only and have never been seen by a human: D.2 grouped rendering, D.3 panels/drawer, D.4a cell clicks, D.4b export/+New, D.4c undo + drift banner, formula editor popups, filter builder dialog, properties panel. One consolidated session in front of the running app would clear all eight. (Completion popup **eyeball DONE 2026-06-11** — all five modes confirmed; surfaced + fixed two popup-sizing bugs, `62e63a24`.)

## Last touched

2026-08-21 — **Cluster O Phase O4 (`CanvasViewActions`) code-complete + offscreen-green, live-eyeball still pending.** New Canvas menu/toolbar: snap-to-grid/snap-to-objects/show-grid (kcfg-backed, fans out to every open canvas leaf, not just the focused one) and zoom in/out/reset/fit/to-selection, plus three O4.T6 disabled M5-placeholder stubs. Full detail in `decisions-archive.md`. 331/331 offscreen (two new tests, `tst_canvas_view_actions`/`tst_canvas_settings_fanout`); three pre-existing tests had a stale "canvas has no provider" assumption from before O4, now fixed. **Next: live-verify O4 against a real canvas** (project memory: offscreen-green is necessary, never sufficient, for chrome changes), then Phase O5 (`BasesViewActions`/`GraphViewActions`).

2026-08-21 — **Both stalled subagent worktrees from the prior session's punch-list sweep finished and landed on `master`.** Search regex/match-case toolbar toggles (`6461bf05`, `tst_search_options_regex`) and 5 swallowed-error → `Notice::post` sites (`c6267e2b`, one signal per lib: `Vault::documentSaveFailed`, `PluginManager::pluginLoadFailed`, `MetadataCache::cacheOpenFailed`, `WorkspaceLeaf::viewTypeUnresolved`) — full detail in `decisions-archive.md`. 330/330 offscreen throughout; both worktrees removed. **Next: continue the punch-list sweep** — `docs/punch-list.md` P2 section (hotkeys.json application, workspace.json split-dimension persistence) and Cluster O Phase O4 (`CanvasViewActions`) remain the highest-value open items.

2026-08-21 — **Cluster P CLOSED: Phase P6 (cross-repo — markoff-family + graffodil) landed on user go-ahead, closing the cluster end to end.** 13 more libraries flipped STATIC→SHARED (`build-dev/` 3.6 GB → 1.8 GB, -84% from the P0 baseline). Verification caught a real cross-repo regression — re-pinning Markoff to `master` HEAD instead of the `feature/rich-clipboard` commit Corbomite's old pin actually sat on silently dropped Cluster N's clipboard API; Corbomite's own incremental rebuilds missed it twice, the Arch PKGBUILD's fresh clone caught it immediately — fixed by merging the branch upstream and re-verifying with genuinely clean (`rm -rf`) rebuilds throughout. Full narrative: [`decisions-archive.md`](decisions-archive.md), 2026-08-21.

2026-08-20 — **Cluster P Phase P4 closed: the plugin-boundary correctness gate — the phase that actually justifies this cluster, proven empirically rather than merely asserted.** `tst_plugin_type_identity` (P4.T1/T2) doesn't take the shortcut a test easily could: it drives the real production `PluginManager`/`KPluginFactory` `dlopen` path (deliberately not `setFactoryOverride`, which constructs the plugin class directly in-process and would prove nothing about the DSO boundary) to load the actual built `corbomite-file-explorer.so`, lets it construct a `Corbomite::NotesTreeModel` inside its own compiled code exactly as `FileExplorerView` already does in production, and `qobject_cast`s the resulting `QAbstractItemModel*` back to `NotesTreeModel*` from the host test process — the confirmed-diverged case P0.T3 found. Per D4 ("a gate never seen red proves nothing"), red was demonstrated empirically rather than theoretically: a scratch `git worktree` was created at `7f7dd3b5` (the commit right after P2 closed, last commit before any P3 flip), the test file and its CMake wiring were copied in, `git submodule update --init --recursive` populated `markoff-family`/`graffodil` fresh, the full dependency chain was built from scratch, and the test failed with exactly the predicted symptom — `qobject_cast` returning nullptr, no crash, no error, nothing but a silent wrong answer. Confirmed green back on `master`; worktree discarded. `tst_no_duplicate_metaobjects` (P4.T3, `cmake/CheckNoDuplicateMetaObjects.cmake`, a `cmake --graphviz`-sibling script test following `tst_no_library_cycles`'s pattern) walks every built module's `nm -D --defined-only` output and asserts no `staticMetaObject` symbol is defined in more than one — its first real run caught a genuine live divergence **entirely outside P3's scope**, not a hypothetical or a markoff-family/graffodil case: `src/sidebar/PropertyEditorWidget.cpp` and `PropertyRow.cpp` were being compiled once into `CorbomiteApp`'s sources and a second time directly into `corbomite-properties`'s sources, producing 8 duplicated `staticMetaObject` symbols (`PropertyRow`, `PropertyEditorWidget`, and six nested `PropertyEditor` subclasses) that P1-P3's library flips could never have touched since these were never in a shared-library target to begin with. Fixed by linking the plugin against `CorbomiteApp` (`SHARED` since P1) instead of recompiling. The fix's correctness was verified two ways: forward (all 8 symbols reduced to zero duplicates after) and backward (reverted the fix via `git stash`, rebuilt just the plugin, confirmed the guard failed listing all 8 symbols by name and both defining modules, then restored the fix and confirmed green) — proving the guard actually catches this bug class, not just passing vacuously. The allowlist for expected future duplication (Phase P6's markoff-family/graffodil libraries) is genuinely empty today, confirmed by the same sweep that found the properties bug — no such duplication exists yet, so nothing was allowlisted away. P4.T4 added a note at `cmake/CorbomitePlugin.cmake`'s `LINK_LIBRARIES` — where a future plugin author would actually make the mistake — explaining `QMetaObject::cast()`'s pointer-identity mechanics and calling out both variants of the mistake (linking a `STATIC` library, or compiling the same source twice) since P4.T3 caught the second one in the wild. 325/325 offscreen throughout (322 baseline + `tst_no_library_cycles` + `tst_xmlgui_resource_present` + `tst_plugin_type_identity` + `tst_no_duplicate_metaobjects`). §7's acceptance checklist mostly ticked; only packaging items remain. **Next: Phase P5** — packaging verification. AppImage build + launch + open a vault (most likely place for a missed-bundle failure per the plan); re-verify the installed release build launches and loads all 9 plugins now that every Corbomite library is `SHARED` (P1 already proved this works for a partial-SHARED state; P5 confirms the full post-P3 set); Arch `PKGBUILD` build; Ubuntu `.deb` is CI-only, dry-run or note deferred to the next tag. Then **Phase P6** (cross-repo — markoff-family + graffodil) needs an explicit user go-ahead per §3's scope call before starting, now that P5's measurements will be available to inform that decision.

2026-08-20 — **Cluster P Phase P3 closed: all 8 remaining Corbomite-owned libraries flipped STATIC→SHARED, leaf-first, one commit per library.** Order followed the plan's dependency table exactly: `corbomite-search` and `forcegraph` (true leaves) first, then `corbomite-core` (a leaf itself post-P2, and the largest blast radius in the whole cluster since nearly everything links it — flipped clean with zero configure errors, the concrete proof the P2 cycle break actually worked), then `corbomite-storage` (Core), `corbomite-models` (Core/Storage/Vault — also the type C2 flagged as already-diverged today), `corbomite-bases` (largest `.a`, 51 MB), `canvas` (Core/Search/`Graffodil::Core` — Graffodil itself stays `STATIC`, its object code is simply baked into `libcanvas.so` until P6), and `jkqtmathtext` (vendored LGPL — SHARED is also the licence-friendlier form, avoiding the relinkable-object-files obligation a static link carries). `search`/`forcegraph`/`canvas`/`jkqtmathtext` all needed a fresh `install(TARGETS ...)` rule added since none had one while `STATIC` (search got the full `EXPORT CorbomiteTargets` treatment matching core/storage/models/bases/vault since it has a `Corbomite::Search` alias; the other three got a plain install rule, no alias to export). Corrected `libs/core`'s stale `BUILD_INTERFACE` comment in the same commit as the `core` flip, per the plan's watch item — it used to claim "plugins must resolve these submodule symbols themselves," true only while `core` was `STATIC`; now that object code is baked into `libcorbomite-core.so` once and plugins get it for free. Font resources embedded in `jkqtmathtext` self-registered correctly at `.so` load, same self-init behaviour P1 already confirmed for the KXMLGUI resource. One flake (`tst_quadtree`, surfaced on the `bases` flip's run — passed standalone immediately and on a full clean re-run, no connection to `bases`) was the only test-suite noise across all 8 flips; every other flip's suite run was clean on the first try. **Disk result:** `build-dev/` dropped from P1's 6.5 GB to **3.6 GB** — **11 GB → 3.6 GB, -67%, end to end across P1+P3 combined**; `libcorbomite-core.so` (62 MB) and `libCorbomiteApp.so` (47 MB) are now the two largest binaries in the tree, ahead of every remaining test executable — the inverse of the pre-refactor shape where the largest items were 33 near-identical test binaries each embedding a private copy of everything. §5 measurements table updated with the full before/after; wall-clock rebuild timings were not re-measured (P3 was 8 incremental single-library flips, not a clean rebuild, so they wouldn't be comparable to the P0 baseline's clean-build numbers). 323/323 offscreen at every step. **Next: Phase P4** — the plugin-boundary correctness gate that justifies the whole cluster. `tst_plugin_type_identity` must be written and demonstrated **red** against pre-P3 `master` (checked out at a commit before this session, or from git history) before being shown green now, per D4/C2 targeting `Corbomite::NotesTreeModel` or `SearchResultsModel` (confirmed-diverged in P0.T3) — not `Bases`, which has no plugin consumer to prove the fix against. Then `tst_no_duplicate_metaobjects` (symbol-level guard across the exe + all built `.so`s, with a documented allowlist for markoff-family/graffodil until P6).

2026-08-20 — **Cluster P Phase P2 closed: broke the `corbomite-core` ↔ `corbomite-storage` dependency cycle (C3), the blocker for Phase P3.** Pure layering fix, no linkage-type changes (doctrine D1 — every phase lands green in isolation). `MarkoffAdapters.{h,cpp}`, core's one textual storage `#include`, moved to `libs/storage/{include/corbomite/storage/markoff_adapters,src}`; discovered mid-task that the entire file — its own storage includes, every class body, and its sole call site in `MainWindow.cpp` — is `#if 0`-disabled pending upstream `Markoff::Vault::*` restoration, so it compiles to an empty translation unit today and contributed zero live symbols. That made the move lower-risk than C3 assumed (no behavioural surface to preserve), but doesn't change the fix: `target_link_libraries` edges are structural regardless of what's inside `#if 0`, so the cycle was real and still needed cutting. The other direction — `DataAdapter.h`, a pure-abstract header-only interface `core` consumes and `storage` implements via `FileSystemAdapter` — moved from `libs/storage` to `libs/core/include/corbomite/core/DataAdapter.h`, fixing what was actually an inverted dependency; ~11 includers across `core`/`storage`/`vault`/3 test files repointed, none needed a link-graph change since everything already linked `Corbomite::Core` transitively. A dead manual `target_include_directories(corbomite-core PRIVATE .../storage/include)` workaround (existed only to make the old `DataAdapter.h` path resolve) was removed alongside the `Corbomite::Storage` link line. New structural guard `tst_no_library_cycles` (`cmake/CheckNoLibraryCycles.cmake`, registered via `add_test` at the top-level `CMakeLists.txt`) shells out to `cmake --graphviz` against the configured build tree and greps for a `corbomite-core -> corbomite-storage` edge, with a sanity check that the retained `storage -> core` edge is still visible so a silent grep-always-passes bug can't hide. **Demonstrated red against the still-cyclic tree before touching any code**, then green after — per D1's "gate never seen red proves nothing" spirit, extended here even though P2 isn't P4's formal correctness gate. 323/323 offscreen (322 + the new test). **Next: Phase P3** — flip the remaining Corbomite libraries `STATIC`→`SHARED` one at a time, leaf-first per the plan's dependency-ordered table (`corbomite-search` and `forcegraph` first as true leaves, then `corbomite-core` — now a leaf itself post-P2 — then `storage`/`models`/`bases`/`canvas`/`jkqtmathtext`), suite green after each individual flip, not batched.

2026-08-20 — **Cluster P Phase P1 closed: `CorbomiteApp` flipped STATIC→SHARED, live-verified against both the dev and installed release builds.** One-line change (`src/CMakeLists.txt:11`) plus a new named regression test, `tests/app/tst_xmlgui_resource_present.cpp`, guarding P1.T2's risk that the compiled-in KXMLGUI `.rc` resource (drives every menu/toolbar) stops self-registering once `CorbomiteApp` is a shared library instead of static — it passed cleanly, confirming shared-library Qt resources self-initialise at load with no `Q_INIT_RESOURCE` call needed. Disk win landed exactly as C4 predicted: `build-dev/` dropped **11 GB → 6.5 GB (-41%)** in one commit, since all 43 former link sites (tests/editor, tests/app, dialogs, e2e, graph, storage) now share one `libCorbomiteApp.so` instead of each embedding a private 47 MB `.a` copy — every former 131-147 MB test binary is now 55-56 MB. Live-verified twice per the standing chrome-adjacent gate (project memory `feedback_verify_ui_fixes_live`): the dev build (user confirmed menus/toolbars render and update correctly switching tabs and document types) and the installed release build (`sudo cmake --install build-release`, launched from `/usr/local/bin`, user confirmed working) — RPATH needed zero new configuration, `readelf -d` shows the installed exe already carries `RUNPATH: [/usr/local/lib]` inherited from the same KDECMakeSettings/ECM machinery that already made `libvault.so`'s SHARED install work, so the "first RPATH this tree has ever required" risk flagged for P5 doesn't apply to P1. 322/322 offscreen (321 baseline + the new test), no regressions. §5 measurements table updated with the full before/after. **Next: Phase P2** — break the `corbomite-core` ↔ `corbomite-storage` dependency cycle (C3's blocker), the prerequisite for P3's remaining library flips. Move `MarkoffAdapters.{h,cpp}` from `libs/core` to `libs/storage` and resolve `TextFileView.cpp`'s `DataAdapter.h` include direction; named test `tst_no_library_cycles` guards against the cycle silently reappearing.

2026-08-20 — **Cluster P Phase P0 closed: baseline measured, corrections addendum landed, ccache installed.** Clean `dev`-preset build measured 1475s wall time, 11 GB total (`bin/` 8.9 GB, `lib/` 613 MB, largest test binary 147 MB) — matches the audit's figures exactly; incremental relink after touching `View.h` (the relink-storm case) is 161s. `/home` free space has improved to 34 GB/85% since the audit's 13GB/95% snapshot — the refactor is no longer under acute disk pressure, though the plugin-boundary correctness fix (C2) remains the stronger justification. P0.T3's symbol-duplication check confirmed C2 empirically rather than by inspection: `Corbomite::NotesTreeModel`/`SearchResultsModel` (Models/Search) already hold two distinct `staticMetaObject` definitions between the exe and `corbomite-search.so`/`corbomite-file-explorer.so` **today**; `Bases::BasesView` has zero plugin consumers currently, so **P4's correctness gate should target Models or Search, not Bases**. The §1 corrections were appended to `audit-2026-08-20-shared-libraries-refactor.md` as a dated addendum (P0.T1). `ccache` needed interactive `sudo` unavailable to the assistant — user installed it directly — then wired into all three `CMakePresets.json` presets via `CMAKE_C/CXX_COMPILER_LAUNCHER`, added *after* the baseline was captured so it wouldn't skew the numbers, and confirmed actually invoked via a verbose incremental build (`compile_commands.json` looked ccache-free but was just stale — the real Make rule showed `ccache /usr/bin/c++ ...`). 321/321 offscreen, no regressions. **Next: Phase P1** — flip `CorbomiteApp` `STATIC`→`SHARED` (the biggest single disk win, zero plugin-boundary risk, one-line change plus the KXMLGUI-resource verification called out in the plan).

2026-08-20 — **Cluster P planned and ready to execute: STATIC → SHARED internal library refactor.** The [plan](superpowers/plans/2026-08-20-cluster-p-shared-libraries.md) expands the stub into phases P0–P6 with named tests, doctrine D1–D7, a measurements table and a standing-traps list. The expansion pass verified the research audit empirically (`nm`/`readelf`/`ldd` against the built tree, not by reading CMake) and **corrected it on three counts — the plan's §1 is normative where it disagrees with the audit**: (C3, blocker the audit missed) `corbomite-core` ↔ `corbomite-storage` is a **dependency cycle**, and CMake tolerates cycles only when every target in the SCC is `STATIC`, so neither can flip until it's cut — mitigated by the cut being thin (only `MarkoffAdapters.cpp` carries real symbols; `TextFileView.cpp`'s `DataAdapter.h` is a header-only pure-abstract interface contributing zero, and `MarkoffAdapters` has exactly one consumer tree-wide). (C1/C2) The audit's "why it hasn't bitten yet" reasoning is wrong — the real mechanism is ELF interposition, with `libvault.so` re-exporting 37 `Corbomite::*` `staticMetaObject`s and accidentally acting as libcore, while the exe exports **no strong symbols at all**; that accident doesn't cover `Models`/`Search`/`Bases`/`canvas`/`forcegraph`, where host and plugin **already hold different `QMetaObject`s today** (verified on `Corbomite::NotesTreeModel`). So the acceptance gate must target a diverged library — a test against a `Core` type would pass pre-refactor and prove nothing. (C4) The largest single disk contributor, `CorbomiteApp` (47MB `.a`, 43 link sites), is linked by **no plugin**, so it has no correctness dimension and is independent of both the cycle and the cross-repo question — it leads as Phase P1 instead of being bundled with the risky work. **Scope assumed: P0–P5 (Corbomite-only) committed, P6 (`markoff-family`/`graffodil`) planned but separately gated on user go-ahead after P5's measurements — confirm at dispatch.** Context: `/home` is at 95% (13GB free), `ccache` confirmed absent (companion task P0.T4). **Next: dispatch Phase P0** (baseline measurement + append the §1 corrections to the audit doc as a dated addendum).

2026-08-20 — **Cluster N merged to `master`, ported onto Cluster O's `MarkdownViewActions`.** Branch had already closed (N5 eyeball passed: multi-flavor copy, Copy as, HTML/RTF paste, paste-as-plain, Reading-faithful copy, Quote → LibreOffice Block Quote via Markoff `bafa3095`), but sat unmerged while `master` moved through Cluster M M4 and all of Cluster O. Pre-merge cleanup found the stalled paste-fix agent's work incomplete (canvas Smart paste still called `applyFlatEdit`, never the finished `Cmd::pasteMarkdown` — pasted tables/headings/lists flattened to paragraphs; reproduced against a real LibreOffice table paste) and a BlockQuote typed-vs-loaded marker asymmetry in both canvas and Styled (Styled also missing ListItem markers entirely — worse than canvas). All fixed, markoff-family 210/210. Merging then found O3.T6 had removed `MainWindow::addEditorActionBase`, the mechanism N's host wiring was built on; ported the 8 clipboard actions + enable-state onto `MarkdownViewActions` (`<Menu name="edit">` merge, same pattern already used for the View menu's Editor Mode group) rather than reintroduce the flat pre-O3 mechanism. Detail: punch-list, decisions-archive.

2026-08-20 — **Cluster O Phase O3 closed: `ViewActions` provider mechanism landed, live-verified.** Two commits (`e22ef7de` `ViewActions`/`ToolBarPolicy` base; `a7e04fb0` `MarkdownViewActions` + controller client-swap + tri-state toolbar policy + `<Merge name="viewtype_merge"/>` in `corbomiteui.rc.in` (version 10→11) + `SettingsDialog` multi-collection support). 320/320 offscreen; one Tier-B refresh bug found and fixed (Reading mode wasn't disabling format verbs). **Live-eyeballed and confirmed**: Format/Heading/Insert/Table and the editor-mode group now genuinely vanish (not grey out) off markdown, File/Edit/Go/View/Settings/Help never shift, and the toolbar's Auto/Always Show/Always Hide context menu works. The first live look appeared broken (menus present but permanently empty everywhere) — root cause was **not a code bug**: dev builds across different git worktrees share one KXMLGUI cache keyed only by app/component name, and a stale cache from the same-day `feature/rich-clipboard` worktree run (independently also `version="11"`, so the bump didn't self-heal) had baked Format/Heading/Insert/Table in as permanent, un-removable base-document entries. Deleting the stale cache fixed it; noted as a standing caveat in `CLAUDE.md`'s Dev Build Isolation section. Full detail: `decisions-archive.md`. **Next: Phase O4** (`CanvasViewActions` — snap/grid/zoom toggles for canvas; closes punch-list `[ui-bundle][canvas][P2][cluster-o]`). Also carries the standing live-eyeball gate.

2026-08-20 — **Cluster O Phase O2 closed: View Tier-B capability contract landed.** Two commits (`d8c414ac` `View::canEdit/canSave/canZoom/canFind/hasSelection/canUndo/canRedo` + `contextChanged()` signal, per-type overrides in MarkdownView/CanvasFileView/BasesView; `ade51e03` `ActionContextController` consumes it generically, replacing the O1-era per-type undo/redo branch and the bespoke Reading-mode `hasEditing()` read). 318/318 offscreen. Two real bugs found and fixed by the new tests: `insert_table`/`insert_callout` were still wrongly enabled in read-only Reading mode despite O1.T6 claiming that fix; a `CanvasFileView` teardown crash where `QUndoStack`'s destructor-time `clear()` emits into a signal on an already-torn-down intermediate base class. Full detail: `decisions-archive.md`. **Next: Phase O3** (`ViewActions` provider mechanism, markdown-only, zero new features — the first phase with a live-eyeball gate).

2026-08-20 — **Cluster O Phase O1 closed: `ActionContextController` extracted, all seven silent no-ops fixed.** Three commits (`7c9faf91` zoom dispatch onto real markdown/canvas/graph viewports, `c3fbf00d` `BasesView::focusSearch()` + real undo-stack-depth queries, `bbe1c658` the controller extraction itself + tests). 317/317 offscreen. Found and fixed one incidental bug along the way: a new test's stack-allocated `GraphView` reparented onto a stack-allocated `QWidget` double-destructed (reverse local-destruction order); fixed in the test, not production code. Full detail: `decisions-archive.md`. **Next: Phase O2** (Tier-B capability contract — `View::canEdit/canSave/canZoom/...` virtuals + `contextChanged()` signal).

2026-08-20 — **Cluster O audited, planned, and O0 executed; Cluster I closed.** Full investigation of every KAction, menu, toolbar and sidebar surface, written up at [`audit-2026-08-20-context-sensitive-ui.md`](audit-2026-08-20-context-sensitive-ui.md). Corbomite has **five disjoint action surfaces**; only one (the static `KActionCollection` + `corbomiteui.rc`) reaches shortcuts, the command palette, and the Hotkeys page, and it is shaped exclusively for markdown — **zero of its ~79 actions belong to canvas, bases, or graph**, 39 are markdown-only, 17 are permanently disabled, the entire Table menu and fold group are dead on every launch, and 7 actions silently no-op on non-markdown tabs (Ctrl+S does nothing on a canvas; Ctrl+F does nothing on a bases tab that has its own search box). Seven concrete bugs surfaced, incl. an action-state refresh gap when a leaf swaps view type **in place** (`setActiveLeaf` early-returns so only `viewChanged` fires, and nothing but back/forward listens — click a `[[foo.canvas]]` link and the Format menus stay live) and a `GraphControlsPanel` shared-singleton with no disconnect (two graph tabs cross-talk). Key reframing: the polymorphic seam for all of this was **already built by legacy Cluster V and rotted** — `View::zoomIn/Out/Reset` are base virtuals, `MarkdownView`'s overrides are empty TODOs, and `MainWindow` bypasses them entirely. User answered all 12 open questions; the [full plan](superpowers/plans/2026-08-20-cluster-o-context-sensitive-ui.md) is written with doctrine (§D1–D7), phases O0–O7, named tests, an appendix assigning every action to a destination, and a standing-traps list. Planning also verified that `MarkoffDocument::applyFlatEdit` creates every new block as `BlockKind::Paragraph` with no kind re-parse, which cut the user-approved "implement the cheap stubs" set from 7 actions to 3 and produced both a Markoff handoff steer (O7.T3) and a new punch-list item (template insertion probably lands multi-block templates as flat paragraphs — verify-first). **Phase O0 executed this session:** Cluster I closed by absorption with a [retro](cluster-retros/cluster-i-ui-surfacing.md) (its Phase 5 tail folded into O5.T5, Phase 6 punch-listed), plan archived, INDEX updated, three new punch-list items filed, and the Cluster M↔O boundary recorded normatively in the M plan (M5 owns canvas capabilities, O4 owns canvas chrome; **M5.2's on-canvas control cluster is now questionable and must be reassessed** rather than built alongside a canvas toolbar). **Next: dispatch Phase O1** — correctness-only, no new mechanism: extract `ActionContextController`, subscribe to `viewChanged`, re-light the zoom virtuals, and make every silently-no-op action either work or disable itself. Acceptance gate is `tst_action_context_no_silent_noop`.

2026-08-20 — **Cluster M Phase M4 live-tested; snap-drag-never-releases bug found + fixed; canvas grid made zoom-adaptive; Cluster O opened.** Live pass on M4's move/snap work found a real bug: dragging a card into an alignment guide made the drag permanently stuck — `SelectMoveTool` derives each frame's proposed position from the primary node's CURRENT (possibly already-snapped) `pos()`, so once a frame snapped onto a candidate, every later frame's proposed position was computed relative to that pinned point, and ordinary per-frame mouse deltas are almost always smaller than the snap tolerance — the search kept re-matching the same candidate at zero offset forever. Fixed (`917bc1b3`) by having `CanvasAlignmentStrategy` track its own free-running (never-pinned) drag position, advanced each frame by the raw mouse delta regardless of whether the applied position is pinned; a new regression test drives `align()` the way `SelectMoveTool` actually does and was verified to fail pre-fix. Same session, the user also flagged the canvas grid's dots didn't scale sanely with zoom (fixed pitch regardless of zoom → visual noise zoomed out, near-invisible zoomed in) — fixed standalone (`CanvasView::drawBackground` now reuses `CanvasAlignmentStrategy::gridSpacingForScale`'s pitch ladder, so drawn grid and snap pitch can't drift apart, thins further beyond that ladder's own floor for extreme zoom-out since M5's zoom clamp hasn't landed yet, and uses a cosmetic 0-width pen so dots stay a constant 1 device-pixel regardless of zoom). 156/156 offscreen throughout. User also flagged that canvas has **zero exposed KActions** for its own settings (snap toggles, grid-visibility toggle) and that editor-mode/markdown-editing chrome stays prominent regardless of what kind of tab is focused — **canvas view is still rather unusable in practice** until the menu/toolbar/sidebar become context-sensitive to the focused tab's document type. Opened **Cluster O** (stub, plan-needed) to track that as a dedicated UX-refactor cluster rather than patching canvas one-off; filed the KActions gap to the punch list (`[ui-bundle][canvas][P2][cluster-o]`). Plan: [`superpowers/plans/2026-08-20-cluster-o-context-sensitive-ui.md`](superpowers/plans/2026-08-20-cluster-o-context-sensitive-ui.md). **Next for M: M4.4's chrome-overlay visual feel still wants a closer look; next for O: brainstorm/audit session** (full menu/toolbar/sidebar action inventory tagged by document type, survey active-leaf-change signals to hook into, decide the action-group-registration mechanism) before a phased plan can be written.

2026-08-20 — **Cluster M Phase M4 (move/snap/selection polish) code-complete, offscreen-green — NOT yet closed.** Four subagent-driven TDD tasks landed in one commit (`85712ae9`): M4.1 new `CanvasAlignmentStrategy` (grid + object snap, both default ON, Alt disables) wired into `Graffodil::SelectMoveTool`'s `align()` hook, which M4.2 also uses for Shift axis-lock-on-move; M4.2 also added Shift aspect-lock on corner resize, redesigned arrow-key nudge (grid-pitch-stepped, replacing a flat 1px/10px, and — a real bug fix — now actually pushes `CmdMoveCards` onto the undo stack, which the old `item->moveBy()` code never did), and edge auto-pan while dragging near the viewport edge; M4.3 replaced `GroupItem`'s live per-frame center-test child-dragging with Obsidian's model — membership captured once at drag start via full-rect containment, frozen for the whole gesture — plus z-order by `-width*height`; M4.4 centralized three near-identical, non-zoom-constant per-item resize-handle paint blocks into one `CanvasNodeChromeOverlay` (`ItemIgnoresTransformations`, retargeted to the selected/hovered node), added connection dots read straight off `CanvasNodeItem::anchors()` (same data source `CreateEdgeTool`'s own hit-test uses, so they can't drift), and added hover tracking + per-zone resize cursors (neither existed before). M4.4 also found and fixed a real crash: `QGraphicsScene::selectionChanged` firing during base-class teardown after `~CanvasScene()` already returned (the item's dynamic type had decayed) — disconnected as the first line of the destructor, same precaution already taken for `m_focusConnection`. **156/156 offscreen throughout.** **Next: live-eyeball pass** before Phase M4 can be marked closed — M4.4's chrome overlay is explicitly eyeball-gated in the plan (handle/dot sizing, colors, hover-vs-selection dot visibility are all best-guess pending a live look), and M4.2's auto-pan/aspect-lock feel needs the same standing check as every other canvas-interaction phase; then Phase M5 (viewport/commands/persistence). Note: this work landed on `master`, same as M0-M3 — unrelated to the concurrent Cluster N work below, which is isolated on its own worktree/branch precisely because it overlaps in time with Cluster M.

2026-08-19 — **Cluster M Phase M3 (edge authoring) CLOSED — live-verified.** Five subagent-driven TDD tasks (`bfec72c8`): M3.1 Obsidian-exact bezier path/arrowhead (`CanvasBezierStrategy`/`CanvasArrowTerminus`, replacing Graffodil's stock curve); M3.2 drag-from-anchor edge creation via Graffodil's `CreateEdgeTool`; M3.3 drop-on-empty create-and-connect popup (compound `CmdAddCard`+`CmdAddEdge`); M3.4 endpoint reconnect — required a new `CanvasEdgeGestureTool` dispatcher because Graffodil's `CompositeTool::addAnchorRoute` always wins routing priority via unconditional prepend, so a plain reconnect route could never be tried first; also fixed `CanvasScene::onEdgeChanged` to rebuild the edge item (not just its data) when `fromNode`/`toNode` change, since `GraphEdgeItem` has no node-identity setter; M3.5 Direction submenu (Nondirectional/Unidirectional/Bidirectional, new `CmdSetEdgeEnds`) plus made "Reverse Direction" (menu + R key) actually undoable, which it previously wasn't. 155/155 offscreen throughout. **Live eyeball found + fixed a real segfault** (`d93f10e2`): right-click an edge → Edit Label → type → Enter crashed, because `QLineEdit::returnPressed` fires synchronously from inside the line edit's own `keyPressEvent`, and the connected handler did `delete proxy` on the `QGraphicsProxyWidget` wrapping that same line edit — use-after-free once the handler returned into the now-freed call frame. The group-label editor had the identical latent bug (same `returnPressed`-connected pattern), fixed alongside it even though not yet tripped live. Both now `removeItem()` synchronously (safe — just detaches from the scene) and `deleteLater()` instead of a raw `delete`. User-confirmed fixed; all other edge-authoring flows (draw, label, reconnect, redirect, delete) confirmed working live. **Next: Phase M4** (move/snap/selection polish — grid/object snapping, modifier polish, arrow-key nudge, edge auto-pan).

2026-08-19 — **Cluster M Phase M2 closed: node-creation flows live-verified.** All six creation flows (double-click, context-menu file picker, drag-drop, clipboard, Alt-drag duplicate, new-canvas command) landed and were live-confirmed working; three bugs found during the pass are fixed (a `~CanvasScene` heap-corruption double-delete, a file-picker path leak, an Alt-drag zero-movement duplicate). Two gaps punch-listed rather than fixed in-phase: file picker isn't `Vault`-backed, canvas has no theme/dark-mode awareness. Full detail: decisions-archive.md. **Next: Phase M3** (edge authoring).

2026-08-19 — **Cluster M Phase M1 closed: `libs/canvas` rebased onto Graffodil::Core, live-verified.** Subagent executed M1.0–M1.6 (verification pass, `CanvasNodeItem`/`CanvasEdgeItem` classes, scene rebase onto `Graffodil::GraphScene`, tool assembly via a bespoke `CompositeTool` — `DefaultGraphTool` couldn't be used as-is, see spec §6a divergence note — undo wiring, and 19 test slots incl. 6 new named tests) across two commits (`00d89904`, `6f476273`), 313/313 offscreen. Live eyeball (M1.7) then found and fixed two bugs: app-wide Ctrl+Z/Redo never checked for an active canvas tab (`MainWindow::activeCanvasView()`, `fb672574`), and PNG/SVG export rendered a black background instead of matching the on-screen white (unset `QGraphicsScene::backgroundBrush()`, `2e06b966`). Both user-confirmed fixed; select/move/resize (incl. the headline file-card-unselectable bug fix), inline edits, export, and undo/redo all live-verified across every node kind. Canvas's total lack of dark-mode/theme awareness punch-listed as a follow-up, deliberately not fixed inside M1's frozen scope. **Next: Phase M2** (node creation flows — double-click-empty, context-menu file picker, drag-drop, clipboard, Alt-drag duplicate, new-canvas command).

2026-08-19 — **Cluster M documentation hardened for hand-off: M1 migration contract spec + executor-grade plan.** User redirected from implementation to exacting documentation so weaker agents can execute the cluster. Both API surfaces (all Graffodil v0.2.3 core headers; all of `libs/canvas`) were read in full and distilled into [`superpowers/specs/2026-08-19-cluster-m1-graffodil-rebase-design.md`](superpowers/specs/2026-08-19-cluster-m1-graffodil-rebase-design.md) — complete class-by-class mapping (incl. the two non-obvious calls: canvas groups must NOT map onto Graffodil's decorative `GroupItem`; edge anchor ids are exactly the `sideToString()` strings), signal→undo-command table, resize stays a consumer tool, file disposition table, named regression tests, 4 pre-code verification reads (V1–V4), and the only 3 intended behavior changes. The cluster plan's phases M1–M6 are now checkbox task lists with named tests; Appendix A pins every normative Obsidian constant (sizes, `clamp(dist/2,70,150)`, 20/40/80/160 grid, 15/scale tolerance, log2 zoom clamp, z=−area, …); Appendix B is the standing-rules/traps list (tools-never-mutate, anchor-id contract, `dynamic_cast` ban, eyeball gate, state tiering). **Next: dispatch Phase M1 starting at task M1.0** (verification reads), any capable agent.

2026-08-19 — **Cluster M Phase M0 closed: Graffodil consumption unblocked and landed.** Graffodil replied to the adoption handoff same day (reply at `~/dev/Graffodil/docs/handoff/2026-08-19-reply-to-corbomite-canvas-adoption.md`): submodule consumption blessed without waiting on 6d (Graffodil's `origin` is GitHub — no mirror lag), pin `v0.2.3`, and the M3 edge-gesture gap was fixed upstream (`CreateEdgeTool` press-drag-release + `setDragOnly(true)` + new `edgeDroppedOnEmpty` signal). Submodule added at `libs/graffodil` (v0.2.3, `dd7667de`), wired via `add_subdirectory`, all six `libgraffodil-*.a` build in the dev preset, **313/313** offscreen. **Next: Phase M1** — feature-frozen rebase of `libs/canvas` onto `Graffodil::Core` (fixes the file-cards-unselectable bug structurally). Detail: plan file M0 resolution block.

2026-08-19 — **Cluster M opened: canvas authoring parity, Graffodil-rebase-first.** Full gap survey of `.canvas` authoring vs the audit's feature catalogue (`obsidian-audit/domains/canvas.md` §9): round-trip fidelity is solid but authoring is nearly absent — file cards aren't even selectable/movable (`SelectMoveTool` casts only Text/Group items), `CreateEdgeTool` is dead code, no creation flows/snapping/reconnect/viewport-persistence/link-indexing. User chose rebasing `libs/canvas` onto Graffodil::Core (now v0.2.2 — phases 1–5 + edge labels + editor affordances complete) over extending the hand-rolled scene. Plan: [`superpowers/plans/2026-08-19-cluster-m-canvas-authoring-parity.md`](superpowers/plans/2026-08-19-cluster-m-canvas-authoring-parity.md). **Next: Phase M0** — Graffodil consumption gate (6d install/export or submodule sign-off, handoff doc to Graffodil) before any Corbomite-side code.

2026-08-18 — **Cluster K Phase 5 closed: Canvas-only LivePreview; QML leaf retired from Corbomite.** Skipped Phase 4 soft-default per user sign-off; deleted `Markoff::Live` construction, `markoff_live`/`markoff_liveplugin` link lines, Qt Quick/QuickWidgets/Qml from Corbomite's `find_package`, and the `CanvasLivePreview` settings toggle. Submodule gained `MARKOFF_BUILD_LIVE` (Corbomite forces OFF) plus a canvas retire-on-destroy fix (`View::setDocument(nullptr)` on document destroy — previously only the MarkdownView base was cleared, leaving a UAF on resize). Tests retargeted to `linkService()` / `canvasEditor()`. **308/308** offscreen; binary no longer NEEDs `libQt6QuickWidgets` (Quick/Qml may still appear via KDDockWidgets). **Next:** resume Cluster L L5 soak, or packaging now that Canvas-only is landed. Full detail: `decisions-archive.md`.

2026-08-18 — **Cluster L Phase L5 (soak dogfood) in progress: found + fixed a P0 Obsidian-round-trip nested-split-scrambling bug.** User dogfooded a real Obsidian-authored 3-pane vault; Corbomite scrambled every pane but the anchor into the wrong quadrant on open. Root cause was two compounding bugs in `WorkspaceSerializer.cpp`: Obsidian's split-`direction` convention (divider-line orientation, not stacking axis) was mapped backwards, and `materializeSplit`'s nested-split anchor-threading recursed into a child's internal structure before its outer siblings were placed, carving up the wrong KDDW frame. Rewrote materialization as a two-phase seed-then-fill algorithm; took two live-test round trips (first attempt fixed only the direction inversion, still produced a different wrong layout) before the user confirmed a match with Obsidian's own rendering. Added `fixture16_nestedSplitFirstChild_matchesObsidianGeometry` — a geometry/adjacency-asserting regression test, since the existing nested-split test (`fixture03`) only checked JSON shape and passed throughout the bug's life. 311/312 offscreen (same pre-existing flake). **Next: continue Phase L5 soak** — drag between groups, split, popout, close-undo, vault switch, then closeout. Full detail: `decisions-archive.md`, punch-list `[cluster-l]`.

2026-08-18 — **Cluster L Phase L4 closed: live dogfood pass confirms D1/D4.** User confirmed, against the running app, that D1 (hidden redundant title bar, middle-click tab close) and D4 (sidebar-width restore across restart) both work as intended — clearing the standing gate noted below (offscreen-green alone wasn't sufficient for keyboard/focus-affecting changes). Phase L4 is done. **Next: Phase L5** (soak & closeout dogfood — drag between groups, split, popout, close-undo, vault switch, Obsidian workspace.json round-trip). Full detail: `decisions-archive.md`.

2026-08-17 — **Cluster L Phase L4 code landed: native UX polish — NEEDS LIVE VERIFICATION before closing.** Back/forward (Ctrl+Alt+←/→, mouse buttons 4/5, real enablement state), tab commands (Ctrl+1..9 jump, pin-tab, move-to-new-window, advisory-only toggle-stacked), and sidebar-width persistence (Phase L2 had built the storage but never wired real values through) are all built and offscreen-green. **D1 (redundant title-bar removal, middle-click tab close) and D4 (width restore) have NOT been seen rendered live** — this is the standing gate (project memory: an offscreen-green keyboard-focus fix was previously found broken live) before the phase can be called done. 311/312 offscreen, no regressions. Full detail: `decisions-archive.md`.

2026-08-17 — **Cluster L Phase L3 landed: cruft removal.** Deleted dead Obsidian-shape shells (`WorkspaceRoot`/`WorkspaceContainer`/`WorkspaceSidedock`, the nullptr-returning `leftSplit()`/`rightSplit()` — kept `WorkspaceFloating`/`WorkspaceWindow`, which have real popout callers); consolidated duplicated `ensureKddwInit`; fixed router O(chain×leaves) focus-handler perf; debounced `Workspace::resize()`'s event mirror (`activeLeafChanged` deliberately left synchronous — real sync consumers exist). **C6** (tab-group cache slimming) and **`eState.scroll`** re-anchoring were deliberately **not** done — both judged out of proportion for this phase (see decisions-archive for why). 311/312 offscreen, same single pre-existing flake, no regressions. **Next: Phase L4** (native UX polish — needs live eyeball per user's standing feedback on keyboard/focus changes) or **Phase L5** (soak & closeout dogfood). Full detail: `decisions-archive.md`.

2026-08-17 — **Cluster L Phase L2 landed: one writer, full fidelity.** Implements the L0 tier doctrine in code: `Workspace::writeWorkspaceJson` promoted to production (fixes B1's floating/lastOpenFiles drop), `_corbomite`/`left-ribbon` denylisted on load (no migration needed — zero installed users), `SessionManager` split into tier-2 (`.obsidian/corbomite/state.json`) + tier-3 (app-data `session.json`) writers, B4 sidecar-map growth bounded, B3 id-assignment fixed (dimension round-trip punch-listed as a known gap — writer rebuilds fresh from KDDW's LayoutSaver each save). Golden fixture + `tst_workspace_session.cpp` cover the fidelity contract. 310/313 offscreen, 2 unrelated pre-existing/flake failures. Full detail: `decisions-archive.md`.

2026-08-17 — **Cluster L opened: workspace/KDDW stabilization & nativization.** Full re-evaluation of the tab/dock layer against the 2026-04-26 workspace audit + current code. Verdict: architecture sound (KDDW substrate + Obsidian-shaped facade is right; the worst audit findings were already fixed), but three live disease clusters: teardown fragility (crash engine — `Workspace::deserialize` bare `qDeleteAll` with no `releaseDockWidget`, four inconsistent destruction disciplines), persistence split-brain (production save drops `floating`+`lastOpenFiles`; `_corbomite` keys in the Obsidian-shared workspace.json; empty node ids / no `dimension`), Obsidian-literalism residue (nullptr shells, dead writers, undebounced event mirrors). Plan: [`superpowers/plans/2026-08-17-cluster-l-workspace-stabilization.md`](superpowers/plans/2026-08-17-cluster-l-workspace-stabilization.md). **Next: Phase L0 (compat-boundary spec, needs user sign-off) then L1 (teardown unification) — L1 before K Phase 4.**

2026-08-17 — **Cluster K Phase 3 closed: 10/14 dogfood findings fixed and live-user-confirmed.** Live re-test of the 5 fixed via subagent TDD (wikilink nav, double/triple-click, Ctrl+Scroll zoom, F3 scroll, image/embed placeholders) confirmed 4 as-is and surfaced a deeper bug in #5 (wikilink click always opened a new tab — a pre-existing Corbomite-side navigation gap, not canvas-specific, fixed in `44d9eb66`); also fixed Ctrl+=/Ctrl+-/Ctrl+0 keyboard zoom (`03f7ceb1`). Remaining 4 findings are low-severity polish, not blockers. **Next: Phase 4 (dogfood-as-default) — recommended over pausing for more Markoff engine work; see the G2-closing handoff doc.** Closeout: `decisions-archive.md`.

2026-08-17 — **Cluster K Phase 2/3: canvas dogfood pass, 5/14 findings fixed.** First hands-on test of the `Markoff::Canvas::EditorWidget` LivePreview toggle surfaced 14 bugs (filed to punch-list `[cluster-k]`); 5 were fixed directly in the `libs/markoff-family` submodule and user-confirmed against the running app (empty-note lockup, `$$` math lockup, undo phantom-link, doubled list marker, Tab focus leak) — full detail + commit shas in punch-list.md and `decisions-archive.md`. **Next step:** pick up the remaining 9 `[cluster-k]` findings from the punch list (wikilink click/middle-click nav is next by severity), or move to Phase 4 (dogfood-as-default) once satisfied with Phase 3 coverage — user's call. Closeout: [`decisions-archive.md`](decisions-archive.md).

2026-06-12 — **Phase 2 hover preview re-lit + eyeball-confirmed.** Hovering a `[[wikilink]]` shows a styled preview of the target note: `HoverPopover::renderTarget` re-pointed from the retired `Markoff::Reading` stack onto the shipped `StyledRenderEngine` + a `VaultResourceProvider` resolver, rendered into a `QTextBrowser`; the trigger rides the shared `LinkService` (`linkHovered`/`linkHoverLeft`), so one pair of connects covers both leaves — no upstream Markoff work or re-pin. **Runtime behaviour confirmed: Reading = plain hover; Live = Ctrl-then-hover** (the Live `LiveView.qml` gates hover emission on Ctrl — Obsidian-faithful; kept by user decision, NOT a bug). Subpath (`#heading`/`#^block`) slicing deferred; **270/270** offscreen (excl. benchmark). **Phase 2 remaining:** placebo removal (wire-or-hide Insert Table/Callout + hamburger Find/Replace), checkbox-in-Reading (upstream). Closeout: [`decisions-archive.md`](decisions-archive.md).

2026-06-11 — **Phase 2 status-bar word count + template-at-cursor shipped.** Two self-contained road-to-dogfood Phase 2 items: (1) status-bar word count now tracks the live document — `NoteEditorWidget` seeds the count on attach and re-emits on `NoteDocument::textChanged` (was permanently "Words: 0"); Ln/Col already landed in Phase 1, so **status-bar honesty is complete** (`fb47120e`). (2) Template insertion lands at the caret via one undo-integrated D2 `applyFlatEdit` (was append-at-end), with two pure unit-tested `LineResolve` bridges (`082589ae`). Corbomite **267/267** offscreen (excl. benchmark). **Checkbox-toggle-in-Reading was scoped and deferred** — it needs an upstream markoff-styled click handler (alongside `LinkInteraction`) + a re-pin, not a Corbomite-only edit (see punch list). **Phase 2 remaining:** hover preview (`HoverPopover` → `StyledRenderEngine`), placebo removal (wire-or-hide Insert Table/Callout + hamburger Find/Replace), checkbox-in-Reading (upstream). Closeout: [`decisions-archive.md`](decisions-archive.md).

2026-06-11 — **Completion revival shipped (A1–A3) + popup polish.** The upstream `Markoff::MarkdownView::caretRect()` contract addition (submodule re-pinned `af91a936` → `e2fefbd2`) feeds a new leaf-agnostic `CompletionController`, delivering `[[` names + aliases + `#`-headings + `^`-existing-blocks and `#` tags across all editable leaves; spec [`superpowers/specs/2026-06-11-completion-revival-design.md`](superpowers/specs/2026-06-11-completion-revival-design.md), plan [`superpowers/plans/2026-06-11-completion-revival.md`](superpowers/plans/2026-06-11-completion-revival.md). A live eyeball pass confirmed all five modes and fixed two `CompletionPopup` sizing bugs (one-row height; over-elided detail column) — `62e63a24`, closing the P5 polish deferral. Suites: markoff 270/273 (3 known queue-#10 reds), Corbomite **265/265**. Remaining `^id`-creation / code-block-suppression deferrals punch-listed under the 2026-06-11 header; closeout in [`decisions-archive.md`](decisions-archive.md). **Next focus: road-to-dogfood Phase 2 remaining items** — status-bar word count, hover preview (re-target `HoverPopover` → `StyledRenderEngine`), placebo removal (wire-or-hide Insert Table/Callout + hamburger Find/Replace), template-at-cursor, checkbox-toggle-in-Reading.

Build/test: `cmake --build --preset dev -j 10`. Tests: `cd build-dev && QT_QPA_PLATFORM=offscreen ctest -E benchmark --output-on-failure -j 10` (offscreen is mandatory). Work on `master` (no feature branches). **Never `git add -A`** — `testvaults/` is deliberately dirty; stage by path. Pre-pull.
