# Project state

> Slim session-start orientation. **Reset 2026-04-26** after a full audit (`docs/audit-2026-04-26/`) regrouped everything into a fresh A-onwards cluster scheme + a flat punch list. Pre-reset state archived at `docs/archive-2026-04-26/PROJECT-STATE-pre-reset.md`.

> **2026-05-25 — Foundation port landed on `master`.** Markoff's QML/D2 rebuild merged to Markoff `master` (tag `v0.7.0-freeze`); Corbomite re-pinned there and merged `port/foundation-exploration` → `master`. The old four-leaf QGraphicsView editor is retired; the QML/D2 live editor is canonical. The rewrite **obsoleted clusters G, H, J** and **re-scopes E**. **Reading mode resolved 2026-05-29: read-only `Markoff::Styled::Editor` leaf** (`775fa54e`; the earlier read-only-Live steer is retired). Port doc (historical): [`docs/port-foundation-exploration.md`](port-foundation-exploration.md). Handoffs: [`docs/handoff/`](handoff/).
>
> **2026-06-10 — Docs reset + full audit.** Code/docs drift audit (9 verification agents): punch list re-triaged (+21 verified items, ④ bucket unblocked), 40 executed/obsolete plans+specs archived, obsidian-audit corrections in `obsidian-audit/addenda/` (4 new), live parity tracking moved to [`docs/PARITY-MATRIX.md`](PARITY-MATRIX.md). Roadmap to a dogfoodable release: [`docs/superpowers/plans/2026-06-10-road-to-dogfood.md`](superpowers/plans/2026-06-10-road-to-dogfood.md).

## Two tracks

Work flows through **two parallel tracks**. Both must be checked at session start.

1. **Punch list** — flat severity-ranked list of small fixes. File: [`docs/punch-list.md`](punch-list.md). Pick from top (P0 first). 58 items at reset.
2. **Strategic clusters** — multi-phase coordinated initiatives. Index: [`docs/superpowers/plans/INDEX.md`](superpowers/plans/INDEX.md). 10 active at reset (A–J).

Most P0/P1 punch-list items are **silent vault-format corruption risks**. Drain them before strategic-cluster work unless explicitly redirected.

The P0/P1 bands were re-verified 2026-06-10 and drained in Phase 0 (branch `feature/phase0-data-safety`): non-atomic `saveDocument`, `modify()` reconciliation, link-clicks-dead, LinkResolver staleness, BOM strip, and DB relocation — all fixed. Remaining open items start at P1 (`onExternalRenamed` descendants) and P2; see punch-list.

## Active strategic clusters (snapshot)

| Cluster | Title | Status | Source |
|---|---|---|---|
| D | Bases UI completion | **Only D.5 (plugin API) remains** — needs fresh spec; D.1–D.4c + formula editor + filter builder shipped 05-25..05-28 (plans archived) | Audit |
| E | Markoff Editor API parity | **Re-scope vs D2 block model** — stub body targets the retired editor (see banner in plan file) | Audit |
| F | Internal-plugin gap fill | Plan-needed (stub) — substrate-independent, dispatchable after brainstorm | Audit |
| I | Editor & Workspace UI surfacing | **Needs rewrite** — its header was false (Phases 1–4 shipped 2026-04-20 as legacy Cluster V); live remnant = Phase 5 tail + Phase 6 | Carried (was Cluster V) |
| L | Workspace/KDDW stabilization & nativization | **L0-L4 landed + closed. L5 PAUSED (not closed) as of 2026-08-18** — soak dogfood found + fixed a P0 nested-split-scrambling bug in the Obsidian round-trip path (direction-mapping inversion + anchor-threading order); see punch-list `[cluster-l]`. Soak was interrupted same-day by the v0.1.0 release push (below) and has not resumed. **Next: resume L5 soak** (drag/split/popout/close-undo/vault-switch), then closeout | New 2026-08-17 |
| M | Canvas authoring parity (Graffodil rebase first) | **M0-M3 CLOSED (2026-08-19). Next: M4 (move/snap/selection polish)** | New 2026-08-19 |
| N | Rich clipboard (copy-as + smart paste) | **CLOSED 2026-08-20 on `feature/rich-clipboard` (N5 eyeball passed).** Merge pending — `master` still mid-M4. Image paste/drop out of scope. | New 2026-08-20 |
| K | Markoff canvas leaf adoption | **CLOSED 2026-08-18 (Phase 5)** — Canvas is the sole LivePreview engine; QML `markoff_live` unlinked. Callouts remain frozen on Markoff E3 | New 2026-08-15 |

Closed (post-reset): A (2026-04-27), B (2026-04-28), C (2026-04-27), **K (2026-08-18)**, **N (2026-08-20, on `feature/rich-clipboard` — merge pending)**.

**Obsoleted by the foundation rewrite (2026-05-25):** G (inline-ORC coherence — no more QTextDocument/ORC substrate), H (block-substitution widgets — peer-delegate model is the new baseline; math/mermaid → Markoff E5), J (qutepart Source fork — likely superseded by `Markoff::Source::Editor`; confirm before formal closeout). See INDEX + decisions-archive.

Full table + plan-file links: [`docs/superpowers/plans/INDEX.md`](superpowers/plans/INDEX.md).

## Recent decisions

- **2026-08-20 — Cluster N CLOSED: rich clipboard (copy-as + smart paste), N5 eyeball passed.** On `feature/rich-clipboard` / `.worktrees/rich-clipboard`; Markoff `bafa3095` (incl. blockquote HTML/RTF export for LibreOffice). Merge pending while `master` is mid-M4. Image paste/drop out of scope. Plan: [`superpowers/plans/2026-08-20-cluster-n-rich-clipboard.md`](superpowers/plans/2026-08-20-cluster-n-rich-clipboard.md).
- **2026-08-18 — v0.1.0 tagged and released; canonical host moved Codeberg → GitHub.** Discovered via git-log audit — five commits landed after the last docs refresh and were never written up. `origin` retargeted to GitHub (`db51acd6`), `libs/markoff-family` submodule repointed to GitHub Markoff (`16b27bdb`), `v0.1.0` tagged with Ubuntu `.deb` CI + AppImage + Arch PKGBUILD packaging (`50a4f424`), two post-tag CI/compat fixes (`f288cadd`/`884e63c3` build deps, `313586de` markoff-family Qt 6.9 bump, `95167dde` Qt 6.10 gate), and a README rewrite with screenshots (`80a1e605`). **Cluster L5 soak was mid-flight and is paused, not closed**, as a result. Known loose end: a stale "Codeberg" mention in a `MainWindow.cpp` comment (cosmetic only). Also flagged: `~/dev/CLAUDE.md`'s cross-project registry still lists Corbomite as Codeberg-owned — needs a manual fix outside this repo. Full detail: `decisions-archive.md`.
- **2026-08-18 — Cluster K Phase 5 closed: QML LivePreview retired; Canvas is the sole engine.** User directed skipping the soft Phase 4 default-flip and going straight to Phase 5. `NoteEditorWidget` always constructs `Markoff::Canvas::EditorWidget`; `markoff_live` / Qt QuickWidgets dropped from Corbomite's link line (`MARKOFF_BUILD_LIVE=OFF` in the submodule); `CanvasLivePreview` kcfg/settings toggle removed. Canvas retire-on-destroy fixed to also clear the composed `View`'s document pointer (UAF on resize after vault teardown). **308/308** offscreen (excl. benchmark). Callouts still frozen on Markoff E3. Detail: `decisions-archive.md`.
- **2026-08-18 — Cluster L Phase L5: found + fixed a P0 nested-split-scrambling bug in the Obsidian round-trip path.** A real Obsidian-authored 3-pane vault, opened in Corbomite, had every pane except the anchor land in the wrong quadrant. Root causes: Obsidian's `"vertical"`/`"horizontal"` split-direction convention was mapped backwards in `WorkspaceSerializer` (direction names the divider line's orientation, not the stacking axis — code comments had it inverted), plus `materializeSplit`'s nested-split anchor-threading placed a nested child's internal structure before its outer siblings existed, carving up the wrong KDDW frame. Fixed via a two-phase seed-then-fill materialization algorithm; took two live-test iterations (first fix only addressed the direction inversion) before the user confirmed the layout matched Obsidian exactly. New geometry-asserting regression test added (`fixture16`) since the prior nested-split test only checked JSON shape, not actual on-screen position, and passed throughout the bug's lifetime. Detail: punch-list `[cluster-l]`, decisions-archive.
- **2026-08-18 — Cluster L Phase L4 closed: live dogfood pass confirms D1/D4.** User live-tested the D1 (KDDW title-bar removal, middle-click tab close) and D4 (sidebar-width restore) changes against the running app and confirmed both work as intended. Phase L4 is done; next is Phase L5 (soak & closeout).
- **2026-08-17 — Cluster K: all 13 actionable canvas dogfood findings fixed and merged to master.** `feature/find-replace` (which had accumulated Find/Replace, hover-preview re-light, and the full Cluster K canvas-adoption arc) merged into `master`; see the Cluster K Phase 3 entries below for the dogfood detail. `libs/markoff-family` re-pinned to `1fd3ef11`. Only the callouts finding remains open (frozen, blocked on Markoff E3).
- **2026-06-14 — Find/Replace shipped (branch `feature/find-replace`).** Phase 2 placebo removal: Replace built on the consumer-owned `FindBar` via new upstream `MarkoffDocument::replaceMatches` (one-undo) + mutation-free `FindController::selectMatchAtOrAfter` (Markoff `master` `c3b5070f`, re-pinned `49f8ac7a`); hamburger Find…/Replace… wired. Two dogfood bugs fixed: black highlights (`ThemeService` stub returned an empty `Markoff::Theme{}` → now palette-based `defaultLight/Dark`) and find-highlights stuck after a replace (markoff-live `applyOps` wiped adapter-owned `findSpans`). 271/271 offscreen. Part B open (Insert Table/Callout dialogs, multi-snippet search `SQLiteIndex.cpp:461`). Detail: [`decisions-archive.md`](decisions-archive.md).
- **2026-06-10 — Full code/docs audit + docs reset.** 9-agent verification pass over all live docs, the obsidian-audit corpus (vs decompiled source), feature parity (vs code), and code quality. Outcomes: punch list re-triaged (3 closed, ④ bucket unblocked → RE-TARGETABLE, `.obsidian` artifacts P4→P1, +21 verified items), 40 executed/obsolete plans+specs archived, 4 correction addenda in `obsidian-audit/addenda/`, new living [`PARITY-MATRIX.md`](PARITY-MATRIX.md), CLAUDE.md/OPS/port-doc drift fixed, `qmarkdowntextedit` symlink + stale `build/` removed. Roadmap: [`plans/2026-06-10-road-to-dogfood.md`](superpowers/plans/2026-06-10-road-to-dogfood.md). Test baseline verified: 250/251 offscreen.
- **2026-06-04 — Retired-renderer tests gated; embed bug steered to Markoff.** `tst_markdownrenderer`/`tst_renderengine` QSKIP-gated + EditorSuggest test mock clamped (`a6a664d5`); `![[…]]` embed image-node target bug handed to Markoff (`b6ae2c0f`). Markoff has since completed its MarkdownView contract-v2 arc — adoption brief awaits (see banner).
- **2026-05-30 — Canvas cards render via StyledRenderEngine.** `StyledRenderEngine` wraps `Markoff::Styled::DocumentRenderer` (`e7a40ae2`); canvas wiring + re-render + teardown (`dee26c2f`, `aaca39b7`, `658bbee8`). First working render engine since the port.
- **2026-05-29 — Reading mode = read-only Styled leaf; architecture audit.** `775fa54e` ships reading as a read-only `Markoff::Styled::Editor`; retired-Reading stub dropped (`ab242ab2`); old read-only-Live steer formally retired (`c7daea89`). Same day: architecture snapshot `audit-2026-05-29-architecture.md` (disposition update appended 2026-06-10).
- **2026-05-28 — Cluster D (Filter Builder) shipped.** Subagent-driven TDD, 4 feature commits (`73e67498..9591ce55`): pure `FilterSpec` value tree + `fromFilter`/`toFilter` converters bridging the immutable `FilterNode` (single-child And/Or collapse via `optimize()`, Not preserved, blank-leaf drop); `FilterBuilderWidget` recursive group editor (leaves = `FormulaInput`, nested groups = child `FilterBuilderWidget`s, `validityChanged` only on flip); `FilterBuilderDialog` scope toggle (per-view + global) with OK gated on both valid; `BasesView` Filters toolbar button → `openFiltersDialog` → public `applyFilterSpecs` → `onConfigMutated`. New tests: `tst_filter_spec` (7), `tst_filter_builder_widget` (5), `tst_filter_builder_dialog` (3), + `tst_bases_view_wiring::applyFilterSpecs_writesBothScopesAndRoundTrips`. All 28 bases/formula/filter tests green; clean build; offscreen launch clean. **Nested builder + dialog rendering pending user eyeball.** Only **D.5 (plugin API)** remains in Cluster D. Closeout: decisions-archive.md.
- **2026-05-28 — `[canvas][interop]` P0 bundle shipped.** All four punch-list items closed via TDD in one session (4 feature commits, `a8b63603..8d00c865`): (1) unknown-field passthrough — `QJsonObject extraData` on `CanvasNode`/`CanvasEdge` + a private `m_extraTopLevel`; capture-set/spread-back model so plugin- and future-Obsidian-written canvases survive a Corbomite save unchanged (was silent data loss). (2) Default-value omission — only actual gap was `backgroundStyle:"cover"`; positive test covers all six defaults so a future regression on color/label/subpath/fromEnd/toEnd fails fast; the dead if/else for `fromSide`/`toSide` removed (always emitted per invariant 2). (3) Missing-side self-heal — ported V5 as `pickSideToward()` (aspect-aware angular sector, explicitly distinct from A3 drag-snap per §8 invariant 10); resolves absent sides during edge parse and persists them in-memory so the next save bakes a concrete value in, killing the would-be diff ping-pong with Obsidian. (4) Geometry rounding — `qRound(toDouble())` on x/y/width/height ends the off-by-one drift on cross-app saves. 29/29 tst_canvasdocument green (6 new tests); full canvas suite green; full ctest pending. Closeout: decisions-archive.md.
- **2026-05-28 — Canvas domain audit + `.canvas` interop punch bundle + markoff-styled handoff (`b769ad78`, pushed).** Audit-expansion pass on Obsidian's Canvas: full 15-section domain doc at [`obsidian-audit/domains/canvas.md`](obsidian-audit/domains/canvas.md) (reverse-engineered from `app.pretty.js`+`app.css` via 6 parallel agents — canvas isn't in the renamed `tree/` corpus, lives inline in the bundle ~lines 186000–196000). Headline finding: the fidelity cliff for canvas cards is *content rendering*, not whiteboard plumbing — Obsidian cards are real `MarkdownPreviewView`s. Four `[canvas][interop]` fixes appended to bottom of P0 (`docs/punch-list.md`): unknown-field passthrough (**DATA LOSS** — lead item), default-value omission on write, missing edge-side self-heal (port Obsidian's `V5` angular-sector picker), integer rounding on parse — all in `libs/canvas/src/CanvasDocument.cpp`, bundleable into one TDD session against a golden round-trip test. Proactive steer to Markoff at [`handoff/2026-05-28-to-markoff-styled-for-canvas-cards.md`](handoff/2026-05-28-to-markoff-styled-for-canvas-cards.md) proposes three Tier-1 additions to `libs/markoff-styled` (extract `DocumentRenderer` from `Editor`'s lifecycle, `idealHeight(width)`, embed-renderer registration hook) that unblock its use as the per-card renderer — no `QWebEngineView` dep needed. No reply required from Markoff. Closeout: decisions-archive.md.
- **2026-05-27 — Cluster D Filter Builder: spec + plan written, NOT yet implemented (handoff).** Brainstormed + specced + planned, awaiting a fresh implementation session. Decisions: raw-expression leaves (reuse `FormulaInput`) in nested And/Or/Not groups; **both** scopes (global + per-view) via a scope toggle; modal dialog from a new toolbar Filters button. Architecture: a pure mutable `FilterSpec` tree + `fromFilter`/`toFilter` converters bridge the immutable `FilterNode` backend; `FilterBuilderWidget` (recursive) + `FilterBuilderDialog` + `BasesView` wiring. **Next action:** execute [`plans/2026-05-27-cluster-d-filter-builder.md`](superpowers/plans/archive/2026-05-27-cluster-d-filter-builder.md) (5 TDD tasks) via subagent-driven-development; spec [`specs/2026-05-27-cluster-d-filter-builder-design.md`](superpowers/specs/archive/2026-05-27-cluster-d-filter-builder-design.md). No code written yet.
- **2026-05-27 — Cluster D (Formula Editor) shipped.** Subagent-driven TDD, 9 commits (`f405ed5..368d561a`): a reusable `FormulaInput` (live parse validation + flat token autocomplete), `FormulaEditDialog`, an extended `PropertiesMenuPanel` (Add-formula + per-formula edit/delete + per-row summary picker, signals-only), `BasesView` wiring that owns the `BasesQuery` mutation via `FormulaOps` through `requestSave()`, plus the `SummaryContext` backend gap-fill so custom summary formulas evaluate. Users can now author named + summary formulas in-UI instead of hand-editing `.base` YAML; autocomplete popup + dialogs **pending user eyeball**. Deferred: type-aware member completion, syntax highlighting, filter builder, reference-rewrite on rename. Closeout: decisions-archive.md.
- **2026-05-27 — Cluster D.4c (Bases undo/redo for value edits) shipped.** Subagent-driven TDD, 4 feature + 2 polish commits. `CmdSetFrontMatter` does all I/O inside one `processFrontMatter` mutator → race-free drift check (skip+notify+neutralize on external change); `BasesTreeModel`/`PropertiesDrawer` emit `frontMatterEditRequested` → `BasesView` per-view `QUndoStack` chokepoint; `MainWindow` Undo/Redo routes to the active `BasesView` via a new `activeBasesView()` helper. New `tst_bases_commands` (6) + a model-emit/no-write test; 20/20 bases green, clean build, offscreen launch clean. **Real Ctrl+Z + drift banner pending user eyeball** (joins the D.2–D.4b backlog). Deferred: "+New"/rename/delete/view-config undo, coalescing, cross-view undo. Closeout: decisions-archive.md.
- **2026-05-26 — Cluster D.4b (Bases export/copy + +New entry) shipped.** Subagent-driven TDD, 6 commits. Two pure widget-free helpers under full TDD: `TableExporter` (flat result → CSV/TSV/Markdown/HTML/`obsidian/table`, `tst_table_exporter`) and `NewItemSeed` (AND-context equality constraints from the filter tree, merged over template frontmatter, `tst_new_item_seed`) + a public `Formula::ast()` accessor. `BasesView` wiring: a Results-menu `QToolButton` (Copy table → 4-MIME `QMimeData`; Export CSV… → `QFileDialog`+`QSaveFile`) and a "+New" button (seeded `createMarkdownNote` → `processFrontMatter` → open-in-tab → rename, reusing D.4a host callbacks); `resolveTemplateProps` skips non-scalar template values to avoid lossy seeds. 21/21 bases tests green; clean build; offscreen launch clean. **Widget paths (clipboard, file dialog, +New flow) pending user eyeball** — joins the D.2/D.3/D.4a verification backlog. Deferred: D.4c undo, formula editor, filter builder, D.5. Closeout: decisions-archive.md.
- **2026-05-26 — Cluster D.4a (Bases cell interactivity) shipped.** Subagent-free TDD, 5 feature commits + close-out: a pure widget-free `CellHitTest` helper owns every interactive sub-region's geometry (`tst_bases_cell_hittest`, 5 slots); `BasesCellDelegate::editorEvent` hit-tests clicks and routes them — checkbox toggles via the model's `setData`→`processFrontMatter` path, link/tag/url emit signals; `paint()` draws into the same rect helpers so targets never drift; the `QCheckBox` editor is gone. `BasesTreeModel` exports entry rows as `[[wikilink]]` drag mime + `ItemIsDragEnabled` (`tst_bases_tree_model` extended). `BasesView` connects the signals to leaf navigation (Ctrl/Cmd/middle-click → new tab), tag search, `QDesktopServices` URL open, and a right-click file context menu (Open / new tab / copy wikilink / rename / delete) + drag enable. `MainWindow` provides the host callbacks; tag search reuses the search panel via a new `Q_INVOKABLE SearchView::setQuery` invoked across the plugin `.so` boundary by name. 19/19 bases tests green; clean build; launch smoke clean. **Interactive flows pending user eyeball** (offscreen Qt can't drive clicks). Hover-preview explicitly deferred on the Markoff read-only-Live dependency. Closeout: decisions-archive.md.
- **2026-05-25 — Cluster D.3 (Bases toolbar menus + properties drawer) shipped.** Subagent-driven TDD, 9 commits (`a1b3d7e..cd20634`): pure `ViewConfigOps` mutators (`tst_view_config_ops`, 18 slots); three `Qt::Popup` toolbar panels — Properties (visibility/reorder), Sort+group (sort stack + group-by), Views (rename/dup/delete/set-default) — wired into `BasesView` via `QToolButton`s; a `PropertiesDrawer` in a `QSplitter` editing the selected row's frontmatter via `processFrontMatter`; delegate role-constant cleanup + `BasesTreeModel::entryForIndex`. 17/17 bases tests green; final holistic Qt review READY; popup re-open debounce + sync-write doc applied. **Visual rendering of menus/drawer pending user eyeball.** Formula editor + filter builder + undo + export + drag/hover/context-menu → D.4. Closeout: decisions-archive.md.
- **2026-05-25 — Cluster D.2 (Bases read-side rendering) shipped.** Subagent-driven TDD, 9 commits (`a46b877..8c078f4`): pure `cycleHeaderSort` helper; `BasesTreeModel` (2-level `QAbstractItemModel`, grouped/flat) replacing the flat `BasesTableModel`; `BasesView` on `QTreeView` with collapsible group headings (label/count/summary cells); multi-key header sort (Shift = add key) persisting via the existing `requestSave()`; `BasesHeaderView` priority indicators; Icon/Image/HTML rich cells (Markdown deferred — frozen on read-only-Live). 16/16 bases tests green; every batch passed spec + Qt-quality review; final holistic review READY. **Visual rendering unverified — pending user eyeball of a grouped `.base`.** Baseline re-confirmed against the actual Obsidian source (`/home/clinton/bin/ObsidianRAW/...`). Spec amended mid-plan: header-sort persists (matches Obsidian + existing reorder/view-switch), not session-local. Closeout: decisions-archive.md. Next D: D.3 (toolbar menus + inline editing).
- **2026-05-25 — Cluster D decomposed; D.1 (Bases backend correctness) shipped.** Cluster D split into sub-projects D.1–D.5. D.1 landed via subagent-driven TDD (8 commits, `85034d6..f5161b0`): a `VaultResolver` seam on `EvalContext` (concrete `BasesVaultResolver` owned per-recompute by `QueryController`), `file()`/`LinkValue.asFile`/`linksTo` now resolve against the vault, one-directional `TagValue::tagMatches` + tag-aware `ListValue::includes`, `sort()` null-last pinned. All 13 bases tests green; every task passed spec + Qt-quality review. `unrecognizedData` non-scalar preservation deferred to the punch list. Next D sub-project: D.2 (read-side rendering). Closeout: decisions-archive.md.
- **2026-05-25 — Foundation port reconciliation; Markoff merged to master.** Markoff's QML/D2 rebuild (`exploration/new-foundation`) merged to Markoff `master` (tag `v0.7.0-freeze`, tip `1e0f332`); old v0.6.x tree preserved at `v0.6.x-final`. The rewrite retired the QGraphicsView / `QTextDocument` / `ObjectReplacementCharacter` substrate, **obsoleting clusters G (inline-ORC coherence) and H (block-substitution widgets)** and **likely J** (qutepart Source fork — superseded by `Markoff::Source::Editor`); **E** (editor plugin API) needs re-scope against the D2 block model; **I** is partly absorbed into the port's editor-action wiring; **D and F are unaffected** and remain the live Corbomite-native cluster work. Three dead old-editor branches archived as `archive/markoff-{fold-v2,reading-split,source-split}` tags. Next: re-pin submodule to `v0.7.0-freeze`, then merge `port/foundation-exploration` → Corbomite `master` (Markoff-first ordering satisfied). Handoffs at [`docs/handoff/`](handoff/) (ours + Markoff's `to-corbomite-merge-complete` reply). Full closeout: decisions-archive.md.

Older closeouts (2026-04-26 reset → 2026-04-28) and all full closeout paragraphs: [`decisions-archive.md`](decisions-archive.md).

## Open questions

- **Confirm J's obsolescence** before formal closeout: does `Markoff::Source::Editor` fully cover the qutepart-fork intent (visual-line scroll, fold serialization, find/replace, markdown awareness)? (Plan file archived 2026-06-10 with this caveat noted.)
- **Eyeball-verification backlog.** Eight UI work-packages shipped offscreen-only and have never been seen by a human: D.2 grouped rendering, D.3 panels/drawer, D.4a cell clicks, D.4b export/+New, D.4c undo + drift banner, formula editor popups, filter builder dialog, properties panel. One consolidated session in front of the running app would clear all eight. (Completion popup **eyeball DONE 2026-06-11** — all five modes confirmed; surfaced + fixed two popup-sizing bugs, `62e63a24`.)

## Last touched

2026-08-20 — **Cluster N CLOSED on `feature/rich-clipboard` (N5 eyeball passed).** Multi-flavor copy, Copy as, HTML→MD paste, paste-as-plain, Reading-faithful copy all user-confirmed; Quote→LibreOffice Block Quote fixed (`bafa3095`). Merge pending (master mid-M4). Image paste/drop still out of scope.


2026-08-19 — **Cluster M Phase M3 (edge authoring) CLOSED — live-verified.** Five subagent-driven TDD tasks (`bfec72c8`): M3.1 Obsidian-exact bezier path/arrowhead (`CanvasBezierStrategy`/`CanvasArrowTerminus`, replacing Graffodil's stock curve); M3.2 drag-from-anchor edge creation via Graffodil's `CreateEdgeTool`; M3.3 drop-on-empty create-and-connect popup (compound `CmdAddCard`+`CmdAddEdge`); M3.4 endpoint reconnect — required a new `CanvasEdgeGestureTool` dispatcher because Graffodil's `CompositeTool::addAnchorRoute` always wins routing priority via unconditional prepend, so a plain reconnect route could never be tried first; also fixed `CanvasScene::onEdgeChanged` to rebuild the edge item (not just its data) when `fromNode`/`toNode` change, since `GraphEdgeItem` has no node-identity setter; M3.5 Direction submenu (Nondirectional/Unidirectional/Bidirectional, new `CmdSetEdgeEnds`) plus made "Reverse Direction" (menu + R key) actually undoable, which it previously wasn't. 155/155 offscreen throughout. **Live eyeball found + fixed a real segfault** (`d93f10e2`): right-click an edge → Edit Label → type → Enter crashed, because `QLineEdit::returnPressed` fires synchronously from inside the line edit's own `keyPressEvent`, and the connected handler did `delete proxy` on the `QGraphicsProxyWidget` wrapping that same line edit — use-after-free once the handler returned into the now-freed call frame. The group-label editor had the identical latent bug (same `returnPressed`-connected pattern), fixed alongside it even though not yet tripped live. Both now `removeItem()` synchronously (safe — just detaches from the scene) and `deleteLater()` instead of a raw `delete`. User-confirmed fixed; all other edge-authoring flows (draw, label, reconnect, redirect, delete) confirmed working live. **Next: Phase M4** (move/snap/selection polish — grid/object snapping, modifier polish, arrow-key nudge, edge auto-pan).

2026-08-19 — **Cluster M Phase M2 closed: node-creation flows live-verified.** All six creation flows (double-click, context-menu file picker, drag-drop, clipboard, Alt-drag duplicate, new-canvas command) landed and were live-confirmed working; three bugs found during the pass are fixed (a `~CanvasScene` heap-corruption double-delete, a file-picker path leak, an Alt-drag zero-movement duplicate). Two gaps punch-listed rather than fixed in-phase: file picker isn't `Vault`-backed, canvas has no theme/dark-mode awareness. Full detail: decisions-archive.md. **Next: Phase M3** (edge authoring).

2026-08-19 — **Cluster M Phase M1 closed: `libs/canvas` rebased onto Graffodil::Core, live-verified.** Subagent executed M1.0–M1.6 (verification pass, `CanvasNodeItem`/`CanvasEdgeItem` classes, scene rebase onto `Graffodil::GraphScene`, tool assembly via a bespoke `CompositeTool` — `DefaultGraphTool` couldn't be used as-is, see spec §6a divergence note — undo wiring, and 19 test slots incl. 6 new named tests) across two commits (`00d89904`, `6f476273`), 313/313 offscreen. Live eyeball (M1.7) then found and fixed two bugs: app-wide Ctrl+Z/Redo never checked for an active canvas tab (`MainWindow::activeCanvasView()`, `fb672574`), and PNG/SVG export rendered a black background instead of matching the on-screen white (unset `QGraphicsScene::backgroundBrush()`, `2e06b966`). Both user-confirmed fixed; select/move/resize (incl. the headline file-card-unselectable bug fix), inline edits, export, and undo/redo all live-verified across every node kind. Canvas's total lack of dark-mode/theme awareness punch-listed as a follow-up, deliberately not fixed inside M1's frozen scope. **Next: Phase M2** (node creation flows — double-click-empty, context-menu file picker, drag-drop, clipboard, Alt-drag duplicate, new-canvas command).

2026-08-19 — **Cluster M documentation hardened for hand-off: M1 migration contract spec + executor-grade plan.** User redirected from implementation to exacting documentation so weaker agents can execute the cluster. Both API surfaces (all Graffodil v0.2.3 core headers; all of `libs/canvas`) were read in full and distilled into [`superpowers/specs/2026-08-19-cluster-m1-graffodil-rebase-design.md`](superpowers/specs/2026-08-19-cluster-m1-graffodil-rebase-design.md) — complete class-by-class mapping (incl. the two non-obvious calls: canvas groups must NOT map onto Graffodil's decorative `GroupItem`; edge anchor ids are exactly the `sideToString()` strings), signal→undo-command table, resize stays a consumer tool, file disposition table, named regression tests, 4 pre-code verification reads (V1–V4), and the only 3 intended behavior changes. The cluster plan's phases M1–M6 are now checkbox task lists with named tests; Appendix A pins every normative Obsidian constant (sizes, `clamp(dist/2,70,150)`, 20/40/80/160 grid, 15/scale tolerance, log2 zoom clamp, z=−area, …); Appendix B is the standing-rules/traps list (tools-never-mutate, anchor-id contract, `dynamic_cast` ban, eyeball gate, state tiering). **Next: dispatch Phase M1 starting at task M1.0** (verification reads), any capable agent.

2026-08-19 — **Cluster M Phase M0 closed: Graffodil consumption unblocked and landed.** Graffodil replied to the adoption handoff same day (reply at `~/dev/Graffodil/docs/handoff/2026-08-19-reply-to-corbomite-canvas-adoption.md`): submodule consumption blessed without waiting on 6d (Graffodil's `origin` is GitHub — no mirror lag), pin `v0.2.3`, and the M3 edge-gesture gap was fixed upstream (`CreateEdgeTool` press-drag-release + `setDragOnly(true)` + new `edgeDroppedOnEmpty` signal). Submodule added at `libs/graffodil` (v0.2.3, `dd7667de`), wired via `add_subdirectory`, all six `libgraffodil-*.a` build in the dev preset, **313/313** offscreen. **Next: Phase M1** — feature-frozen rebase of `libs/canvas` onto `Graffodil::Core` (fixes the file-cards-unselectable bug structurally). Detail: plan file M0 resolution block.

2026-08-19 — **Cluster M opened: canvas authoring parity, Graffodil-rebase-first.** Full gap survey of `.canvas` authoring vs the audit's feature catalogue (`obsidian-audit/domains/canvas.md` §9): round-trip fidelity is solid but authoring is nearly absent — file cards aren't even selectable/movable (`SelectMoveTool` casts only Text/Group items), `CreateEdgeTool` is dead code, no creation flows/snapping/reconnect/viewport-persistence/link-indexing. User chose rebasing `libs/canvas` onto Graffodil::Core (now v0.2.2 — phases 1–5 + edge labels + editor affordances complete) over extending the hand-rolled scene. Plan: [`superpowers/plans/2026-08-19-cluster-m-canvas-authoring-parity.md`](superpowers/plans/2026-08-19-cluster-m-canvas-authoring-parity.md). **Next: Phase M0** — Graffodil consumption gate (6d install/export or submodule sign-off, handoff doc to Graffodil) before any Corbomite-side code.

2026-08-18 — **Cluster K Phase 5 closed: Canvas-only LivePreview; QML leaf retired from Corbomite.** Skipped Phase 4 soft-default per user sign-off; deleted `Markoff::Live` construction, `markoff_live`/`markoff_liveplugin` link lines, Qt Quick/QuickWidgets/Qml from Corbomite's `find_package`, and the `CanvasLivePreview` settings toggle. Submodule gained `MARKOFF_BUILD_LIVE` (Corbomite forces OFF) plus a canvas retire-on-destroy fix (`View::setDocument(nullptr)` on document destroy — previously only the MarkdownView base was cleared, leaving a UAF on resize). Tests retargeted to `linkService()` / `canvasEditor()`. **308/308** offscreen; binary no longer NEEDs `libQt6QuickWidgets` (Quick/Qml may still appear via KDDockWidgets). **Next:** resume Cluster L L5 soak, or packaging now that Canvas-only is landed. Full detail: `decisions-archive.md`.

2026-08-18 — **Cluster L Phase L5 (soak dogfood) in progress: found + fixed a P0 Obsidian-round-trip nested-split-scrambling bug.** User dogfooded a real Obsidian-authored 3-pane vault; Corbomite scrambled every pane but the anchor into the wrong quadrant on open. Root cause was two compounding bugs in `WorkspaceSerializer.cpp`: Obsidian's split-`direction` convention (divider-line orientation, not stacking axis) was mapped backwards, and `materializeSplit`'s nested-split anchor-threading recursed into a child's internal structure before its outer siblings were placed, carving up the wrong KDDW frame. Rewrote materialization as a two-phase seed-then-fill algorithm; took two live-test round trips (first attempt fixed only the direction inversion, still produced a different wrong layout) before the user confirmed a match with Obsidian's own rendering. Added `fixture16_nestedSplitFirstChild_matchesObsidianGeometry` — a geometry/adjacency-asserting regression test, since the existing nested-split test (`fixture03`) only checked JSON shape and passed throughout the bug's life. 311/312 offscreen (same pre-existing flake). **Next: continue Phase L5 soak** — drag between groups, split, popout, close-undo, vault switch, then closeout. Full detail: `decisions-archive.md`, punch-list `[cluster-l]`.

2026-08-18 — **Cluster L Phase L4 closed: live dogfood pass confirms D1/D4.** User confirmed, against the running app, that D1 (hidden redundant title bar, middle-click tab close) and D4 (sidebar-width restore across restart) both work as intended — clearing the standing gate noted below (offscreen-green alone wasn't sufficient for keyboard/focus-affecting changes). Phase L4 is done. **Next: Phase L5** (soak & closeout dogfood — drag between groups, split, popout, close-undo, vault switch, Obsidian workspace.json round-trip). Full detail: `decisions-archive.md`.

2026-08-17 — **Cluster L Phase L4 code landed: native UX polish — NEEDS LIVE VERIFICATION before closing.** Back/forward (Ctrl+Alt+←/→, mouse buttons 4/5, real enablement state), tab commands (Ctrl+1..9 jump, pin-tab, move-to-new-window, advisory-only toggle-stacked), and sidebar-width persistence (Phase L2 had built the storage but never wired real values through) are all built and offscreen-green. **D1 (redundant title-bar removal, middle-click tab close) and D4 (width restore) have NOT been seen rendered live** — this is the standing gate (project memory: an offscreen-green keyboard-focus fix was previously found broken live) before the phase can be called done. 311/312 offscreen, no regressions. Full detail: `decisions-archive.md`.

2026-08-17 — **Cluster L Phase L3 landed: cruft removal.** Deleted dead Obsidian-shape shells (`WorkspaceRoot`/`WorkspaceContainer`/`WorkspaceSidedock`, the nullptr-returning `leftSplit()`/`rightSplit()` — kept `WorkspaceFloating`/`WorkspaceWindow`, which have real popout callers); consolidated duplicated `ensureKddwInit`; fixed router O(chain×leaves) focus-handler perf; debounced `Workspace::resize()`'s event mirror (`activeLeafChanged` deliberately left synchronous — real sync consumers exist). **C6** (tab-group cache slimming) and **`eState.scroll`** re-anchoring were deliberately **not** done — both judged out of proportion for this phase (see decisions-archive for why). 311/312 offscreen, same single pre-existing flake, no regressions. **Next: Phase L4** (native UX polish — needs live eyeball per user's standing feedback on keyboard/focus changes) or **Phase L5** (soak & closeout dogfood). Full detail: `decisions-archive.md`.

2026-08-17 — **Cluster L Phase L2 landed: one writer, full fidelity.** Implements the L0 tier doctrine in code: `Workspace::writeWorkspaceJson` promoted to production (fixes B1's floating/lastOpenFiles drop), `_corbomite`/`left-ribbon` denylisted on load (no migration needed — zero installed users), `SessionManager` split into tier-2 (`.obsidian/corbomite/state.json`) + tier-3 (app-data `session.json`) writers, B4 sidecar-map growth bounded, B3 id-assignment fixed (dimension round-trip punch-listed as a known gap — writer rebuilds fresh from KDDW's LayoutSaver each save). Golden fixture + `tst_workspace_session.cpp` cover the fidelity contract. 310/313 offscreen, 2 unrelated pre-existing/flake failures. Full detail: `decisions-archive.md`.

2026-08-17 — **Cluster L opened: workspace/KDDW stabilization & nativization.** Full re-evaluation of the tab/dock layer against the 2026-04-26 workspace audit + current code. Verdict: architecture sound (KDDW substrate + Obsidian-shaped facade is right; the worst audit findings were already fixed), but three live disease clusters: teardown fragility (crash engine — `Workspace::deserialize` bare `qDeleteAll` with no `releaseDockWidget`, four inconsistent destruction disciplines), persistence split-brain (production save drops `floating`+`lastOpenFiles`; `_corbomite` keys in the Obsidian-shared workspace.json; empty node ids / no `dimension`), Obsidian-literalism residue (nullptr shells, dead writers, undebounced event mirrors). Plan: [`superpowers/plans/2026-08-17-cluster-l-workspace-stabilization.md`](superpowers/plans/2026-08-17-cluster-l-workspace-stabilization.md). **Next: Phase L0 (compat-boundary spec, needs user sign-off) then L1 (teardown unification) — L1 before K Phase 4.**

2026-08-17 — **Cluster K Phase 3 closed: 10/14 dogfood findings fixed and live-user-confirmed.** Live re-test of the 5 fixed via subagent TDD (wikilink nav, double/triple-click, Ctrl+Scroll zoom, F3 scroll, image/embed placeholders) confirmed 4 as-is and surfaced a deeper bug in #5 (wikilink click always opened a new tab — a pre-existing Corbomite-side navigation gap, not canvas-specific, fixed in `44d9eb66`); also fixed Ctrl+=/Ctrl+-/Ctrl+0 keyboard zoom (`03f7ceb1`). Remaining 4 findings are low-severity polish, not blockers. **Next: Phase 4 (dogfood-as-default) — recommended over pausing for more Markoff engine work; see the G2-closing handoff doc.** Closeout: `decisions-archive.md`.

2026-08-17 — **Cluster K Phase 2/3: canvas dogfood pass, 5/14 findings fixed.** First hands-on test of the `Markoff::Canvas::EditorWidget` LivePreview toggle surfaced 14 bugs (filed to punch-list `[cluster-k]`); 5 were fixed directly in the `libs/markoff-family` submodule and user-confirmed against the running app (empty-note lockup, `$$` math lockup, undo phantom-link, doubled list marker, Tab focus leak) — full detail + commit shas in punch-list.md and `decisions-archive.md`. **Next step:** pick up the remaining 9 `[cluster-k]` findings from the punch list (wikilink click/middle-click nav is next by severity), or move to Phase 4 (dogfood-as-default) once satisfied with Phase 3 coverage — user's call. Closeout: [`decisions-archive.md`](decisions-archive.md).

2026-06-12 — **Phase 2 hover preview re-lit + eyeball-confirmed.** Hovering a `[[wikilink]]` shows a styled preview of the target note: `HoverPopover::renderTarget` re-pointed from the retired `Markoff::Reading` stack onto the shipped `StyledRenderEngine` + a `VaultResourceProvider` resolver, rendered into a `QTextBrowser`; the trigger rides the shared `LinkService` (`linkHovered`/`linkHoverLeft`), so one pair of connects covers both leaves — no upstream Markoff work or re-pin. **Runtime behaviour confirmed: Reading = plain hover; Live = Ctrl-then-hover** (the Live `LiveView.qml` gates hover emission on Ctrl — Obsidian-faithful; kept by user decision, NOT a bug). Subpath (`#heading`/`#^block`) slicing deferred; **270/270** offscreen (excl. benchmark). **Phase 2 remaining:** placebo removal (wire-or-hide Insert Table/Callout + hamburger Find/Replace), checkbox-in-Reading (upstream). Closeout: [`decisions-archive.md`](decisions-archive.md).

2026-06-11 — **Phase 2 status-bar word count + template-at-cursor shipped.** Two self-contained road-to-dogfood Phase 2 items: (1) status-bar word count now tracks the live document — `NoteEditorWidget` seeds the count on attach and re-emits on `NoteDocument::textChanged` (was permanently "Words: 0"); Ln/Col already landed in Phase 1, so **status-bar honesty is complete** (`fb47120e`). (2) Template insertion lands at the caret via one undo-integrated D2 `applyFlatEdit` (was append-at-end), with two pure unit-tested `LineResolve` bridges (`082589ae`). Corbomite **267/267** offscreen (excl. benchmark). **Checkbox-toggle-in-Reading was scoped and deferred** — it needs an upstream markoff-styled click handler (alongside `LinkInteraction`) + a re-pin, not a Corbomite-only edit (see punch list). **Phase 2 remaining:** hover preview (`HoverPopover` → `StyledRenderEngine`), placebo removal (wire-or-hide Insert Table/Callout + hamburger Find/Replace), checkbox-in-Reading (upstream). Closeout: [`decisions-archive.md`](decisions-archive.md).

2026-06-11 — **Completion revival shipped (A1–A3) + popup polish.** The upstream `Markoff::MarkdownView::caretRect()` contract addition (submodule re-pinned `af91a936` → `e2fefbd2`) feeds a new leaf-agnostic `CompletionController`, delivering `[[` names + aliases + `#`-headings + `^`-existing-blocks and `#` tags across all editable leaves; spec [`superpowers/specs/2026-06-11-completion-revival-design.md`](superpowers/specs/2026-06-11-completion-revival-design.md), plan [`superpowers/plans/2026-06-11-completion-revival.md`](superpowers/plans/2026-06-11-completion-revival.md). A live eyeball pass confirmed all five modes and fixed two `CompletionPopup` sizing bugs (one-row height; over-elided detail column) — `62e63a24`, closing the P5 polish deferral. Suites: markoff 270/273 (3 known queue-#10 reds), Corbomite **265/265**. Remaining `^id`-creation / code-block-suppression deferrals punch-listed under the 2026-06-11 header; closeout in [`decisions-archive.md`](decisions-archive.md). **Next focus: road-to-dogfood Phase 2 remaining items** — status-bar word count, hover preview (re-target `HoverPopover` → `StyledRenderEngine`), placebo removal (wire-or-hide Insert Table/Callout + hamburger Find/Replace), template-at-cursor, checkbox-toggle-in-Reading.

Build/test: `cmake --build --preset dev -j 10`. Tests: `cd build-dev && QT_QPA_PLATFORM=offscreen ctest -E benchmark --output-on-failure -j 10` (offscreen is mandatory). Work on `master` (no feature branches). **Never `git add -A`** — `testvaults/` is deliberately dirty; stage by path. Pre-pull.
