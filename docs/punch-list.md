# Corbomite punch list

> Audit-derived task tracker. Source: [audit-2026-04-26/](audit-2026-04-26/).
> Each item is a checkbox + severity tag + domain tag + headline + path:line + link to the audit sub-report section.
> Mark items `[x]` when committed; do not delete (history of fixes lives here).
> When picking up new work, scan top-down: P0 first.
> Strategic clusters (multi-phase coordinated work) live separately under `superpowers/plans/`.

**Last refreshed:** 2026-06-10 (full code-verification re-triage — every open item's verdict checked against the current tree; 3 closed as fixed/overtaken, ④ bucket unblocked → re-targetable, 1 re-rank P4→P1, 21 new verified items added tagged `[audit-2026-06-10]`). Prior: 2026-05-29 architecture audit (3 save-path integrity items at top of P0); 2026-05-26 full-suite triage during D.4b + save-flag fix; 2026-05-25 re-triage against the QML/D2 foundation; 2026-04-28 shakedown + bug-bash round 2.
**Item count:** 129 top-level checkboxes (133 with sub-items). Prior header claimed 82; actual pre-refresh count was 108 (112 with sub-items) — corrected, plus 21 new `[audit-2026-06-10]` items.

---

## Re-triage 2026-05-25 — against the QML/D2 foundation

> The foundation port landed on `master` (see [`PROJECT-STATE.md`](PROJECT-STATE.md)). The old four-leaf editor — QGraphicsView scene, `QTextDocument`, `Markoff::Editor`, `Markoff::Reading::ReadingView`, `buildScene` — is **retired**. That changes the status of many open items in the severity sections below.
>
> **This block is authoritative for picking work.** The severity sections below it are the historical record — do not pick top-down from them without checking the disposition here first.

**① RESOLVED — stale-open duplicates already closed in bug-bash round 2 (2026-04-28).** appearance.json write (T-16), layout split-child swap (T-21), tree-collapse-on-rename (T-19), silent create-collision (T-20). The "Open (deferred)" copies predate their closures lower in the file; now marked `[x]`.

**② ACTIONABLE NOW — substrate-independent (the real pick-from pool).**
- P3: plugin-side `Events` mixin (`vault.on(...)`).
- P4: `appId` per-vault QtKeychain scoping; `quit` event + async-cleanup collector.
- P5: kitchen-sink reference plugin; PDF *view* shell (the `![[file.pdf]]` *embed* is gated — see ⑤); About page.
- P6: document `apiLevel: 1` ABI; document `X-Corbomite-Dock*` manifest keys.
- Shakedown: move Corbomite artifacts out of `.obsidian/`; PropertiesView editing UX; Bases editing UX (→ Cluster D); bookmark heading/block-anchor affordances; periodic crash-recovery snapshot.

**③ RE-TARGET — our work, but the cited reference is retired (re-aim at the QML editor / new Live API).**
- `setCursorLine` (was Reading-mode; wire to Live scroll / `setCursorLine`).
- HoverPopover anchor-to-mouse (needs a `hoveredLinkRect()` equivalent on the *new* Live editor API, not the retired `Markoff::Editor`).
- Paste-HTML→Markdown and drag-drop-image hooks (conversion logic survives; the interception point moved to the QML editor — and Markoff already routes drag-drop through the document layer, so re-verify first).

**④ RE-TARGETABLE — was frozen on read-only-Live; unblocked by StyledRenderEngine (2026-05-30); each item needs a re-aim at the styled path before work.** *(History: the original blocker — the read-only-Live renderer steer sent to Markoff 2026-05-25 — was retired twice over: reading mode shipped as a read-only **Styled** leaf, `775fa54e` 2026-05-29, and `StyledRenderEngine` wrapping `Markoff::Styled::DocumentRenderer` landed 2026-05-30, `e7a40ae2`.)*
- HoverPopover preview rendering (currently stubbed dark) + child-popover chains.
- `MarkdownRenderer::render` — **REGRESSED**: it wrapped `Markoff::Reading::ReadingView` and is now `#if 0` with link-only stubs (P3 item is checked-closed below; the feature is not live).
- `MarkdownRenderChild` framework.
- Wire `PostProcessorRegistry` / `CodeBlockProcessorRegistry` / `DecorationProvider` into the render path — all three targeted `ReadingView`/`buildScene`; re-target to the QML render path once it exposes a hook.
- Page Preview plugin (depends on HoverPopover).
- Checkbox-click-to-toggle (a read-mode interaction; lands with read-only Live).

**⑤ FROZEN — blocked on a Markoff E-phase:**
- Callouts → **E3**. Embeds incl. `![[file.pdf]]` → **E3**. Footnotes → re-verify; Live footnote rendering likely needs upstream support.
- Mermaid dark-theme → **E5** (Mermaid is a no-op until then).

**Bottom line:** pick from bucket ② plus the bucket-③ re-targets. Everything rendering-/popover-/Reading-shaped (④) is now unblocked by `StyledRenderEngine` (2026-05-30) but each item needs a re-aim at the styled path before work; callout/mermaid/embed (⑤) wait on Markoff E3/E5. Strategic tracks D (Bases UI) and F (internal plugins) are both substrate-independent and remain live.

- [x] **[RESOLVED 2026-05-29 — `ab242ab2`.]** [editor][rendering] **Reading-mode "dead-end" fork (audit-2026-05-29) closed by a read-only `Markoff::Styled::Editor` leaf.** Reading mode is no longer read-only Live nor the retired `Markoff::Reading::ReadingView` — it owns a styled QWidget leaf (no QML). The old `Capabilities::Editable=false` steer to Markoff is retired; the nullptr-returning `readingView()` stub + forward-decl are deleted from `NoteEditorWidget`. See [`handoff/2026-05-29-to-markoff-styled-document-renderer.md`](handoff/2026-05-29-to-markoff-styled-document-renderer.md).

---

## Added 2026-06-10 (dogfood loop — first-run console noise)

> Surfaced on the first real run of Corbomite (Phase 6 dogfood loop). The first-run SIGSEGV (vault-teardown UAF / welcome double-open) is **fixed** (`ddb1701d` + Markoff `af91a936`; see decisions-archive). The plugin Id-in-metadata warnings are **fixed** below. The rest are cosmetic console noise — no functional loss observed — filed so they're tracked, not re-triaged as new.

- [x] **[RESOLVED 2026-06-10.]** [plugins][P5] **`kf.coreaddons: plugin explicitly states an Id in the embedded metadata` (`corbomite-template.so`, `note-stats.so`).** The example plugins' `metadata.json.in` set `KPlugin.Id` (`example.template` / `example.note-stats`) ≠ the filename-derived id (`corbomite-template` / `note-stats`); KCoreAddons keys enable/disable state off the derived id, so the embedded Id was dead-and-divergent. Removed the `Id` field from both `examples/*/metadata.json.in` (derive from filename). Verified: 2 → 0 at startup.
- [ ] [ui][xmlgui][P5] **Repeated `kf.xmlgui: Index 18 is not within range (0 - 16)` on every startup (~30×).** `ActionList::plug` (`kxmlguifactory_p.cpp:28`) — the Kate-MDI tool-view `<ActionList>` placeholder in the View menu (`src/mdi/CorbomiteMDI.cpp:89`, plugged in `updateActions()` :248) gets a merging index one past the View menu's action count, so the 3 tool-view items (Tool Views / Sidebar Buttons / Focus Toolview) are appended at the end instead of at the placeholder. Near-verbatim Kate-MDI pattern; actions still appear → **cosmetic placement drift**, fires per `updateActions()`. Fix needs KXMLGUI merge-index investigation (compare against upstream Kate's `KateMDI`).
- [ ] [ui][qml][P5] **`qt.qml.typeregistration: Invalid QML element name "Theme"; value type names should begin with a lowercase letter`.** `Markoff::Theme` Q_GADGET is exposed to QML via `QML_FOREIGN` (`ThemeForeign.h`, markoff-live) under the uppercase name `Theme`; QML wants value-type names lowercase. Cosmetic — the `Theme.<Slot>` enum access works. Markoff-side; fixing means renaming the foreign type (risk: delegates reference `Theme.TextDefault`) — defer / steer upstream.
- [ ] [infra][dbus][P6] **`qt.qpa.services: Failed to register with host portal ... App info not found for 'org.corbomite.Corbomite.Dev'`.** The Dev build's desktop file (`org.corbomite.Corbomite.Dev`) isn't installed where the XDG portal can find it, so portal app-id registration fails. Dev-only, no functional impact (file dialogs still work via fallback). Fix: install/symlink the dev `.desktop` for dev runs, or accept as a dev-only diagnostic.

---

## Added 2026-06-11 — completion revival follow-ups

> Deferrals from the completion-revival closeout (A1–A3 shipped; spec [`superpowers/specs/2026-06-11-completion-revival-design.md`](superpowers/specs/2026-06-11-completion-revival-design.md) §14). All three are user-approved deferrals, not regressions — filed so the closeout doesn't lose them.

- [ ] [editor][completion][P3][completion-revival] **`^id` creation on block pick.** Picking a `[[note#^…]]` candidate inserts only *existing* block ids; choosing a block that has no id yet should mint a `^id` on the target block (append + persist) and link to it. Today the A3 picker is existing-ids-only. Spec §14.
- [ ] [editor][completion][P4][completion-revival] **Code-block trigger suppression via `EditorContext`.** `[[`/`#`/`^` triggers fire inside fenced/inline code spans where completion is unwanted; suppress when the caret's `EditorContext` reports a code context. Needs a code-context flag surfaced on the editor context. Spec §14.
- [x] **[RESOLVED 2026-06-11 — `62e63a24`. User eyeball surfaced two real rendering bugs the offscreen tests couldn't: the popup rendered one row tall (multi-candidate sets like Beta vs sub/Beta were invisible; arrow-nav moved an unseen selection) and the detail column was unrendered, then once added over-elided. Fixed: CompletionPopup sizes height to the row count (cap 10, then scrolls) and width to fit display + detail (clamped 240–640px), with rows forced to viewport width so detail right-aligns to a stable column. Regression tests `popupHeight_growsWithRowCount` + `popupWidth_fitsDetailWithoutEliding`.]** [editor][completion][P5][completion-revival] **Popup `detail` column rendering polish.** The completion popup's secondary `detail` column (alias source, heading path, block preview) renders unstyled/cramped; needs layout + elide polish. Cosmetic. Spec §14.

---

## Added 2026-05-25 (post-port / D.1 close-out)

- [x] **[RESOLVED 2026-05-26 — `1be8a577`.]** [bases][parsing][P1] **`.base` `unrecognizedData` truncates non-scalar shapes** — top-level path in `BasesQuery.cpp` dropped nested YAML (the unrecognizedData switch `default: break;` skipped Seq/Map Kinds). Fix: promoted the recursive `yamlToVariant` helper out of `BasesViewConfig.cpp`'s anon namespace to a shared decl in `BasesViewConfig.h`, and use it for top-level unrecognizedData so nested maps/lists round-trip. Emit side (`emitMap`/`emitList`/`emitValue`) already handled nested shapes — no writer change. Test: `tst_bases_yaml_schema::testUnknownTopLevelNestedShapePreserved` (nested map + list survive load→save→load). `BasesViewConfig.cpp` already used `yamlToVariant` correctly. Forward-compat round-trip risk: a future Obsidian `.base` feature that nests YAML would be silently lost on first save. Deferred out of Cluster D.1 (evaluator-only) as a sibling of the Cluster A key-order fix. See [audit-2026-04-26/bases.md](audit-2026-04-26/bases.md) §"Missing — Structural #1/#3".
- [x] **[RESOLVED 2026-05-25 — Markoff shipped the D2-clear fix (`f48525d` + CRDT `local_clear()`); Corbomite re-pinned to `082b063` and updated the 3 tests, all green.]** [editor-markdown][vault][P0] **Content doubling on `setMarkdown()`/content-reset (foundation-port regression).** Replacing a loaded doc's content (`NoteDocument::setMarkdown` → `MarkoffDocument::resetContent` → `buildD2FromBytes`) **appended** new D2 blocks without clearing the old ones, so `serializeForSave()` wrote new+stale content to disk (e.g. `"modified content\n\noriginal content\n"`). Silent content corruption on save; triggered on `setMarkdown`/external-reload/revert-to-saved (not interactive typing). Markoff-owned; steer at [`handoff/2026-05-25-to-markoff-d2-clear-on-reset.md`](handoff/2026-05-25-to-markoff-d2-clear-on-reset.md), their closure reply at `libs/markoff-family/docs/handoff/2026-05-25-*`. Fix: `wipeD2State()` before rebuild on all reset/reload origins. The canonical trailing-`\n` (B1) is accepted-by-design; the 3 integration tests now assert it. Out-of-scope follow-ups Markoff flagged: D2-undo of revert, collab broadcast of resets, tombstone compaction, Origin-enum tidy.
- [ ] [bases][ui-bundle][P3][FOLLOWUP] **D.2 read-side rendering — visual verification pending (user).** D.2 (collapsible group headings + count + summary cells; multi-key header-sort indicators ▲/▼ + priority; Icon/Image/HTML cells) shipped 2026-05-25, code-reviewed + 16/16 bases tests green, but subagents can't see pixels. **User to eyeball a grouped `.base`** (group headings render, chevrons collapse, sort indicators draw on header click/Shift-click, Icon/Image/HTML cells display) and report any visual issues — the structure is sound so fixes should be small. See [`specs/2026-05-25-cluster-d2-bases-read-side-rendering-design.md`](superpowers/specs/archive/2026-05-25-cluster-d2-bases-read-side-rendering-design.md).
- [x] **[RESOLVED 2026-06-10 — Phase 1 Task 1/2: fix implemented upstream as `9a6a6b74` (plan deviation: it had NOT landed at planning time), re-pin `08098ca2`; both oracle slots green.]** [metadata][parsing][P2][HANDED-OFF→MARKOFF] **`tst_metadataparser` embed `.original` field mismatch (foundation-port).** Surfaced in the post-D.1 full-suite run; outside `libs/bases`. The two failing slots are `testParseEmbedVsLink` and `testParseEmbedAsImageNode`; both `QCOMPARE` mismatches on the embed cache shape. **Triaged 2026-06-04 (root cause found):** tree-sitter routes `![[…]]` through its `image` node; Markoff's `TreeSitterParser::extractLinkFromNode` image-path is incomplete vs. its `wiki_link`/`inline_link` siblings — it neither strips the `[…]` brackets from `displayText` nor falls back `target`, so `LinkInfo` returns `target=""`, `displayText="[Image.png]"`. Corbomite's `MetadataParser.cpp:373-377` then builds `original = "|[Image.png]"` (`target + "|" + displayText`), breaking embed resolution. Fix belongs upstream (parser owns the tree-sitter quirk); steer sent to Markoff at [`handoff/2026-06-04-to-markoff-embed-image-node-target.md`](handoff/2026-06-04-to-markoff-embed-image-node-target.md). The two tests are **deliberately left red** as the cross-repo acceptance oracle — they go green on re-pin to the fixed Markoff commit. Not a regression (repros on the prior pin `8db7c5a9`).

---

## Added 2026-05-26 (full-suite triage during D.4b)

> Surfaced by the full `ctest` run while verifying the D.4b leaf-navigation fix. All four pre-date D.4b — confirmed by re-running against the stashed baseline tree (the render trio + metadataparser fail identically without any D.4b/nav changes). Filed here so future full-suite runs don't re-triage them as new regressions.

- [ ] [rendering][editor-markdown][RE-TARGETABLE ④] **`tst_markdownrenderer` + `tst_renderengine` — Markdown→HTML render path stubbed post-port.** `libs/core/src/MarkdownRenderer.cpp` is `#if 0`-disabled from the top (its `TODO(port-foundation-exploration)` header: `Markoff::Reading::ReadingView` was retired, no renderer entry); the `MarkoffRenderEngine` path it backs produces no document/HTML. **Update 2026-06-10:** tests are no longer red — QSKIP-gated in `a6a664d5`; `MarkdownRenderer.cpp` still `#if 0`; re-target candidate is `StyledRenderEngine` (landed `e7a40ae2`, 2026-05-30). Re-aim the renderer + tests at the styled path. No vault-data risk (rendering is read-only display).
- [x] **[RESOLVED 2026-05-26 — watermark gate in `NoteDocument::setModified`.]** [editor-markdown][P2] **`tst_e2e_gui::testSaveShortcut` — document still `isModified()` after `file_save` trigger.** Triaged: **not data loss** — `Vault::saveDocument` persisted the bytes correctly and `setModified(false)` cleared the flag synchronously. Root cause: `MarkoffDocument::scheduleD2Changed()` defers `d2DocumentChanged` via `QTimer::singleShot(0)`; an edit-then-immediate-save (Ctrl+S right after typing) cleared the flag in the same turn, then the *stale* deferred signal fired on the next event loop and `NoteDocument`'s handler blindly re-marked the just-saved doc modified. The same ordering defeated the documented `setModified(false)` intent in `Vault`'s external-reload paths (`Vault.cpp:872-889`). Fix: `NoteDocument` records an edit-sequence watermark (`editSequence()` + `d2EditSequence()`) on `setModified(false)` and gates the `d2DocumentChanged`/`documentReloaded` re-dirty on actual advancement past it. Regression test `tst_notedocument_dirty` (falsifiability-checked); `tst_e2e_gui` now 17/0. (Pre-existing `tst_notedocument.cpp` stays `if(FALSE)`-disabled on retired `resetContent(QString)`/`undoStack`/`MarkdownDelta` APIs — re-enable is separate follow-up.)
- [x] **[RESOLVED 2026-06-11 — dispatch-side clamp, Task 6 of completion revival; production clamp now tested.]** **[RESOLVED 2026-06-10 — fixed in `a6a664d5`: the abort was in the test's own SigilSuggest mock, not production code (production exonerated); mock clamped.]** [editor-markdown][crash][NEEDS-TRIAGE → P3] **`EditorSuggest` aborts on cursor-position past end of line.** `tst_editorsuggest::testTriggerInfoCarriesQuery` SIGABRTs on a QString out-of-range assert (`n <= d.size - pos`, qstring.h:1226) when `EditorSuggestManager::dispatch(cursorPos=5, "@hi!")` is called with `cursorPos` beyond the line length (4) — the trigger/substring path doesn't clamp `cursorPos` to the line length before slicing. The test's own comment expects graceful handling ("cursorPos=5 is past end; clamp test"). Confirmed **pre-existing** (fails with this session's save/nav fixes stashed; the fixes can't touch suggest logic) — it was stale-binary-passing in earlier parallel `-j10` runs and surfaced deterministically after a clean rebuild. Likely fix: clamp `cursorPos`/guard the `.mid()` in `EditorSuggestManager::dispatch` or `SigilSuggest::onTrigger`. Reachability in the live app (stale cursor during rapid edits?) unverified — triage. `tests/core/tst_editorsuggest.cpp:117`.
- [ ] [editor-markdown][test][P5] **Re-enable `tst_notedocument.cpp` (currently `if(FALSE)`).** Disabled in `tests/core/CMakeLists.txt:52-61` because it calls retired MarkoffDocument APIs (`resetContent(QString)` → now `QByteArray`; `undoStack()`/`MarkdownDelta` → now the D2 undo path). Port it to the foundation API and re-enable. The dirty-flag watermark regression (above) is meanwhile covered by the live `tst_notedocument_dirty`.
- [ ] [infra][test][P6] **`tst_benchmark_layout` times out under default `ctest`.** A layout benchmark, not a correctness test — the timeout is environmental (offscreen + machine load), not a product bug. Either bump its per-test `TIMEOUT` property in the test's `CMakeLists.txt` or move it behind a `benchmark`-labelled `ctest` set excluded from the default run, so a green full-suite isn't blocked on benchmark wall-clock.

---

## P0 — Vault-format silent-corruption fixes (FIX FIRST)

### Added 2026-05-29 — save-path integrity (from `audit-2026-05-29-architecture.md`)

- [x] **[FIXED 2026-06-10 — `51d62910`.]** [vault][data-loss][P0] **(verified still open 2026-06-10 — most dangerous open item)** **`Vault::saveDocument` writes via raw `QFile(WriteOnly|Truncate)`, not the atomic adapter path.** `libs/vault/src/Vault.cpp:755` opens `QFile(abs)` with `WriteOnly|Truncate` and `f.write(bytes)` directly, bypassing `m_adapter->writeBinary(...)` (which `FileSystemAdapter` implements as an atomic temp-file-rename). A crash/power-loss mid-write truncates the note — the exact silent-corruption class this section exists for. It also means a mock `DataAdapter` can't intercept the canonical save path (tests can't observe it). Fix: route the save through `m_adapter->writeBinary(abs, bytes, hints)` after the U+FFFC guard, keeping the echo-suppression stamp ordering. Verify against `tst_vault_save_reload`. See [audit-2026-05-29-architecture.md](audit-2026-05-29-architecture.md) §"Data-integrity defects" #1. Fixed: routed through `m_adapter->writeBinary`; test `tst_vault_adapter::saveDocument_routesThroughAdapterWriteBinary`.
- [x] **[FIXED 2026-06-10 — `d9f1c102`+`ddfacc03`.]** [vault][data-loss][P1] **(verified still open 2026-06-10 — second most dangerous open item)** **`Vault::modify()` doesn't reconcile an open `NoteDocument` → silent buffer staleness.** `libs/vault/src/Vault.cpp:213-239` updates `m_readCache` + `TFile::stat` but never touches `m_docs`. Because `modify()` stamps a self-write, the watcher echo is suppressed, so a frontmatter/process write (`FileManager::processFrontMatter` → `Vault::process` → `modify`) to a file that *also* has a live editor `NoteDocument` leaves the in-memory D2 buffer stale vs disk, with no reconciliation path (the `onExternalModified` reload is deliberately suppressed for self-writes). Fix: if `m_docs` holds the path, re-sync the open document (or refuse the optimization for open files). **No test covers this** — add one (a `modify()` on an opened doc asserts the doc's `serializeForSave()` reflects the new bytes). See [audit-2026-05-29-architecture.md](audit-2026-05-29-architecture.md) §"Data-integrity defects" #2. Fixed: `reconcileOpenDocument` — refresh-if-clean / signal-if-dirty via existing `externalReloadConflict`; test `tst_vault_modify_reconcile`.
- [ ] [editor-markdown][data-loss][NEEDS-TRIAGE → OPEN DECISION] **Markdown open→save collapses blank-line runs (working-tree evidence: `Start Here.md` 68→33 lines).** The save path is `doc->markoff()->serializeForSave()` — **Markoff's** D2 serializer (submodule), a known data-loss class already steered via [`libs/markoff-family/docs/handoff/2026-05-21-save-path-data-loss.md`](../libs/markoff-family/docs/handoff/2026-05-21-save-path-data-loss.md). Cross-repo (Markoff-owned), like the `2026-05-25` D2-clear fix. **Triage step:** confirm the dirty `testvaults/starter-vault/PKM LM/*.md` files were Corbomite-written (the AST-round-trip pattern matches Markoff, not Obsidian) before raising with Markoff. Not Corbomite-fixable locally. See [audit-2026-05-29-architecture.md](audit-2026-05-29-architecture.md) §"Adjacent". **Triage verdict (2026-06-10):** confirmed Corbomite/Markoff round-trip-written; intentional Markoff normalization (B1 spec 2026-05-18 §2). Confirmed Obsidian round-trip-fidelity gap. Now an **OPEN DECISION**: steer Markoff toward spacing-preserving save vs accept + document. Triage + handoff doc: [`handoff/2026-06-10-blank-line-collapse-triage.md`](handoff/2026-06-10-blank-line-collapse-triage.md). Bears on release criterion `:147` ("no unexplained diffs") — see also Phase 0 status note in road-to-dogfood.

### Added 2026-06-10 — code-verification re-triage

- [x] **[FIXED 2026-06-10 — `88ad1b46`+`8c9d8c8d`.]** [core][editor-markdown][P0][audit-2026-06-10] **Link clicks dead in every mode — no `LinkService` consumer.** `Markoff::LinkService::linkActivated` is connected nowhere in src/; `NoteEditorWidget::linkActivated` never emitted; `MainWindow.cpp:1120` connects a dead signal. Core PKM loop broken. Quick win: wire `binding()->linkService()` + `m_styledReadingView->setLinkService`. — verified 2026-06-10 Fixed: shared `DefaultLinkService` wired to Live binding + Styled Reading leaf → MainWindow resolves via LinkResolver → navigates; create-on-click parity for missing targets; tests `tst_link_activation`, `tst_mainwindow_link_navigation`.

- [x] [vault][parsing] `processFrontMatter` reorders YAML keys alphabetically on every metadata edit — `libs/vault/src/FileManager.cpp:138-143` — see [parsing.md](audit-2026-04-26/parsing.md) §"Frontmatter round-trip risks (CRITICAL)"
- [x] [vault][settings] Two parallel `.obsidian/*.json` writers diverge on indent (Bookmarks plugin writes 4-space; route `Vault::writeConfigJson` through `VaultConfig::serializeObsidianStyle`) — `libs/vault/src/Vault.cpp` vs `VaultConfig::serializeObsidianStyle` — see [settings.md](audit-2026-04-26/settings.md) §"On-disk schema compatibility matrix (per-file)"
- [x] [bases][parsing] `.base` YAML emitter alphabetises keys (same root cause as frontmatter sort) — `libs/bases/src/BasesQuery.cpp:108-127` — see [bases.md](audit-2026-04-26/bases.md) §"On-disk `.base` format compatibility" *(top-level + per-view canonical order; user-keyed dicts still alphabetise — see follow-up)*
- [x] [core][metadata] `resolveSubpath` block-id case-sensitivity — lowercase both sides — `libs/core/src/LinkUtils.cpp:121-155` — see [core-and-addenda.md](audit-2026-04-26/core-and-addenda.md) §"Notable concerns / suspected bugs"
- [x] [vault] Folder rename loses descendants (recursive walk + per-descendant `renamed` emission missing) — `libs/vault/src/Vault.cpp:337-373` — see [vault.md](audit-2026-04-26/vault.md) §"Notable concerns / suspected bugs"
- [x] [vault] `FileManager::renameFile` rewrites markdown-style links and full-path forms; should use `MetadataCache` snapshot as source-of-truth — `libs/vault/src/FileManager.cpp:148-216` — see [vault.md](audit-2026-04-26/vault.md) §"Notable concerns / suspected bugs"
- [x] [settings] `appearance.json` `theme` value vocabulary mismatch — write `"obsidian"`/`"moonstone"` not `"system"`/`"light"`/`"dark"` — see [settings.md](audit-2026-04-26/settings.md) §"On-disk schema compatibility matrix (per-file)"
- [x] [settings][plugin] Wire `core-plugins.json` and `community-plugins.json` to `PluginManager` so toggle state transfers Corbomite ⇄ Obsidian — see [settings.md](audit-2026-04-26/settings.md) §"On-disk schema compatibility matrix (per-file)" — design: [`docs/superpowers/specs/archive/2026-04-26-plugin-enable-state-cross-app-compromise.md`](superpowers/specs/2026-04-26-plugin-enable-state-cross-app-compromise.md)
- [x] [vault] `Vault::create` doesn't case-insensitively collision-check — see [vault.md](audit-2026-04-26/vault.md) §"Notable concerns / suspected bugs"
- [x] [vault] `CaseSensitivityProbe` is dead code (no callers) — see [vault.md](audit-2026-04-26/vault.md) §"Notable concerns / suspected bugs"
- [x] [parsing] `processFrontMatter` cannot strip an emptied frontmatter block — see [parsing.md](audit-2026-04-26/parsing.md) §"Notable concerns / suspected bugs"
- [x] [metadata] `SQLiteIndex` doesn't write `frontmatterLinks` rows (SQL backlinks miss every link declared in `related: "[[Foo]]"`) — see [metadata.md](audit-2026-04-26/metadata.md) §"Notable concerns / suspected bugs"
- [x] [metadata] `drainOnePath` only re-resolves `cache.links` (embeds + frontmatterLinks stay raw) — see [metadata.md](audit-2026-04-26/metadata.md) §"Notable concerns / suspected bugs"
- [x] [metadata] `LinkResolver` step 4 dot-relative path with extension fails for folders named `2026.04` — see [metadata.md](audit-2026-04-26/metadata.md) §"Notable concerns / suspected bugs"
- [x] [metadata][parsing] `collectFrontmatterLinks` grabs only the first wikilink per string leaf — see [metadata.md](audit-2026-04-26/metadata.md) §"Notable concerns / suspected bugs"

### Added 2026-05-28 — `.canvas` interop bundle (from `domains/canvas.md` §11.1)

> Four small fixes in `libs/canvas/src/CanvasDocument.cpp` (+ `CanvasTypes.h`). Bundle into one TDD session against a golden Obsidian-written `.canvas` round-trip test. The first item is actual data loss; the others are diff-churn / interop polish landed alongside it for free.

- [x] [canvas][interop] **`.canvas` drops unknown JSON fields on save (DATA LOSS).** Fixed 2026-05-28 (`a8b63603`): `QJsonObject extraData` on `CanvasNode`/`CanvasEdge` + `m_extraTopLevel` on `CanvasDocument`; load captures everything outside the known-keys set, save spreads it back. `tst_canvasdocument` covers node + edge + top-level passthrough.
- [x] [canvas][interop] **`.canvas` writes default values Obsidian omits.** Fixed 2026-05-28 (`9f8ce4db`): the only actual gap was `backgroundStyle:"cover"` — color/label/subpath/fromEnd/toEnd were already conditional — now omitted when equal to Obsidian's default. Positive test (`testDefaultValuesOmittedOnWrite`) covers all six so a future regression on any of them fails fast. Also dropped a dead if/else for `fromSide`/`toSide` (always emitted per §3 invariant 2).
- [x] [canvas][interop] **`.canvas` doesn't self-heal edges with missing `fromSide`/`toSide`.** Fixed 2026-05-28 (`9c6aa0b2`): ported V5 as `pickSideToward()` (aspect-aware angular sector, explicitly distinct from A3 drag-snap), runs during edge parse when `fromSide`/`toSide` came in empty, persists the computed value in-memory so the next save bakes it into JSON. Tests cover horizontal, vertical, and a non-trivial aspect-ratio case.
- [x] [canvas][interop] **`.canvas` geometry rounding semantics differ.** Fixed 2026-05-28 (`8d00c865`): `qRound(toDouble())` for x/y/width/height. `testFractionalGeometryRoundsNotTruncates` covers the off-by-one drift case.

## P1 — Workspace.json round-trip fixes

- [ ] [workspace][P4][phase1-2026-06-10] **`eState.scroll` stores a 0.0–1.0 scroll fraction (contract v2), not a visual line.** `EphemeralState.scroll` semantics changed in Phase 1 Task 9 (`5d7fcc5e`): the value is `MarkdownView::scrollPositionVisualLine()`'s fraction, not the pre-port visual-line float. Corbomite-internal JSON shape today, so no interop break — but revisit in Phase 3 workspace-fidelity if Obsidian interop of this field (its `eState.scroll` is a line number) starts to matter.

- [x] [workspace] Consolidate `Workspace::serialize` and `WorkspaceSerializer::toJson` into one writer — see [workspace.md](audit-2026-04-26/workspace.md) §"Notable concerns / suspected bugs"
- [x] [workspace] Implement nested-split round-trip so opening + saving an Obsidian-authored layout doesn't degrade — see [workspace.md](audit-2026-04-26/workspace.md) §"Top gaps"
- [x] [workspace] Per-group `currentTab` instead of conflated global active-leaf-index — see [workspace.md](audit-2026-04-26/workspace.md) §"Top gaps"
- [x] [workspace] Stop blind write-through of `left`/`right` `m_unknownRoot` subtree in `SessionManager` — match Obsidian shape or omit — see [workspace.md](audit-2026-04-26/workspace.md) §"Top gaps". Spec for the deferred decision: [`specs/2026-04-26-workspace-serializer-consolidation-design.md`](superpowers/specs/archive/2026-04-26-workspace-serializer-consolidation-design.md) §Deferred follow-ups. Three options to pick from: (A) drop on save; (B) pass through unmodified unless Corbomite mutated sidebar state (recommended); (C) translate to/from `CorbomiteMDI::Sidebar`. **Picked B**: `SessionManager` now tracks an `m_sidebarDirty` bit (set on identity-change in `saveSidebarState`); on save, `left`/`right` are dropped iff dirty. `floating` write-through left untouched (separate concern — Corbomite serializes its own floating section but MainWindow currently drops it before handoff).
- [x] [workspace] Repurpose `m_tabGroupOf` against live `Layout::groups()` (cache or eliminate). Update `Workspace.h:267-271` comment claiming KDDW has no public Group enumeration API. Audit: [workspace.md](audit-2026-04-26/workspace.md) §"High severity" #1 — now solvable via the public KDDW API documented in [`obsidian-audit/addenda/2026-04-26-kddw-public-enumeration.md`](obsidian-audit/addenda/2026-04-26-kddw-public-enumeration.md)
- [x] [workspace] Popout window leak — add `m_windows.removeOne(window)` to the X-close path — see [workspace.md](audit-2026-04-26/workspace.md) §"Top gaps"
- [x] [workspace] `m_tabGroupOf` lags user drags — see [workspace.md](audit-2026-04-26/workspace.md) §"Top gaps" — closed jointly with the previous item: tab-navigation primitives now read membership from `DockRegistry::groups()` directly, so user drags are reflected immediately.
- [x] [workspace] `undoCloseLeaf` loses original parent + leafHistory + eState — `closeLeaf` now captures a live tab-group sibling id as `UndoEntry::parentId`; `undoCloseLeaf` resolves it to `createLeafInGroupOf(sibling)`, restores `setEphemeralState(eState)` + overwrites `leaf->history()` with the captured `LeafHistory`, and rekeys `m_leavesById` + KDDW dock-widget uniqueName to the restored leaf id. — `libs/core/src/Workspace.cpp` (closeLeaf, undoCloseLeaf)
- [x] [workspace] `MenuEventEmitter::fileMenu` lacks source discriminator — added `(QString source, QObject* leaf=nullptr)` to the `fileMenu` signal + `emitFileMenu` helper; introduced `Corbomite::FileMenuSource::*` constants for the six Obsidian source values; `MenuInjector::onFileMenuBuilt` now takes a `FileMenuHandler = void(QMenu*, QString filePath, QString source)` so plugin handlers can scope by invocation site. — `libs/core/{include/corbomite/core/MenuEventEmitter.h,src/MenuEventEmitter.cpp,include/corbomite/core/proxies/MenuInjector.h,src/proxies/MenuInjector.cpp}`
- [x] [workspace] `addAction` appends rather than prepends — `ItemView::addAction` now uses `m_actionsLayout->insertWidget(0, btn)` so the most-recent action sits leftmost (closest to the title) and the hamburger stays anchored rightmost, matching Obsidian. — `libs/core/src/ItemView.cpp:87`

### Added 2026-06-10 — code-verification re-triage

- [x] **[FIXED 2026-06-10 — `4b44b255`.]** [metadata][core][P1][audit-2026-06-10] **LinkResolver staleness** — in-session create/rename/delete never call `addVaultPath`/`removeVaultPath` on the app LinkResolver (populated once at vault open, `MainWindow.cpp:2200-2207`); new notes stay "unresolved" until vault reopen. Also only fed `.md` files, so attachment embeds never resolve. — verified 2026-06-10 Fixed: create/rename/delete → add/removeVaultPath; feeds non-`.md`; test `tst_mainwindow_link_resolver_freshness`.
- [ ] [parsing][HANDED-OFF→MARKOFF][P1][audit-2026-06-10] **Frontmatter closing-fence false-match** — markoff-family parser `Document.cpp:42` matches `\n---` without an end-of-line check, so `----` or `---xyz` falsely closes frontmatter; write-path amplification via `processFrontMatter`. Markoff-owned; steer upstream. — verified 2026-06-10 **[STEERED 2026-06-10 — `69162236`+`7a87daf7`.]** Handoff written at [`handoff/2026-06-10-to-markoff-frontmatter-fence-eol.md`](handoff/2026-06-10-to-markoff-frontmatter-fence-eol.md) (confirmed at markoff-parser `Document.cpp:42`/`:191`). Awaiting upstream fix + re-pin.
- [x] **[FIXED 2026-06-10 — `1d7f477f`.]** [metadata][parsing][P1][audit-2026-06-10] **BOM'd files lose frontmatter in bulk index** — `MetadataCache::rebuildVault` (`MetadataCache.cpp:142-148`) reads raw bytes without BOM strip, unlike `Vault::read`. — verified 2026-06-10 Fixed: BOM strip in `MetadataCache::rebuildVault`; the real symptom was hash-divergence/re-parse churn, since `QString::fromUtf8` already strips the BOM before parse — the fix adds explicit strip to prevent churn, not parse failure; test `tst_metadatacache_bom`.
- [ ] [workspace][P1][audit-2026-06-10] **workspace.json: production save drops `floating` windows + `lastOpenFiles`** — `MainWindow::saveSessionState` (`MainWindow.cpp:856-866`) extracts only `main`+`active` from `Workspace::serialize()`. — verified 2026-06-10

### Added 2026-06-10 (Phase 0 review — discovered items)

- [ ] [metadata][vault][P1][audit-2026-06-10] **`Vault::onExternalRenamed` doesn't iterate folder descendants** — `Vault.cpp:941-960` handles an externally-renamed folder but does not walk descendants (unlike programmatic `Vault::rename`, `:434-460`), so an external folder rename leaves descendant paths stale in LinkResolver + MetadataCache until vault reopen. Discovered during Phase 0.3 review.
- [ ] [storage][P3][audit-2026-06-10] **UTF-8 BOM-strip duplicated** — `Vault::stripUtf8Bom` (`Vault.cpp:169-178`) and `MetadataCache` lambda (`MetadataCache.cpp:136-144`); consolidate into one `Corbomite::Storage` helper (libs/vault depends on libs/storage). Also note (benign): `SQLiteIndex::writeRowsFromCache` (`SQLiteIndex.cpp:213-214`) reads disk via `QString::fromUtf8` without explicit strip — harmless (`fromUtf8` auto-strips) but inconsistent. Discovered during Phase 0.5 review.

## P2 — High-traffic UX correctness

- [x] **[RESOLVED 2026-06-10 — Phase 1 Task 9 (`5d7fcc5e`): `goToLine` dispatches `setCursorPosition` on the `Markoff::MarkdownView` base, works in all three modes (Reading keeps a caret while read-only); Live attach-window delivery fixed upstream (`8112833f`). Deep subpath nav / ancestor un-fold remains future work.]** [editor-markdown] `setCursorLine` no longer hard-coded false — `src/editor/MarkdownView.cpp:81` forwards to `NoteEditorWidget::goToLine`, which works in Source only; Live + Reading return false (TODOs). Implement goToLine for Live and the styled Reading leaf (at minimum scroll to section containing line N; ideally subpath nav with ancestor un-fold) — see [editor-markdown.md](audit-2026-04-26/editor-markdown.md) §"Top suspected bugs"
- [ ] **[FROZEN ⑤ — re-verify against Live; footnote rendering likely needs Markoff support.]** [rendering][parsing] Footnotes are functionally dead — render definitions, hover-popover for refs, scroll-to-definition on click — see [rendering.md](audit-2026-04-26/rendering.md) §"Top gaps"
- [ ] **[FROZEN ⑤ — Markoff E3.]** [rendering][parsing] Callouts not parsed/rendered (data model exists in `Theme`, no consumer) — add `> [!note]`/`[!warning]` parser+renderer — see [rendering.md](audit-2026-04-26/rendering.md) §"Top gaps"
- [ ] [ui-bundle][rendering] HoverPopover lacks Mod-key pinning + child-popover chains + anchor-to-mouse + `elementFromPoint` poll — see [ui-bundle.md](audit-2026-04-26/ui-bundle.md) §"Top gaps". Decomposed into 4 sub-items:
  - [x] **Mod-key pinning** — explicit `State` enum (Hidden/Pending/Visible/Pinned); app-level event filter on `qApp` watches `Qt::Key_Control` press while Visible → Pinned (Q3=A latching, Linux `Ctrl` only); Esc + outside-MouseButtonPress unpin; replacement on `scheduleShow(newTarget)` always wins. Visual: `palette().highlight()` border accent. Spec: [`docs/superpowers/specs/archive/2026-04-27-hoverpopover-pinning-grace-design.md`](superpowers/specs/archive/2026-04-27-hoverpopover-pinning-grace-design.md). — `src/editor/HoverPopover.{h,cpp}`, tests `tests/editor/tst_hover_popover_pinning.cpp` (7 cases: pinTransitionsOnCtrlPress, escUnpins, outsideClickUnpins, replacementWinsOverPinned, pinningPersistsAcrossLinkHoverEnded, plus the 2 grace-timer cases below)
  - [x] **`elementFromPoint`-style grace timer** — 500 ms `m_graceTimer` (audit's cited Obsidian poll constant), one-shot. Started on `linkHoverEnded()` (new public method; `NoteEditorWidget` linkHovered("") now calls this instead of `cancel()`) and on popover `leaveEvent`. Stopped on `enterEvent` and on same-target `scheduleShow`. On fire, walks the parent chain from `QApplication::widgetAt(QCursor::pos())` to test popover-or-descendant containment; hides only if cursor is genuinely elsewhere. Pinned state ignores `linkHoverEnded()` entirely. — same files
  - [ ] **[RE-TARGETABLE ④ — was frozen on read-only-Live; unblocked by StyledRenderEngine (2026-05-30); re-aim the HoverPopover preview at the styled path before work.]** **Child-popover chains** — hovering a `[[link]]` inside a Pinned popover should spawn a sibling popover; parent stays open while any child is. Requires a popover registry + parent/child lifecycle. Out of scope for the 2026-04-27 PR.
  - [ ] **[GATED ON MARKOFF API — needs a `hoveredLinkRect()` equivalent on the Live editor API; not ④.]** **Anchor-to-mouse for tall targets** — Obsidian uses `offsetHeight > 300` to pick "anchor near mouse" vs "anchor to source-link rect". Today the popover always anchors to `globalPos + (0,20)` because Markoff supplies a global *point*, not a *rect*. Needs the Live-API `hoveredLinkRect()` extension first (named follow-up at `HoverPopover.h:30-32`).
- [x] [ui-bundle] `Notice(text, 0)` should not auto-close (one-line fix) — `src/dialogs/Notice.cpp:48-50` (audit cited the wrong path; lives in src/dialogs/, not libs/ui/) — see [ui-bundle.md](audit-2026-04-26/ui-bundle.md) §"Top suspected bugs"
- [x] [ui-bundle] FileExplorer F2/Delete now route through `FileManagerProxy::promptForFileRename`/`promptForDeletion` (the existing validating `RenameDialog`/`DeleteConfirmDialog`) instead of `QInputDialog`/`QMessageBox::question`. The two prompt helpers were promoted onto the plugin proxy gated on `vault.write` — `libs/vault/include/corbomite/vault/proxies/FileManagerProxy.h`, `src/plugins/file-explorer/FileExplorerView.cpp`
- [x] [views][vault] Title not refreshed on external rename — `NoteDocument` got a `setRelativePath` setter + `pathChanged(oldRelativePath)` signal; `Vault::rename` (programmatic + folder-rename + watcher-driven external) now rekeys `m_docs` and calls `setRelativePath` on the cached doc; `FileView::loadFile` connects to `pathChanged` and re-emits `displayTextChanged` so the leaf's tab caption refreshes via `WorkspaceLeaf`'s existing `m_dockWidget->setTitle` wire. — `libs/core/{include/corbomite/core/{NoteDocument.h,FileView.h},src/{NoteDocument.cpp,FileView.cpp}}`, `libs/vault/src/Vault.cpp`
- [x] [search] Bare `/regex/` query returns zero results — `SQLiteIndex.cpp:397` early-return ignores `postFilter` — see [search.md](audit-2026-04-26/search.md) §"Top suspected bugs"
- [x] [search] Top-level `-foo` query returns zero results — produces invalid FTS5 — `libs/search/src/SearchDSL.cpp:542-545` — see [search.md](audit-2026-04-26/search.md) §"Top suspected bugs". Fix: emit a single top-level negation in the And case when there is no positive sibling, then reroute leading `NOT …` from `compile()` into a new `excludedFts5Query` channel; `SQLiteIndex::searchCompiled` applies it as `path NOT IN (… MATCH excludedFts5Query)`. Also fixes `-foo -bar` (was producing `foo NOT bar`).
- [ ] **[FROZEN ⑤ — Markoff E5; Mermaid is a no-op until then.]** [rendering] Mermaid renders as light theme on dark themes — `MermaidRenderer::renderSvg` takes only source string, no theme parameter — see [rendering.md](audit-2026-04-26/rendering.md) §"Top suspected bugs"
- [x] [editor-markdown] Live-preview off-by-one column round-trip — **audit was wrong; no bug**. `Markoff::Editor::cursorColumn()` returns 1-based (`columnNumber() + 1`) while `goToLineAndColumn` takes 0-based column — that's an asymmetry inside Live's read/write API itself. The `-1` on capture (`src/editor/NoteEditorWidget.cpp:291`) compensates for that asymmetry, so EphemeralState consistently stores 0-based column across all three modes (Source returns 0-based directly; Live converts on capture; restore in both modes passes 0-based through to the corresponding setter). Round-trip pinned by a new test `livePreviewCursorRoundTrip` in `tests/editor/tst_note_editor_widget_ephemeral.cpp` (places cursor at line 5 / col 7 0-based, verifies `cursorColumn()` reads back 8 1-based, save/restore preserves both)
- [x] [views] `writeBackup` no longer writes inside the vault; backups land at `<AppLocalDataLocation>/file-recovery/<vault-id>/<name>-<ts>.md` (per-vault subdir keyed by basename + 12-char hash of the vault root) so the recovery copy doesn't show up in tree/search/graph or re-trigger `Vault::modified`. Routed through `m_adapter->mkpath`+`write` so the existing MemoryAdapter test still exercises the path. — `libs/core/src/TextFileView.cpp`
- [x] [views] Open file deleted externally + `FileView::setState` missing-file silently — `NoteDocument` got a `deleted()` signal + `markDeleted()`; `Vault` calls it from all four delete sites (programmatic remove, system trash, vault trash, external watcher) before `deleteLater`-ing the cached doc; `FileView::loadFile` subscribes to `deleted()` and nulls `m_file` + schedules `Workspace::closeLeaf` on the next event-loop turn so a subsequent save can't silently re-create the file via `Vault::modify`. `FileView::setState` does the same when its resolveFile lookup misses (and `m_allowNoFile` isn't set). — `libs/core/{include/corbomite/core/{NoteDocument.h,FileView.h},src/{NoteDocument.cpp,FileView.cpp}}`, `libs/vault/src/Vault.cpp`
- [x] [editor-markdown] `setFoldedHeadingLines` line-count invalidation — `NoteEditorWidget` now stashes the capture-time line count in `EphemeralState::extraKeys["corbomite.foldedHeadingsLineCount"]` (Obsidian sees it as an unknown key + round-trips it). On restore, when the saved count != current line count, the fold list is dropped (passes empty vec to `setFoldedHeadingLines`) instead of folding the wrong headings. — `src/editor/NoteEditorWidget.cpp`
- [ ] **[RE-AIMED 2026-06-10 — reading is now a Styled leaf, not read-only Live; re-aim at styled-leaf interaction. Live mode already has in-delegate checkbox toggle; the gap is Reading.]** [editor-markdown] Checkbox-click-to-toggle missing in Reading — see [editor-markdown.md](audit-2026-04-26/editor-markdown.md) §"Top gaps"
- [x] [bases][parsing] User-keyed dicts inside `.base` (`properties`/`formulas`/`summaries`) still alphabetise on round-trip — added `propertyOrder`/`formulaOrder`/`summaryFormulaOrder` companion lists tracked at parse time; `emitMap` extended with a `nestedKeyOrder` arg so per-block insertion order overrides QVariantMap's alphabetical sort. — `libs/bases/{include/corbomite/bases/BasesQuery.h,src/BasesQuery.cpp}`
- [x] [parsing][editor-markdown] Markoff `![[…]]` embed target — tree-sitter routes `![[…]]` through its `image` node in inline contexts where the wiki-link grammar doesn't win, dropping the target (image_description = `[Target]`, link_destination = ""). `TreeSitterParser::collectInlineQueries` now sniffs the raw bytes at the start of the image branch and emits a true `LinkInfo::Embed` with extracted target/display, so `MetadataCache::drainOnePath` actually has a non-empty `original` to resolve. — `libs/markoff-family/libs/markoff-parser/src/TreeSitterParser.cpp`

### Added 2026-06-10 — code-verification re-triage

- [ ] [workspace][ui-bundle][P2][audit-2026-06-10] **Sidebar layout amnesia** — KateMDI `Sidebar::saveSession/restoreSession` exist (`CorbomiteMDI.cpp:1351-1460`) but are never called; `saveSessionState` hardcodes width 200 / rightVisible=false (`MainWindow.cpp:855`). — verified 2026-06-10
- [ ] [workspace][P2][audit-2026-06-10] **Tab history never populates** — `WorkspaceLeaf::goBack/goForward` + `LeafHistory` fully built, zero app callers; `openFileInWorkspace` bypasses `navigate()`; no back/forward actions exist. — verified 2026-06-10
- [ ] [settings][P2][audit-2026-06-10] **hotkeys.json parsed but never applied** — `Hotkey.cpp` + `VaultConfig::readHotkeysJson` are test-only; Obsidian hotkeys do nothing. — verified 2026-06-10
- [ ] [settings][P2][audit-2026-06-10] **`app.json` settings read by nothing** — trashOption, alwaysUpdateLinks, useMarkdownLinks, newLinkFormat, userIgnoreFilters all unconsumed (`VaultConfig` API exists). — verified 2026-06-10
- [ ] [vault][P2][audit-2026-06-10] **getAvailablePath divergence** — collision counter starts at 2 ("Untitled 2") and uses case-sensitive lookup; Obsidian starts at " 1" (`FileManager.cpp:583-597`). — verified 2026-06-10
- [ ] [canvas][P2][audit-2026-06-10] **Canvas file-card paths resolve relative to the canvas file's dir, not vault root** (`CanvasViewTab.cpp:26-33`) — subfolder canvases show empty cards. — verified 2026-06-10
- [ ] [canvas][interop][P2][audit-2026-06-10] **Canvas node/edge order not preserved** — `QHash` storage (`CanvasDocument.h:57-58`) reorders arrays every save — diff churn vs Obsidian. — verified 2026-06-10
- [ ] [workspace][interop][P2][audit-2026-06-10] **workspace.json split/tabs nodes written with empty `id` and no `dimension`** (split proportions) — `WorkspaceSerializer.cpp` walkLayoutContainer/walkLayoutNode never assign; proportions neither saved nor applied. — verified 2026-06-10

## P3 — Plugin extension surface (individual items; coordinated proxy work in Cluster B)

- [x] **[REGRESSED 2026-05-25 — wrapped the now-retired `Markoff::Reading::ReadingView`; currently `#if 0` with link-only stubs in `libs/core/src/MarkdownRenderer.cpp`. Re-target to the read-only-Live renderer (bucket ④).]** [editor-markdown][plugin] `MarkdownRenderer.render(...)` static API — Cluster B Phase 2.3. Returns `QFuture<void>`; wraps `Markoff::Reading::ReadingView`. Permissionless. `Corbomite::MarkdownRenderer::render(...)` (static method on the existing class).
- [ ] **[RE-TARGETABLE ④ — was frozen on read-only-Live; unblocked by StyledRenderEngine (2026-05-30); re-aim at the styled render path before work.]** [editor-markdown][plugin] `MarkdownRenderChild` framework — parent registry, addChild, mount/unmount signals — see [editor-markdown.md](audit-2026-04-26/editor-markdown.md) §"Top gaps". Cluster B's `DecorationProvider` covers part of the use case; the parent registry / addChild / mount/unmount split is a separate refactor.
- [x] [plugin][ui-bundle] `addStatusBarItem` — Cluster B Phase 2.1. `Plugin::addStatusBarItem(localId, widget)`; `ui.statusbar` permission token; `StatusBarRegistry` wraps `QMainWindow::statusBar()`.
- [x] [ui-bundle] Lucide icon registry + `Plugin.addIcon(name, svg)` — Cluster B Phase 2.2. `LucideIconRegistry` singleton; `addIcon` registrar; `ui.icons` permission token. **Follow-up:** bundled SVG set (~50 icons referenced by Obsidian's built-in commands) not yet pre-populated; `lucide-*` names without explicit `addIcon` registrations still render blank.
- [x] **[RESOLVED 2026-06-10 — shipped: `libs/core/include/corbomite/core/Events.h` (Obsidian-modeled bus) + `VaultProxy::on/off` ("create"/"modify"/"delete"/"rename") gated by `vault.events` permission.]** [plugin][core] Plugin-side `Events` mixin — Obsidian-shape `vault.on("create", cb)` won't work today — see [plugin.md](audit-2026-04-26/plugin.md) §"Top gaps"
- [x] [plugin][core] `registerObsidianProtocolHandler` — Cluster B Phase 3.2. `corbomite://` routes; `protocol` permission token. **Follow-up:** `obsidian://` opt-in (Settings checkbox + xdg-mime registration on toggle).
- [ ] **[RE-TARGETABLE ④ — was frozen on read-only-Live; unblocked by StyledRenderEngine (2026-05-30); re-aim at the styled render path before work.]** [plugin][rendering] Cluster B follow-up — wire `Corbomite::Core::PostProcessorRegistry` (host-wide singleton at `MainWindow::m_pluginPostProcessors`) into ~~`Markoff::ReadingView`'s~~ the render pipeline. Plugin registrations land but aren't dispatched today.
- [ ] **[RE-TARGETABLE ④ — was frozen on read-only-Live; unblocked by StyledRenderEngine (2026-05-30); re-aim at the styled render path before work.]** [plugin][rendering] Cluster B follow-up — wire `Markoff::CodeBlockProcessorRegistry` (host-wide singleton at `MainWindow::m_pluginCodeBlocks`) into ~~`Markoff::ReadingView::codeBlockProcessorRegistry()`~~ the render path (or a fallback consult). Plugin registrations land but aren't dispatched today.
- [ ] **[GATED ON MARKOFF API — Live render path is Markoff QML; needs an upstream hook, not ④.]** [plugin][editor-markdown] Cluster B follow-up — a `DecorationProviderHook` virtual call site in the Live render path that consults `Corbomite::DecorationProviderRegistry::instance().providers()` and merges results with built-in decorations. Plugin registrations land but aren't dispatched today.

### Added 2026-06-10 — code-verification re-triage

- [ ] [search][P3][audit-2026-06-10] **Search returns one snippet per file** (`SQLiteIndex.cpp:461`) — multi-match files lose all but one hit; no history/sort/copy in SearchView. — verified 2026-06-10
- [ ] [graph][settings][P3][audit-2026-06-10] **Graph settings not persisted** — no graph.json read/write anywhere; every slider resets per session. Local graph depth hardcoded 2 (`LocalGraphView.cpp:72`). — verified 2026-06-10
- [ ] [bases][P3][audit-2026-06-10] **Bases built-in summaries missing** — Range/Earliest/Latest/Checked/Unchecked/Empty/Filled absent from `BasesQueryResult.cpp:177-187`. — verified 2026-06-10
- [ ] [ui-bundle][P3][audit-2026-06-10] **Quick switcher matches filename only** — no path/alias matching, create-row invisible (`QuickSwitcher.cpp:171-182`). — verified 2026-06-10
- [ ] [core][P3][audit-2026-06-10] **Template insertion appends at end of note instead of cursor** (`MainWindow.cpp:2573-2613`). — verified 2026-06-10. *Partial 2026-06-10 (Phase 1):* the `{{cursor}}` marker now moves the caret post-insert via `goToLine` (`5d7fcc5e`); the insertion *position* still appends.
- [ ] [ui-bundle][P3][audit-2026-06-10] **Placebo dialogs** — Insert Table/Callout exec then discard results (`MainWindow.cpp:543-567`); hamburger Find…/Replace… actions have no connect (`MarkdownView.cpp:298-312`). Disable or wire. — verified 2026-06-10
- [ ] [parsing][interop][P3][audit-2026-06-10] **Frontmatter YAML re-emission diverges from Obsidian** (ryml emit vs js-yaml dump style) — Properties edits reformat the whole block; no byte-format test. — verified 2026-06-10
- [ ] [settings][P3][audit-2026-06-10] **Trash mode read from corbomiterc, ignores vault `app.json` trashOption** (`SettingsDialog.cpp:96-103`). — verified 2026-06-10

## P4 — Lifecycle / per-vault isolation

- [x] [core][vault] Dangling adapter pointers in `onVaultClosed` — `MainWindow::onVaultClosed` now drops `m_embedRenderer`'s cached `metadataParser` pointer (matched the existing `metadataCache`/`resources` nulls) and `.reset()`s `m_linkResolverAdapter` / `m_metadataCacheAdapter` / `m_metadataParserImpl` before deleting the wrapped `m_metadataCache` / `m_linkResolver`, so a fresh open's `make_unique` re-creation can't transiently destroy a still-referenced shim. — `src/app/MainWindow.cpp:2289-2310`
- [x] [plugin] `onLoad` throw auto-unload — `PluginManager::enablePlugin` wraps `plugin->load(ctx)` in try/catch, calls (idempotent) `plugin->unload()` on throw, deletes the instance + context, clears `info->{instance,context,enabled}`, and stores `LoadState::OnLoadThrew` so `PluginsPage` surfaces a "this plugin raised an exception" notice. Persisted enabled-state is left intact so a fixed plugin re-enables on next launch. Test: `tst_plugin_manager_lifecycle::onLoadThrowAutoUnloads`. — `libs/vault/src/PluginManager.cpp:274-310`, `src/dialogs/PluginsPage.cpp:196-207`
- [x] [plugin][workspace] Detach-leaves-of-type on plugin disable — added `Workspace::detachLeavesOfType(type)` (snapshots + closeLeaf-loops, guards empty type), `ViewRegistrar::registeredTypes()` accessor for the host. MainWindow connects a second handler to `pluginUnloading` that reads `info->context->views()->registeredTypes()` and calls `detachLeavesOfType(type)` for each before the proxy destructor unregisters factories — so any leaves the disabling plugin owned are evicted before their backing view-factory disappears. Test: `tst_workspace_factory::detachLeavesOfType_closesMatchingLeavesOnly` (markdown + plugin-view stubs; verifies type-match, no-match no-op, and empty-string guard). — `libs/core/{include/corbomite/core/Workspace.h:107-115,src/Workspace.cpp:385-396}`, `libs/core/include/corbomite/core/proxies/ViewRegistrar.h:36-39`, `src/app/MainWindow.cpp:255-272`
- [x] [plugin] `onExternalSettingsChange()` + `data.json` `QFileSystemWatcher` — Cluster B Phase 3.3. `Plugin::onExternalSettingsChange()` virtual + `PluginManager::m_dataJsonWatcher` + `simulateExternalSettingsChange` for tests.
- [ ] [core][plugin] `appId` for per-vault QtKeychain scoping — see [core-and-addenda.md](audit-2026-04-26/core-and-addenda.md) §"Top gaps"
- [ ] [core][plugin] `quit` event with `Eb`-equivalent collector for plugin async cleanup — see [core-and-addenda.md](audit-2026-04-26/core-and-addenda.md) §"Top gaps"
- [x] [core][workspace] `css-change` event — bridge `ThemeService::themeChanged` to Workspace — `Workspace::cssChange()` Q_SIGNAL + `emitCssChange()` re-entry; MainWindow connects `m_themeService::themeChanged` to it post-Workspace-construction; `WorkspaceController::cssChange` mirrors onto the plugin proxy surface so plugins observing theme transitions can re-render decorations. Test: `tst_proxy_workspace::cssChange_reEmitsFromWorkspace`. — `libs/core/include/corbomite/core/Workspace.h:248-260`, `libs/core/src/proxies/WorkspaceController.cpp:33-39`, `src/app/MainWindow.cpp:1597-1611`

## P5 — Missing features (structurally absent; coordinated internal-plugin work in Cluster F)

- [ ] [plugin] Cluster B follow-up — kitchen-sink reference plugin under `tests/plugins/cluster-b-kitchen-sink/`. Exercises every Cluster B verb in one `onload()` as a canonical reference for plugin authors. KPluginFactory `.so` module + metadata.json declaring all relevant permissions.
- [ ] **[RE-TARGETABLE ④ — was frozen on read-only-Live; unblocked by StyledRenderEngine (2026-05-30); re-aim the HoverPopover preview at the styled path before work.]** [plugin][rendering] Page Preview plugin — registry is built; tiny plugin-shaped popover orchestrator closes the loop — see [plugin.md](audit-2026-04-26/plugin.md) §"Top gaps"
- [ ] **[SPLIT — PDF *view* is actionable now ②; the `![[file.pdf]]` *embed* is FROZEN ⑤ on Markoff E3.]** [rendering] PDF view + `![[file.pdf]]` embed — biggest single rendering-domain capability gap — see [rendering.md](audit-2026-04-26/rendering.md) §"Top gaps"
- [ ] **[RE-TARGET ③ — conversion survives; move the paste-interception hook to the QML editor.]** [rendering][editor-markdown] Paste-from-HTML → Markdown (`htmlToMarkdown` analogue / Turndown port) — see [rendering.md](audit-2026-04-26/rendering.md) §"Top gaps"
- [x] [bases][workspace] Bases Phase 2 — `BasesView` got a public `setCurrentFile(TFile *)` that forwards to its `QueryController`. `MainWindow::propagateServicesToView` now connects each `BasesView` to `Workspace::activeLeafChanged`, resolves the new active leaf's file via `m_vaultObj->getFileByPath(state.file)`, and pushes it into the controller — also seeds once with the currently-active leaf at wire-up time. `this` formulas now re-evaluate when the user switches notes. — `libs/bases/{include/corbomite/bases/BasesView.h:60-66,src/BasesView.cpp:122-125}`, `src/app/MainWindow.cpp:1140-1163`
- [x] [settings][ui-bundle] Hotkeys page — `SettingsDialog` got a fifth page that embeds `KShortcutsEditor` directly (`AllActions`, `LetterShortcutsAllowed`); commits on dialog accept, undoes on cancel. `SettingsDialog` constructor now takes a `KActionCollection *` (passed `actionCollection()` from MainWindow); when null the page renders an "open a vault to configure shortcuts" placeholder. **Follow-up:** optional `hotkeys.json` ⇄ `KSharedConfig` bridge for round-trip with Obsidian's persisted shortcuts is not yet wired — KF6 persists changes through XMLGUI's own rc-file path. — `src/dialogs/{SettingsDialog.h,SettingsDialog.cpp:218-258}`, `src/app/MainWindow.cpp:1181`
- [ ] [ui-bundle] About page — see [ui-bundle.md](audit-2026-04-26/ui-bundle.md) §"Top gaps"
- [ ] [canvas][NEEDS-DESIGN] **Canvas has no interactive edge-creation UX.** Edges *display* and *round-trip* from a loaded `.canvas` (`CanvasScene::onEdgeAdded` → `addEdgeItemToScene`; the 2026-05-28 interop bundle made existing edges save-faithful), but there is **no way for a user to draw a new connection between cards.** `CreateEdgeTool` (`libs/canvas/src/CanvasTool.cpp:454-605`) holds working press/drag/release edge-creation logic but is **dead code** — never instantiated or activated (`CanvasScene` ctor only ever creates `SelectMoveTool`, `CanvasScene.cpp:36`; `git log -S 'CreateEdgeTool'` on the scene/app is empty). No toolbar, keyboard, context-menu ("Connect"), or hover-handle affordance exists. Confirmed 2026-05-30: the canvas was built for *opening pre-existing vaults* (display + fidelity), not authoring connections. **To build:** (1) pick the activation affordance — Obsidian-faithful is hover-handles (drag a dot on a card edge); alternatives are a tool/mode toggle or a context-menu "Connect to…". (2) Widen `CreateEdgeTool::findNearCard` — it `dynamic_cast`s only to `TextCardItem` (`CanvasTool.cpp:478`), so it can't see `FileCardItem`/`GroupItem` (siblings under `ConnectableItem`); connect to `ConnectableItem` instead so file cards/groups work. (3) Wire `CreateEdgeTool` into the scene's tool set + the chosen affordance. Needs a brainstorm/design pass before implementation. — surfaced 2026-05-30 (user testing)

## P6 — API stability / future-proofing

- [x] [plugin][core] Move permission tokens from `PluginContext.cpp:21-32` to `corbomite/core/PluginPermissions.h` with `inline constexpr auto` constants — Cluster B Phase 0.
- [ ] [plugin] Document the `apiLevel: 1` ABI before first third-party plugin release — see [plugin.md](audit-2026-04-26/plugin.md) §"Top gaps"
- [ ] [plugin] Document the `X-Corbomite-DockArea`/`DockIcon`/`DockTitle` extension to manifest format — see [plugin.md](audit-2026-04-26/plugin.md) §"Top gaps"
- [x] [core] MomentFormatter missing tokens — added `Y` (year, no padding), `Q` (quarter 1-4), `gg`/`gggg` (locale week year, mapped onto Qt's ISO `weekNumber` out-param), `E` (ISO day-of-week 1-7), `e` (locale day-of-week 0-6 via `(qDow - locale.firstDayOfWeek + 7) % 7`), `k`/`kk` (hour 1-24 with midnight → 24), `Z`/`ZZ` (timezone offset `+HH:MM` / `+HHMM` from `QDateTime::offsetFromUtc`), and the moment-en locale shortcuts `LT/LTS/L/l/LL/ll/LLL/lll/LLLL/llll` (each routed through a recursive `format()` call against the literal moment-en sub-format). Tests: 9 new cases in `tst_momentformatter` (`testYearWithSign`/`testQuarter`/`testWeekYearGgGggg`/`testIsoDayOfWeekE`/`testLocaleDayOfWeekE`/`testHourKkk`/`testTimezoneOffsetZ`/`testLocaleShortcuts`/`testEscapedNewTokensStayLiteral`). Adjusted two pre-existing tests whose "literal pass-through" / "unknown token" probes used letters that are now recognized tokens. — `libs/core/src/MomentFormatter.cpp:115-340`

---

## Shakedown 2026-04-28 — user-testing findings (Phases 0–1)

> Surfaced during the live shakedown documented in `~/dev/test-vaults/SHAKEDOWN-2026-04-28.md`.
> Closures within this section reflect immediate fixes shipped in the same session;
> open items are deferred to the next planning round.

### Closed (this session)

- [x] [vault][settings] **`Vault::writeConfigJson(name, …)` produced double `.json.json` extension** when the caller passed a name that already ended in `.json` (e.g. BookmarksPlugin used `"bookmarks.json"`, leading to `.obsidian/bookmarks.json.json` on disk — invisible to Obsidian, which silently re-creates an empty `bookmarks.json` next to the misnamed one). Fix: `configJsonAbs` (libs/vault/src/Vault.cpp:518) now strips a trailing `.json` defensively; `BookmarksPlugin` (src/plugins/bookmarks/BookmarksPlugin.cpp:44,82) now calls with the canonical `"bookmarks"` stem. Existing tests in `tst_vault_config_json` still pass byte-exact. — surfaced T-11
- [x] [settings][plugin] **In-tree plugin manifests lacked `X-Obsidian-Id`**, and the fallback alias dict in `PluginManager.cpp:42-55` keyed on stale `corbomite_<slug>` (underscore) names that never matched the actual `corbomite-<slug>` (hyphen) plugin IDs. Result: `obsidianIdFor()` returned empty for every built-in, so `writeEnabledState` dual-write never wrote `core-plugins.json`. **The dual-write claimed in P0 #8's closeout was effectively dead code.** Fix: added `X-Obsidian-Id` to bookmarks/backlinks/file-explorer/outline/outlinks/properties/search/graph-view manifests (mapping to Obsidian's canonical core slug). Local-graph deliberately left unmapped (Obsidian routes both global + local through one `graph` plugin; mapping both would cause double writes). The stale alias dict is now dead code; not removed (low risk; can be cleaned in a follow-up). — surfaced T-12
- [x] [bases][ui-bundle] **FileExplorer hid `.base` files.** `NotesTreeModel::isTreeFile` (libs/models/src/NotesTreeModel.cpp:12-17) only accepted `md`/`canvas`. Added `base` and a `"base"` icon hint to `FileTypeRole` so kepano-style vaults with 30+ `.base` files render in the tree. — surfaced T-13
- [x] [ui-bundle] **FileExplorer context menu missing Rename/Delete on directories**, and F2/Delete keypresses ignored when a directory row was selected. Plumbing on the proxy side already accepts `TAbstractFile *`, so this was a UI-only gap. Added Rename + Delete to the `isDir` branch of `showContextMenu` (suppressed on the virtual root) and dropped the file-only guard in `eventFilter` so F2/Delete now work on both files and folders. — surfaced T-18

### Closed (Phase 2 follow-up — same session)

- [x] [ui-bundle][plugin][CRITICAL] **`PluginsPage::onItemChanged` use-after-free crash on plugin toggle.** When the user clicks a plugin's checkbox in Settings → Plugins, the slot calls `m_mgr->enablePlugin(id)` (or disable). Inside, the manager emits `pluginEnabled`/`pluginDisabled` synchronously; PluginsPage's connected slot calls `rebuild()`, which `m_list->clear()`s every QListWidgetItem — including the `item` parameter of the still-running `onItemChanged`. Control returns and line 118 (`m_list->row(item)`) dereferences a dangling pointer → SEGV. Confirmed via two backtraces in `~/dev/test-vaults/observations/phase1/t-12-plugins-backtrace`. Fix: change the two `connect()` calls in `PluginsPage::PluginsPage()` to use `Qt::QueuedConnection` so `rebuild()` runs after `onItemChanged` returns to the event loop. The synchronous re-entry path is now broken; cross-app sync rebuilds (which were the legitimate use case) still work, just deferred one event-loop turn. — surfaced T-12 follow-up
- [x] [plugin] **KCoreAddons warning: explicit `Id` field in KPlugin manifest is redundant.** The .so basename derives the plugin ID; explicit `Id: "corbomite-bookmarks"`/`"corbomite-backlinks"` in the manifests was logging at every launch. Removed both. — surfaced T-12 follow-up
- [x] [vault][P0] **Folder rename did not rewrite descendant body-links — silent vault-format corruption.** When a folder containing referenced notes was renamed, `Vault::rename` correctly moved the descendants' on-disk paths (P0 #5 closure was preserved), but `FileManager::renameFile`'s snapshot/rewrite phase only matched against the FOLDER's own path, not its descendants'. Result: every wikilink like `[[Targets/Plain target]]` in any source file was left pointing to the old path even though the file had moved. The bookmarks plugin handled its own rewrites via the per-descendant `renamed` signal, so bookmarks survived; markdown body content did not. **Discovered by user in mid-session shakedown** after the T-18 fix made folder rename UI-accessible. Fix: `renameFile` now expands a folder rename into a `moves[]` vector (folder + every descendant from `MetadataCache::allPaths()`) and runs the snapshot/rewrite passes against the union — for each link the new `matchMove` helper finds which descendant's path it points to and uses *that* descendant's old/new for `rewriteLinkLiteral`. The single-file path is unchanged (moves[] reduces to one entry). All 7 existing rename tests pass; behaviour for non-folder rename is byte-identical. — `libs/vault/src/FileManager.cpp:360-525` — surfaced 2026-04-28 mid-session retest

### Open (deferred — Phase 1 re-runs + Phase 2)

- [x] **[RESOLVED 2026-04-28 — see "Closed (bug-bash round 2)" T-16 below; this deferred copy was superseded.]** [vault][settings][P0] **`appearance.json` is never written from Settings → Appearance.** Theme is currently a global app preference (KConfig-backed, `~/.config/corbomite-devrc`), not per-vault. Obsidian writes `theme: "obsidian"`/`"moonstone"` per vault; the cross-app contract for appearance is therefore not honoured. Two-step fix: (a) decide policy — pure per-vault, or per-vault override on top of a global default; (b) wire `ThemeService::themeChanged` (or the SettingsDialog Apply path) to `VaultConfig::writeAppearanceJson` with the existing P0 #7 vocabulary mapping. The vocabulary mapping is already in `VaultConfig`; only the call-site is missing. — surfaced T-16
- [x] **[RESOLVED 2026-04-28 — see "Closed (bug-bash round 2)" T-21 below; this deferred copy was superseded.]** [workspace][P1] **Layout deserialization swaps split-child positions.** Saved `workspace.json` describes `{type: split, direction: horizontal, children: [vertical-split-with-tabs, single-tabs-leaf]}`. On reload Corbomite shows the single-tabs-leaf on the LEFT and the vertical-split on the RIGHT — positions reversed. Obsidian opens the same file correctly (3 columns visible), so the JSON shape is right, but Corbomite's deserializer (or the KDDW LayoutSaver consumer) iterates children in reverse / last-wins order. Diagnostic JSON pasted in T-21 of the shakedown report. Likely a 1–10-line fix once the iteration order is identified, but needs careful read of `WorkspaceSerializer::fromJson` and `Workspace::deserialize`. — surfaced T-21
- [x] **[FIXED 2026-06-10 — `41b4f537`+`7fdbb25b`.]** [vault][core][P1] **(re-ranked P4→P1, 2026-06-10: `index.sqlite` + `metadata-cache.db` + WAL churn inside the vault pollutes synced/Obsidian-shared vaults and feeds the file watcher; verified at `MainWindow.cpp:2181,2235,2240`; move target `<AppLocalDataLocation>/<vault-id>/` — precedent: file-recovery move.)** **`.obsidian/` accumulates Corbomite-specific artifacts.** Opening a vault in Corbomite creates `.obsidian/index.sqlite`, `.obsidian/metadata-cache.db`, and `.obsidian/plugins/`. These are Corbomite-private state. Issues: (a) Obsidian doesn't expect them; benign today but pollutes the cross-app file inventory and inflates `git diff` after every Corbomite session; (b) they don't survive vault-relocation cleanly; (c) precedent established by file-recovery (P2, 2026-04-27) is to live under `<AppLocalDataLocation>/<corbomite-cache>/<vault-id>/…`. Decide: move all three out of `.obsidian/` (preferred, matches file-recovery), or namespace under `.obsidian/.corbomite/` (smaller change, still off Obsidian's radar). The `plugins/` directory is the biggest design call — community plugins need it for `data.json` storage; built-ins don't. — surfaced T-02 + T-10 Fixed: DBs now under `<AppLocalDataLocation>/index/<vault-id>/` via `PathUtils::vaultLocalDataDir`; legacy in-`.obsidian/` copies cleaned on open; tests `tst_mainwindow_db_paths`, `tst_path_utils`.
- [x] **[RESOLVED 2026-05-27 — full editing surface shipped (Tasks 0–8 of the plan; commits `66997a47`/`dc86ca7`(Markoff)..`5e7310f3`).]** [plugin][ui-bundle][P2] **PropertiesView is read-mostly.** (Framing was already stale: commit/add worked since the InternalPlugin migration; the real gaps were delete/reorder/rename + add-with-type.) Reworked from `QFormLayout` to an ordered list of `PropertyRow` widgets (grip / key / editor / delete). Add-with-type dialog, inline rename (position+value preserved), delete, and drag-reorder all funnel through one debounced wholesale write via a new order-authoritative `FileManager::setFrontMatter` (+ `vault.write`-gated proxy), which deletes omitted keys and persists row order. Complex values (nested maps / lists-of-non-scalars) are classified read-only (`isEditableFrontmatterValue`) and preserved **verbatim** via a new Markoff `YamlValue::setChildFrom` primitive — **this fixed a pre-existing silent corruption** where editing any property in a note with a nested-map frontmatter value blanked that map. Tests: `tst_setfrontmatter` (vault, 6 slots incl. the corruption guard), `tst_properties_plugin` (add/delete/rename/reorder + classifier), Markoff `tst_frontmatter` (setChildFrom). **Interactive paths pending user eyeball** (add dialog, inline-rename click, drag-reorder, complex-value read-only summary) — offscreen Qt can't drive them. Spec/plan: [`specs/2026-05-27-properties-panel-editing-design.md`](superpowers/specs/archive/2026-05-27-properties-panel-editing-design.md) / [`plans/2026-05-27-properties-panel-editing.md`](superpowers/plans/archive/2026-05-27-properties-panel-editing.md). Unblocks user verification of P0 #1 / P0 #11. — surfaced T-14 + T-17
- [x] **[RESOLVED 2026-04-28 — see "Closed (bug-bash round 2)" T-19 below; this deferred copy was superseded.]** [ui-bundle][P2] **FileExplorer tree collapses on rename.** When a file rename succeeds, the open/expanded folder state is lost (tree resets to all-collapsed). Likely cause: `NotesTreeModel::onRenamed` triggers `beginResetModel`/`endResetModel` (full rebuild) rather than mutating the affected node in place. Preserve expansion state across the rename — either via incremental signals (`dataChanged` for the renamed node + `layoutChanged` if path changes parent) or by snapshotting + restoring expansion state in the view. — surfaced T-19
- [x] **[RESOLVED 2026-04-28 — see "Closed (bug-bash round 2)" T-20 below; this deferred copy was superseded.]** [ui-bundle][P2] **`Vault::create` collision is silent.** Attempting to create `home` when `Home.md` exists is correctly refused (case-insensitive collision check shipped via P0 #9), but no error toast / dialog tells the user *why*. The error is swallowed somewhere between `Vault::create` and the FileExplorer "New Note" path. Add a `Notice` (using the existing dialog) with text like "A file named 'Home' already exists (case-insensitive match)." — surfaced T-20
- [x] **[CLOSED 2026-06-10 — overtaken by Cluster D (D.2–D.4c + formula editor 05-27 + filter builder 05-28); only D.5 plugin API remains.]** [bases][ui-bundle][P2] **Bases editing UX is too underdeveloped to drive a round-trip test.** A `.base` file opens and the table renders, but there is no UI affordance to add/remove/edit columns, change views, edit filters, or modify formulas (screenshot at `~/dev/test-vaults/observations/phase1/BaseViewCorbomite.png`). Cluster K shipped Bases as MVP "viewer + parser + persistence" — the editor is the next phase. Already tracked as Cluster D ("Bases UI completion") in `superpowers/plans/INDEX.md`; this is a pointer that the plan needs filling in before any further user-visible Bases testing. — surfaced T-13 follow-up
- [ ] [ui-bundle][bookmarks][P3] **Bookmark UX missing affordances for in-document anchors.** Obsidian's bookmark form shows an immutable `Path` (which can be `Note.md#Heading` or `Note.md#^block`) plus an editable `Title`. Corbomite's form has `Name` + `Group` only — no way to bookmark a heading or block, no display of the anchor target. Roundtrip works for plain-file bookmarks (T-11 PASS), but heading/block bookmarks (e.g. those Obsidian creates via right-click on a heading) cannot be created or edited in Corbomite. — surfaced T-11 follow-up
- [ ] **[RE-TARGET ③ — drop-interception moved to the QML editor; Markoff now routes drag-drop through the document layer (audit L3), so re-verify current behaviour before fixing.]** [editor-markdown][P2] **Drag-drop image into editor pastes URL as text.** Dragging an image file (e.g. from a filesystem browser or another app) into a Corbomite editor leaf inserts a literal `file:///path/to/image.png` string instead of either (a) copying the image into the vault's attachment folder + inserting `![[…]]`, or (b) inserting `![](file://…)` if vault-copy is disabled. There is also no "Insert Image" action surfaced in the menu/command palette as a fallback. Obsidian handles this via copy-into-attachments + relative `![[…]]` link. — surfaced T-22
- [ ] [editor-markdown][P3] **No periodic crash-recovery snapshot of unsaved work.** `TextFileView::writeBackup` exists but only fires on save *failure*, not as a periodic dirty-state snapshot. If Corbomite crashes (or is force-killed) between saves, unsaved typing is lost — there is nothing in `<AppLocalDataLocation>/file-recovery/` to recover. Obsidian's File Recovery core plugin snapshots dirty notes every N minutes; we need an equivalent. The recovery-write *destination* is correct (P2 closeout 2026-04-27 routed it outside the vault); only the periodic snapshot trigger is missing. — surfaced T-25 (the test step was wrong: write fires on save failure, not on dirty-timer; restating as the missing-feature flag)

### Closed (bug-bash round 2 — 2026-04-28)

- [x] [vault][settings][P0] **`appearance.json` not written from theme changes.** Theme was a global KConfig preference; nothing wrote `.obsidian/appearance.json` per vault, so opening a Corbomite-themed vault in Obsidian saw no theme set. Fix: in `MainWindow`, two new `ThemeService::themeChanged` connections — one persists the active theme to `appearance.json` mapped to Obsidian's vocabulary (`"moonstone"`/`"obsidian"`/empty + custom-name passthrough), the other on `onVaultOpened` reads `appearance.json` and applies the matching internal theme via `setActiveThemeByName`. Both directions are idempotent (writer compares against existing `theme` value, reader skips when already on the right theme), so the read→change→write loop doesn't churn the file. **Note** — the original test script's expected vocabulary was inverted (claimed `"obsidian"`=light, `"moonstone"`=dark); Obsidian's actual mapping is `"moonstone"`=light, `"obsidian"`=dark, which is what `VaultConfig::obsidianAppearanceTheme` already encoded correctly. Test script entry should be updated. — `src/app/MainWindow.cpp` (themeChanged write hook + onVaultOpened read+apply) — surfaced T-16
- [x] [workspace][P1] **Layout deserialization swapped split-child positions** when a JSON split node held both nested-split and tabs children at the same level. `SplitNode` had separate `splitChildren` + `tabsChildren` lists, parsing bucketed children by type into them, materializing iterated tabs first then splits, and rendering wrote splits first then tabs — so the original interleaved order was destroyed at every step. Obsidian-authored layouts and KDDW LayoutSaver dumps both produce mixed orderings, so the EITHER-OR assumption in the comment was simply wrong. Fix: added `QList<QPair<bool,int>> childOrder` to `SplitNode` recording original sequence, plus `addTabs`/`addSplit` helpers that maintain it. `parseSplit`, `walkLayoutNode`, and the floating-window fallback all switched to the helpers. `renderSplit` and `materializeSplit` now walk `childOrder` (with a fallback to the old behavior when `childOrder` is empty, defensive). All 15 workspace tests pass; the user's diagnostic JSON in T-21 now round-trips correctly. — `libs/core/src/WorkspaceSerializer.cpp` — surfaced T-21
- [x] [ui-bundle][P2] **FileExplorer tree collapses on rename / create / delete.** Confirmed cause: `NotesTreeModel::{onCreated,onDeleted,onRenamed}` all call `rebuild()` which does a full `beginResetModel`/`endResetModel`, dropping the QTreeView's expansion state. Fix kept the simple full-rebuild model behaviour but preserves expansion in the view: `FileExplorerView` now connects `modelAboutToBeReset` → `m_savedExpansion = expandedFolderPaths()` and `modelReset` → `setExpandedFolderPaths(m_savedExpansion)`. The two helper methods already existed (used by session save/restore) so this is just wiring. Verified with `tst_fileexplorer_plugin` + `tst_notestreemodel`. — `src/plugins/file-explorer/FileExplorerView.{h,cpp}` — surfaced T-19
- [x] [ui-bundle][P2] **`Vault::create` collision is silent.** Cause: `FileExplorerView::onNewNoteIn` ignored a null return from `createNewMarkdownFile`. Fix: when create returns null, show a `QMessageBox::warning` explaining the case-insensitive collision so the user knows why the operation refused. (`Notice` toast would be the prettier UX but pulling `src/dialogs/Notice.h` into a plugin module crosses the dependency boundary; using QMessageBox keeps the plugin self-contained, matches the existing `QInputDialog`/`QMenu` pattern in the file.) — `src/plugins/file-explorer/FileExplorerView.cpp` — surfaced T-20

---

*Derived from audit dated 2026-04-26. Refresh after each major audit pass.*
*Shakedown findings (2026-04-28) appended below the audit body — fold into severity sections at next refresh.*
